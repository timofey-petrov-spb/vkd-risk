numpy==2.5.1
pandas==2.3.3
scipy==1.18.0
sgp4==2.27
skyfield==1.55
ppigrf==2.1.0
requests==2.34.2
plotly==5.24.1
streamlit==1.64.0
pytest==9.1.1

# PDF-отчёт НЕ входит в зависимости площадки. 19.09 в 19:33 добавление reportlab и pypdf
# совпало с падением развёрнутого сервиса («Error running app»), и восстановление службы
# важнее вёрстки отчёта. Сборка PDF в app/report_pdf.py ввозится отложенно и при отсутствии
# библиотеки кладёт в архив выгрузки честную причину вместо файла — это поведение уже
# проверено тестами. Локально отчёт собирается, если поставить:
#     pip install reportlab pypdf
