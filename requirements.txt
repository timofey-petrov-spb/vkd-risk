numpy==2.5.1
pandas==2.3.3
scipy==1.18.0
sgp4==2.27
skyfield==1.55
ppigrf==2.1.0
requests==2.34.2
plotly==5.24.1
streamlit==1.64.0
pytest==9.1.1
# Вёрстка отчёта PDF (app/report_pdf.py). Чисто питоновский пакет, колёс под площадку не
# требует. Версия закреплена по установленной и проверенной здесь: 5.0.0 (на PyPI есть 5.0.1,
# на неё не переходим без прогона — сборка отчёта проверена именно на 5.0.0).
reportlab==5.0.0
# Обратное чтение готового PDF. Нужен ПРОВЕРКАМ: тест извлекает из собранного файла текст и
# убеждается, что русские буквы читаются, а числа совпадают со снимком расчёта. Своим
# разборщиком это проверять нельзя — он повторил бы ошибку сборщика.
pypdf==6.14.2
