# Журнал: товарищ + Codex

Пишет только Codex. Читает Claude. **Дописывать только в конец.**

---

<!-- Первая запись появится после первой сессии Codex.
     Шаблон:

## ГГГГ-ММ-ДД ЧЧ:ММ — Codex

**Сделано:**
**Изменилось для товарища:**
**Нужно от товарища:**
**Заблокирован:**
-->


## 2026-09-18 00:55 — Codex

**Сделано:** изучены все файлы репозитория на базе e6fdb54, подготовлен `docs/REPOSITORY_REVIEW.md`. Основная среда — macOS arm64: Qt 6.11.0, Apple Clang 15, CMake 3.31.10, Ninja 1.13.2, Python 3.14.3 с инструментами подготовки данных, Tesseract 5.5.3 (rus/eng) и Poppler 26.09.0. Нативная сборка и запуск окна прошли; набор Qt-модулей проверен отдельной пробой SDK. В существующей Windows VM дополнительно собраны mono/dll и проверен запуск обоих окон. CTest находит 0 тестов. Код решения не писался.

**Изменилось для товарища:** добавлены инструкции `docs/DEVELOPMENT_ENVIRONMENT.md`, снимки версий и lock-файлы в `docs/environment/`, отдельные скрипты сборки/настройки Mac и проверки Windows. Для `.sh` зафиксированы LF. Текущий CMakeLists, общий договор, интерфейсы и чужие области не изменялись. Основная разработка Codex будет на Mac; Windows — только дополнительная проверка при необходимости.

**Нужно от товарища:** при сверке получить вывод `scripts/environment_report_windows.cmd` на вашей машине. В Git отсутствуют оригиналы ГОСТ 165/167 (5831.pdf и 4293854146.pdf), ОСТ 1044 и большинство прочих источников из списка; для оцифровки нужны эти файлы. По сборке: схема пресетов v6 требует CMake 3.25+, в исходных .bat жёсткие пути, тесты пока не добавлены. Перед реализацией прочитать замечания `docs/REPOSITORY_REVIEW.md`, особенно противоречия старого Python-плана, переход Si→ткань, обоснование эквивалентной защиты по ECSS 11.5d и получение среды во времени из усреднённых таблиц. Это перечень для следующего согласования, не изменение договора.

**Заблокирован:** установка основного Mac-окружения не заблокирована. Оцифровка ГОСТ 165/167 ждёт отсутствующие исходники; это не выполнялось в рамках подготовки среды.


## 2026-09-18 17:42 UTC — Codex

**Задача / версия:** редакция по efaa86d; ветка `codex/plan-coordination`;
локальный diff, без коммита/push. `CONTRACT.md` v3 и `vkd/types.py` v2
прочитаны целиком; Python-код и договор не менялись.

**Сделано:** проверено соответствие официальной постановке; исправлен
порядок работ в существующем плане без смены его структуры. Добавлены
`docs/PLAN_REVIEW.md` (аудит/источники), `docs/TEAM_SYNC.md` (ТЗ двух
участников/стык/интеграция) и `docs/PROGRESS.md` (честный статус).
Убраны отменённые обязательные правила из README/инструкций агентам;
старые исследования обозначены как материалы, а не текущая спецификация.
Это документация, не реализация или пройденная приёмка приложения.

**Решение по типам v2: НЕ ЗАМОРОЖЕНЫ, окончательно не согласованы.**
Структура подходит; минимальные правки перед заморозкой:
1. В `EventInterval` добавить `kind: Kind`, `valid_from_utc`, `valid_to_utc`;
   границы физического события, действие предупреждения и публикация различны.
2. Для Manifest закрепить реестр `source_id → raw_record_id → metadata`
   и получение по каждой записи: несколько выпусков одного источника
   должны сохраняться без потери версии/хеша/публикации/доступности.
3. Зафиксировать семантику и функции стыка с одним примером (R8).
После ревизии Claude — проверка Codex и отдельная запись согласования.
Большая переделка классов не нужна; независимые задачи можно делать сейчас.

**Карта покрытия:** подтверждены 28 файлов NOAA 3day_forecast 1–14 мая,
29 июньских начиная с 16 июня 12:30 UTC; daypre — 31 майский и 28 июньских
(нет 1 и 18 июня, соответствующие дни есть в предыдущих прогнозах).
Для отсечки 1 мая 00:00 нужен daypre 30 апреля, Issued 22:35 UTC.
Mid/High k не Kp; суточная вероятность не 6-часовая. Проверены заголовки
26 NASA OEM: 29 апреля + 13 майских + 12 июньских, EME2000/UTC;
перекрытие периода есть, но это не 26 независимых прогнозов и не готовый
интерполятор. Создание/дата S3/доказанная доступность/наше получение
раздельны. При недостатке доказательств орбита — реконструкция.
Три конкретных запроса с URL/временами записаны в обзоре.

**Выбор статистики:** единая методика ECSS Rev.1: Grün и сезонная
составляющая для коротких интервалов. Одна односторонняя случайно
ориентированная пластина 1 м²; масса ≥10⁻³ г; поток м⁻²·год⁻¹,
31 557 600 с/год, N безразмерно. Изотропная средняя модель не требует
экстраполяции табличного наклонения ГОСТ к 51,6°; для 2024 это климатология,
а не популяция конкретного года. Только природные метеороиды, не весь MMOD.
ECSS требует потоки для <3 недель; Grün уже включает их среднегодовой
вклад, поэтому простое сложение недопустимо. До проверки коэффициентов
и геометрии сезонной части — неполный охват, никакого вывода об отсутствии
метеорной активности. Контроль среднего компонента при 400 км/6 ч:
N = 5.609728448e-7; это проверка формулы, не конкретный выход.

**Изменилось для товарища:** Б — единственный интегратор main, владелец
оценки ECSS в `vkd/assess/`, UI/экспорта/экспериментов/общего lock и статуса.
А — данные/орбита/replay/методическая спецификация. Передача небольшими
ветками каждые 60–90 минут, плюс рубежи I0–I4; оба проверяют один SHA.
Остались ошибки договора R3–R7: sr в ОСТ, GOES по времени измерения,
IGRF-14 в строгой линии, допуски/суточные вероятности и неполный охват MMOD.
Предлагаемые формулировки в обзоре, сам договор не менял.

**Нужно от товарища:** закрыть R1/R2/R8, затем вернуть конкретную версию
типов/пример; согласовать точечные R3–R7; выбрать Python patch/lock/площадку.
Далее А делает три запроса I1, Б — каркас и потребителя общей фикстуры.

**Проверено:** сверка документов и первоисточников NOAA/NASA/ECSS/ОСТ;
подтверждение метаданных не объявляется тестом replay. Проверены локальный
Markdown-diff, ссылки и блоки Markdown: ошибок нет. Контрольные числа
ECSS независимо пересчитаны и совпали. Исполняемые
тесты приложения здесь не запускались: приложение не реализовывалось.

**Заблокирован:** заморозка типов ожидает минимальной ревизии Б; сезонный
компонент ECSS — проверки единиц/геометрии. Редакция документов завершена.
**Следующий шаг:** передача редакции Б; I0, затем историческая приёмка I1.


## 2026-09-18 17:47 UTC — Codex

**Задача / версия:** публикация редакции по efaa86d; ветка
`codex/plan-coordination`. Пользователь разрешил коммит и push всех
подготовленных изменений документации.

**Сделано:** комплект из 10 Markdown-файлов подготовлен для передачи через
GitHub; текущий статус обновлён с локального черновика на передачу веткой.
Предыдущая запись описывает состояние до разрешения публикации.

**Изменилось для товарища:** обновлённый план, ТЗ двух участников, протокол
интеграции и обзор R1–R8 доступны в передаваемой ветке. Python-код, типы
и договор остаются без изменений.
**Нужно от товарища:** проверить ветку, затем выполнить I0/R1/R2/R8 и
согласование остальных точечных замечаний.
**Проверено:** состав изменений ограничен Markdown; проверка diff перед
коммитом. Приёмка приложения этой публикацией не заявляется.
**Заблокирован:** публикация не заблокирована; вопросы реализации —
в `docs/PROGRESS.md`.
**Следующий шаг:** совместная проверка редакции и интеграция в main.


## 2026-09-18 20:35 UTC — Codex

**Задача / версия:** согласование ролей, порогов и типов; база
`10972c16a7a9c4e4a228ad481593fef790ee863f`, ветка `codex/agree-roles`.
Номера строк ниже относятся к этой базе. Прочитаны в запрошенном порядке
весь `docs/KRITERII_PLAN.md`, три последние записи `journal/tim.md`, весь
`vkd/types.py`; затем сверены `TEAM_SYNC.md` §1/§4 и первоисточники.

**Сделано:** правки построчно; безусловное согласование ролей и порогов
пока не даю. Общая граница А/Б, Streamlit Community Cloud без домена,
одна методика ECSS для природных метеороидов, необязательный SOCRATES,
три базовых режима и видео как страховка принимаются. Код и договор не менял.

1. **`vkd/types.py:123–135`, KRITERII:21 — принято, но ещё не внесено.**
   В `EventInterval` отсутствуют `kind`, `valid_from_utc`, `valid_to_utc`.
   Предложение: Б добавляет принятые поля; start/end относятся к физическому
   событию, valid_from/to — к действию сообщения, published — к публикации.
   Источник: `docs/PLAN_REVIEW.md:27`, постановка, с. 3. Типы v2 не заморожены.

2. **`vkd/types.py:65–68` — реестр по выпускам не закреплён.**
   Тип dict технически допускает вложенность, но комментарии ещё описывают
   одну версию/получение на источник. Предложение:
   `source_versions[source_id][raw_record_id] → metadata`,
   `fetched_utc[source_id][raw_record_id]`; metadata содержит URL, версию,
   SHA-256 исходных байтов, публикацию, доказательство доступности и путь raw.
   В снимке нужны версия схемы и эффективная конфигурация с defaults.
   Источник: `PLAN_REVIEW.md:28,42–51`, `TEAM_SYNC.md:50–70`.
   До заморозки Б также фиксирует функции стыка и один пример (R8).

3. **KRITERII:311, запрос Claude про lock — неверный владелец.**
   Заменить «Б, lock от А» на «Б — зависимости/lock/развёртывание и комплект
   воспроизведения; А проверяет на macOS». Задачи B1/B4/B6; I0 — среда,
   I2 — минимальный экспорт, I4 — окончательное воспроизведение.
   Источник: `TEAM_SYNC.md:32,35,37,41–42,118–122`.
   KRITERII:235: Python на Community Cloud выбирается в Advanced settings,
   `runtime.txt` не использовать как подтверждённый способ выбора версии;
   requirements экспортировать из зафиксированных зависимостей, не только >=.
   Источник: https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/deploy

4. **KRITERII:303,325 — сценарий и тип ошибочно назначены А/A2.**
   Предложение: «Б/B1 пишет схему; Б/B3+B4 реализует сценарий; А проверяет
   происхождение и контрольный пример». A2 остаётся историческим отбором.
   В краткую границу KRITERII:289–290 добавить TrajectoryMeta и реестр raw.
   Источник: `TEAM_SYNC.md:18,25,32–35,42,60–70`.

5. **KRITERII:258,325; `types.py:127` — simulated не физический тип события.**
   Строка `kind_of_event="simulated"` допустима Python, но теряет SEP/GST/CME
   и не позволяет прежнему обработчику определить механизм. Предложение:
   сохранять физический `kind_of_event`, добавить отдельный
   `is_simulated: bool = False`; идентификатор сценария и происхождение
   синтетических входов — в metadata/settings снимка. `Kind.OWN_CALCULATION`
   без флага не отличает сценарий от обычного расчёта. Метка сценария нужна
   всему результату и экспорту; синтетические значения не попадают в live
   кеш и строгий replay. Отказ источника меняет статус источника, задержка
   работ — Request, скачок Kp/потока требует значения и единиц; один
   EventInterval не представляет все эти изменения. Источник:
   `types.py:19–22,50–57,105–135`, `TEAM_SYNC.md:35,65–70`.

6. **KRITERII:257,298,321 — S2 не подтверждает приказ прервать ВКД.**
   NOAA S1/S2/S3 соответствуют >=10/100/1000 pfu для протонов >=10 MeV;
   в S2 нет указанного правила прерывания ВКД, рекомендация избегать
   радиационной опасности для ВКД появляется в S3. Предлагаемая формулировка:
   «S>=3 по актуальным качественным наблюдениям/подтверждённому ALERT —
   приоритетное предупреждение, окно требует срочной проверки специалистом;
   S1–S2 — предупреждение о протонном событии. Их исключение из автоматического
   выбора — консервативное правило прототипа, не эксплуатационная норма».
   Автоматические «ВКД прерывается» и «идущая ВКД не прерывается» убрать:
   ни продолжение, ни возвращение экипажа из этих индексов не следуют.
   Два класса можно оставить как приоритет проверки; отдельный статус
   недостатка данных сохраняется. Источник:
   https://www.spaceweather.gov/noaa-scales-explanation
   Наблюдение сейчас не распространяется молча на всё будущее окно.
   Для прогноза S>=1 использовать явный WARNING и пересечение Valid From/To,
   не ненулевую суточную вероятность. Нужны качество, первичный аппарат,
   энергетический канал и актуальность; сырые выбросы не называем ALERT.
   Источник: https://www.spaceweather.gov/products/goes-proton-flux

7. **KRITERII:257 — Kp>=7 подтверждается как G3, но не как запрет ВКД.**
   Предложение: «Kp>=7 — триггер дополнительной проверки условий модели,
   орбиты/связи; сам по себе не означает запрет или отмену ВКД». Если команда
   исключает такие окна, обозначить собственную консервативную политику и
   проверить чувствительность к ней. Не выдавать её за норму NOAA/ГОСТ.
   Kp относится к трёхчасовому интервалу; Mid_k/High_k не заменяют Kp.
   Источники: https://www.spaceweather.gov/noaa-scales-explanation и
   https://www.spaceweather.gov/products/planetary-k-index .

8. **KRITERII:257,264 — TCA внутри окна не доказывает опасное столкновение.**
   Предложение: «качественное сообщение о потенциальном сближении МКС,
   пересекающем окно, требует ручной оценки; учитываются расстояние,
   неопределённость/ограничения продукта, версия и время выпуска».
   Не вводить произвольную границу промаха как доказанную вероятность
   попадания; отсутствие строки в усечённой выдаче не доказывает отсутствие
   сближений. Источники: CONTRACT §3/§9;
   https://www.nasa.gov/cara/step-1-conjunction-event-prediction/ .

9. **KRITERII:256 — часовая контрольная доза нужна только как контекст.**
   ГОСТ 25645.215-85, п. 2.3, приложение 1 термин 6, приложение 2:
   для полёта 6 мес приведена контрольная часовая равноценная доза 85 мкЗв
   (средняя скорость 85 мкЗв/ч при таком представлении). Она определена
   через накопление за весь полёт и нормативный риск, не как аварийный
   предел отдельной ВКД. П. 3 требует дозиметрических средств; GOES pfu,
   Kp и B не являются измерением этой дозы. Предложение: отдельный порог
   отмены/прерывания не вводить; показывать справочно с длительностью полёта,
   видом дозы и границами. Оригинал, PDF с. 4–6 (печатные 2–4):
   https://files.stroyinf.ru/Data2/1/4294828/4294828777.pdf .
   Для «500 мЗв/год по ООКОКП-2004» первичный текст с конкретным пунктом
   пока не проверен: убрать из подтверждённых нормативов либо явно
   обозначить вторичный источник. Полная панель норм — усиление;
   источник/применимость реально используемых правил — ядро.

10. **KRITERII:264,301,102,268 — поправить уровни готовности.**
    SOCRATES: «необязательная линия/усиление после ядра» вместо «ядро»
    (источник: KRITERII:19, CONTRACT:61, TEAM_SYNC:21).
    О5/I3: один полный путь пользователя; переключатель двух уровней —
    после ядра, как уже указано в KRITERII:262 (TEAM_SYNC:35,121).
    Численная сходимость и обоснование допуска равнозначности — ядро
    I2–I4; интерактивная панель чувствительности — фича после I4.
    Смена канала GOES меняет показатель, а не определяет его погрешность
    (TEAM_SYNC:34,122; PLAN_REVIEW:R6). Режим «Что если» — усиление;
    базовые сбои/отключение и пересчёт длительности остаются в ядре.

11. **KRITERII:255 — «все четыре покрыты» и ГОСТ вне применимости.**
    Предложение: «обязательный охват — космопогода и природные метеороиды;
    сближения отдельно, техногенная статистика пока вне охвата». Исследование
    ГОСТ 167 вынести после I4: подпись «вне области применимости» сама
    по себе не делает численный результат пригодным. Одна ECSS сохраняется;
    для короткого выхода нужны сезонные потоки и исключение их повторного
    среднегодового учёта либо явное неполное покрытие. Источники:
    KRITERII:20, TEAM_SYNC:21,33; PLAN_REVIEW §4; ECSS-E-ST-10-04C Rev.1
    §10.2.2.2c, Annex C, примечание к C-7.

12. **KRITERII:260,306,323 — 24 ч не предел всех исходных данных.**
    Предложение: «период начала до 24 ч; траектория и пригодные прогнозы
    сохраняются до конца последнего окна (до 32 ч); фактический горизонт
    каждого канала показан отдельно». Нельзя обрезать пригодный прогноз
    на 24 ч и затем оценивать целиком окно, начинающееся через 24 ч.
    Обязательный прогноз/расчёт минимум на 6 ч хотя бы по одной линии,
    расширение до суток — при покрытии. Источники: постановка с. 3,
    TEAM_SYNC:19, CONTRACT §0.

13. **KRITERII:154,156,307 — строгая история и контрольные даты.**
    «GOES по времени наблюдения» заменить на «доступная к cutoff конкретная
    версия; иначе только разбор/проверка после факта». Исключать поздние
    наблюдения/редакции, но оставлять прогнозные значения своевременного
    выпуска. Разрыв относится к выпускам 3day_forecast, а не всем величинам;
    daypre обеспечивает собственные каналы, не Kp (PLAN_REVIEW:R4, §3).
    Демо 3 мая/1 июня/25 июня принимаю с явной отсечкой 12:00 UTC каждого
    дня, длительностью 6 ч и поиском 24 ч. Для I1 сохранить граничные
    01.05 00:00Z и 30.06 12:00Z и проверку середины разрыва 20.05 12:00Z
    из TEAM_SYNC:119/PLAN_REVIEW §3; новые демодаты дополняют этот набор.

14. **KRITERII:58,299 — потерян отдельный исход all_need_check.**
    Предложение: сохранить пять исходов из Recommendation, включая
    «все варианты требуют проверки», и отличать его от недостатка данных.
    Источники: types.py:195–203, TEAM_SYNC:34; постановка с. 3.
    KRITERII:178–180: отказ с пригодным кешем не обязательно даёт PARTIAL
    или отказ от рекомендации; демонстрация insufficient — при отсутствии
    пригодного кеша/резерва (TEAM_SYNC:20,125–126).

15. **KRITERII:266–267,313–315 — не заявлять ещё не доказанное.**
    «Локальный запуск уже работает без внешней зависимости» заменить на
    план B1/B6: app/main.py пока нет; автономный повтор возможен по
    сохранённому набору, текущие данные требуют сети. «Стоимость ноль» —
    «без обязательных платных API/GPU, бесплатный тариф с ограничениями».
    Суммы баллов по людям удалить: 7+6+8+половина5=23,5, а не26;
    официального деления общих критериев между людьми нет. Источники:
    дерево10972c1; TEAM_SYNC:32,37; опубликованные критерии Т1/Т2/Т4/Т6.

16. **Запрошенные однострочные формулировки R3–R7 для владельца договора:**
    R3: «Приложение А ОСТ: всенаправленный поток част./(см²·с·МэВ),
    флюенс част./см²; GOES pfu хранится отдельно, без произвольного 4π».
    R4: «В строгом режиме допустима только доказанно доступная к cutoff
    версия каждого использованного значения, времени наблюдения недостаточно».
    R5: «Строгая магнитная линия 2024 использует IGRF-13; IGRF-14 допустима
    лишь в отдельной реконструкции, не улучшающей скрыто строгий прогноз».
    R6: «Допуск сравнения проверяется для фиксированного показателя/порога/
    геометрии; будущие наблюдения и поздние редакции исключены, своевременные
    прогнозы будущего разрешены, суточная вероятность не есть событие в окне».
    R7: «ECSS оценивает природные метеороиды на одну опорную пластину;
    непроверенные сезонные потоки и техногенный мусор явно вне полного охвата».
    Источники и обоснование: `docs/PLAN_REVIEW.md:29–33,53–58,104–180`.
    `CONTRACT.md` оставлен без изменений, как запрошено.

**Проверено:** матрица всех 15 критериев, §5 (14 пунктов), типы v2,
первичные NOAA/SWPC, NASA CARA, оригинал ГОСТ с визуальной проверкой таблицы,
официальная документация Streamlit. Оставшиеся строки матрицы согласуются
с областями А/Б; научные пороги и заморозка типов требуют правок выше.

**Изменилось для товарища:** вместо ложного полного согласования передан
точный список; зависимости/lock остаются у Б. A1 не ждёт правок типов.
Схема реестра A1 будет независимой от dataclass, с раздельными публикацией,
получением, версией/хешем и пригодностью к строгому replay.

**Нужно от товарища:** внести минимальные изменения типов/семантики и
исправить указанные строки; отказаться от неподтверждённых команд
прервать/продолжать ВКД; интегрировать только эту запись в main.

**Заблокирован:** начать A1 ничего не мешает. Независимая подготовка A2
(публикации/версии/отбор raw) тоже не блокирована; заморозки требует только
окончательная интеграция с потребителем. Публикация конкретной версии,
если не доказана, отмечается неизвестной, не выдумывается.

**Следующий шаг:** после push этой записи немедленно начать A1 в отдельной
ветке `codex/a1-sources` от базового main: сохранить архивные выпуски,
реестр SHA-256/публикаций/версий и карту покрытия мая–июня 2024.


## 2026-09-18 21:01 UTC — Codex

**Задача / версия:** A1/A2, база `10972c1`, ветка `codex/a1-sources`, локальный diff без коммита. Запись согласования ранее отправлена отдельно: `d84564b87045b7a07d0e486aa7110d3fdea441a2` в `codex/agree-roles`.

**Сделано:** начата реализация параллельно B1. `vkd/sources/registry.py` загружает и проверяет архив A1 (390 записей: NOAA, NASA OEM, DONKI), отдаёт копии метаданных и повторно проверяет raw при чтении. `vkd/sources/noaa.py` разбирает реальные таблицы 3day_forecast/daypre с единицами и исходным временным разрешением. `vkd/history/replay.py` выбирает последний допустимый выпуск по публикации и доступности, отдаёт значения выбранного канала, покрытие и пробелы. Будущие прогнозные значения раннего выпуска сохраняются; поздние публикации/версии исключаются. OEM и непроверенный DONKI не допускаются в строгую прогнозную линию. Суточные вероятности остаются суточными, Mid/High k не подменяет Kp. Разные версии с одной публикацией дают ambiguous, порча — invalid. Полного replay приложения и I1 пока нет.

**Проверено:** `/Users/aleksandrboda/.local/share/vkd-risk/tooling/bin/python -m unittest discover -s tests -t . -v`; Python 3.14.3, macOS; 25 тестов успешно. Все 117 выпусков NOAA разобраны: 6561 числовая ячейка. Шесть отсечек + новая дата; численное покрытие 12/32 для 16 мая 12:00 UTC; поздние выпуски/редакции не меняют результат; аномалия Issued 16 июня 12:50; повреждение SHA; независимость копий. Пример вызова: `vkd/history/README.md`.

**Изменилось для товарища:** появились работающие внутренние функции данных и архив. Нового общего ABI/dataclass не вводил, `vkd/types.py`, файлы B1, CONTRACT и main не менял. Модуль использует только стандартную библиотеку Python, зависимостей/lock не меняет. Результат содержит record/cells/status/gaps/coverage_fraction, но это внутренний отчёт данных, не Recommendation. Код и архив пока локальные, не опубликованы; ранее опубликована только запись согласования.

**Нужно от товарища:** продолжать B1; после фиксации общего вызываемого стыка подключаем адаптер к готовому отбору/парсеру. Не ждать полной готовности A2 для каркаса. В shared Manifest переносить provenance исходных записей, а не один source_id на все выпуски.

**Заблокирован:** нет для дальнейшей реализации A. Историческая публичная доступность OEM ещё не доказана, архив GOES и семантика DONKI остаются работой A1/A2; это явно ограничивает полноту строгого режима, но не останавливает разработку.

**Следующий шаг:** расширить A2 содержательным разбором датированных сообщений и подготовить подключение к стыку B1; затем независимый расчёт орбиты A3.


## 2026-09-18 21:30 UTC — Codex

**Задача / версия:** A3 + спецификация A5, база `10972c1`, ветка `codex/a1-sources`, локальный diff. После fetch проверен стык B1 в `origin/main` на `652768d`; merge/pull, коммиты и push этой работы не выполнялись согласно просьбе отложить синхронизацию. Типы v2 и CONTRACT не изменены.

**Сделано:** реализован `vkd/orbit/`: SGP4/WGS72 для ISS TLE; исторический NASA OEM с интерполяцией положения/скорости и преобразованием EME2000 → ITRS/WGS84; поле IGRF-13 для 2024 и IGRF-14 для текущих дат; согласованные дипольные L, B/B0 и вертикальная жёсткость обрезания со статусом approximation. Никакой подстановки текущего TLE в 2024, экстраполяции OEM или молчаливых клампов. Входы и коэффициенты фиксированы хешами. Три аргумента `trajectory(start_utc, minutes, saa_B_threshold_nT)` возвращают существующие типы, максимум 32 часа / 1921 точка с конечной точкой. Полный отчёт происхождения — `trajectory_with_provenance`, инструкция в `vkd/orbit/README.md`.

Отдельно, методическая работа A5: `docs/methods/METEOROIDS_GRUN_SPEC.md` фиксирует одну модель/геометрию/порог: ECSS Grün, случайно ориентированная односторонняя пластина 1 м², m ≥ 0,001 г. Формулы, J-6 и контроль N переданы для реализации Б, не продублированы в assess. 30 пар J-5 и 49 фактических строк C-2 сохранены в `data/meteoroids/`. Разрешена неоднозначность единиц McBride по первичному руководству ESABASE2: масса кг, поток м⁻²·с⁻¹; Grün — масса г. Подтверждено вычитание среднегодового вклада потоков перед добавлением текущего. Блокер среднего фонового N закрыт; сезонный охват не объявляется полным.

**Проверено:** `/Users/aleksandrboda/.local/share/vkd-risk/orbit-dev/bin/python -m pytest -q` — 37 тестов и 559 subtests успешно, 3,35 с; в том числе A3 — 12 тестов и 38 subtests. Python 3.14.3/macOS, точные версии — `data/orbit/validation_environment.json`; это не общий lock и не проверка развёртывания Python 3.12. Опубликованные векторы Vallado AIAA 2006-6753 (ближний/глубокий SGP4), независимый синтез pyIGRF с одинаковыми коэффициентами, все 26 OEM, шесть исторических стартов по 32 ч, согласованность сеток 30/60 с, отказы при устаревании/недоказанной публикации/неверных входах. Исходное различие встроенных коэффициентов pyIGRF/ppigrf выявлено и описано, допуск не расширялся. Контроль арифметики Grün: N(400 км, 6 ч) = 5.609728448352106e-7; 30 округлённых J-5 дают максимальное расхождение 0,488%. Проверены хеши орбитальных входов/фикстур; `git diff --check` без ошибок. Физическая точность относительно наблюдаемой МКС/поля этими численными тестами не заявляется.

**Изменилось для товарища:** после интеграции импорт B1 автоматически найдёт реальный модуль вместо stub. Для выбранного строгого режима Б должен передать `mode='history_forecast', cutoff_utc=...`; автоматический режим на 2024 — явно обозначенная реконструкция. Сейчас все OEM без доказанной публичной доступности, поэтому строгий вызов возвращает OrbitDataError и должен отображаться как недостаток покрытия. Ошибку расчёта нельзя превращать в разрешение использовать stub. Перенести `provenance.records` (включая коэффициенты) в общий Manifest. Конечная точка горизонта включена: интегрировать интервалы, не добавлять лишнюю минуту по количеству точек. Средний N можно реализовать сразу по A5; частичный сезонный охват не скрывать. `L/B/B0/cutoff` — дипольные приближения, не трассировка IGRF и не нормативная точность. Файлы Б, общий договор, зависимости/lock и main не изменены.

**Нужно от товарища:** при следующей плановой интеграции подключить A3 и реализовать B2 среднего N по спецификации/контрольным данным; переносить ограничения модели и происхождение в UI/экспорт. До синхронизации код и материалы остаются только локально у А.

**Заблокирован:** для дальнейшей разработки А — нет. Строгая историческая орбита ограничена отсутствием доказанной публикации OEM; проверка источников — А. Для полного сезонного A5 остаётся подтвердить нормировку направленного потока на площадь, перевод к случайной пластине и согласованные движение/затенение/годовое среднее; владелец методики — А. Не заменять этот пробел нулём. Сетевого обновления TLE в A3 нет: снимок ограничен возрастом, автоматическое получение входит в A4.

**Следующий шаг:** A4 — обновляемый источник TLE с тайм-аутами, кешем, давностью и происхождением; точка входа текущего потребителя — `vkd/orbit/trajectory.py`, чтение `data/orbit/manifest.json`.


## 2026-09-18 22:08 UTC — Codex — публикация и сверка А/Б

**Задача / версия:** синхронизация по явному поручению пользователя. Ветка `codex/a1-sources`: А опубликован в `d095c7a`, Б на `386ed17`, merge `19ef170`; последующий коммит содержит исправления стыка и этот отчёт. Main не изменялся. Единственный конфликт журнала разрешён сохранением обеих серий записей.

**Сделано:** модули А объединены с актуальным приложением Б. Исправлена новая сигнатура `trajectory(..., tle_path=None)` — live использует переданный TLE, история OEM. Добавлены параметры `oem_raw_record_id` и `expected_record_hashes` для закреплённого повтора; изменение набора исходников/хешей даёт отказ. Исходная система `meta.frame` теперь TEME/EME2000, выход WGS84 в provenance. Рабочие файлы Б и CONTRACT вручную не редактировались; обновления Б пришли merge.

**Проверено:** `python -m pytest -q` — 80 passed, 559 subtests passed, 11,45 с. Установлены точные пакеты из requirements.txt Б, Python 3.14.3/macOS; совпадение Python 3.12 и проверка облака пока не заявляются. Три исторических сквозных запуска (03.05, 20.05, 30.06 с 32 ч), live с закреплённым TLE и отсутствующими наблюдениями, экспорт ZIP, начальный экран Streamlit AppTest без исключений. Результаты и версии — `docs/integration/SYNC_CHECK_2026-09-19.json`. 03.05 при PARTIAL космопогоды получается preferred: это выявленный дефект политики достаточности, а не успешная научная приёмка.

**Изменилось для товарища:** типы `vkd/types.py` версии 2.1 — **согласовано**. Ответ на чек-лист Б: (1) средний Grün, геометрия, масса, год и J-6 — ок; сезонный охват не закрыт; (2) S1/S3/Kp7 — ок как политика проверки, остаётся ограничить условия по времени/давности; (3) НЕ ок: 95% нельзя называть полным обязательным охватом, отсутствие строк в DONKI не доказывает отсутствие воздействия, PARTIAL не равно полноценному резерву; (4) конец интервала Kp и исключение наблюдений без публикации — ок; (5) схема 2.1 согласована, наполнение экспортного реестра ещё нет; (6) старые примеры относятся к stub-орбите, после OEM нужно пересоздать и закрепить новую версию. Полный короткий список с местами кода и владельцами — `docs/integration/SYNC_2026-09-19.md`.

**Нужно от товарища:** принять ветку в main, затем подключить `trajectory_with_provenance` к экспорту и pinned OEM replay; исправить правила достаточности/сезонную полноту и время действия условий. Сейчас ZIP содержит TLE вместо фактически использованного OEM; источники и история приложения всё ещё из experiments/stub_sources и stub_history. А подключает архивные источники к потребителю отдельным следующим шагом. Согласование типов не означает согласование этих дефектов.

**Заблокирован:** разработка А не заблокирована. Общая приёмка I1–I4 не пройдена: экспорт и политика достаточности — Б; типизированный адаптер NOAA/исходных DONKI и сезонная нормировка — А. Успешные тесты не являются основанием скрыть эти пункты.

**Следующий шаг:** после принятия объединённого SHA — А: адаптер A1/A2 к приложению с request/cutoff/горизонтом; Б: экспорт/повтор и полнота по отчёту синхронизации.


## 2026-09-18 22:11 UTC — Codex — дополнительный одновременный push Б

**Задача / версия:** обновление сверки; `a9d464f` Б объединён в `98f5801`, ветка `codex/a1-sources`, PR #1.
**Сделано:** включены последние изменения Б; отчёт синхронизации актуализирован. Б уже исправил coverage_missing и доступность панели сценария/порогов в истории; замечание о старой подписи ECSS снято.
**Проверено:** на обновлённой сборке 80 passed + 559 subtests, 10,88 с; Streamlit AppTest переключён через все три режима без исключений, источники закреплены для проверки без сети.
**Изменилось для товарища:** отчёт `docs/integration/SYNC_2026-09-19.md` учитывает a9d464f. Остальные пункты экспорта/полноты/адаптера остаются.
**Нужно от товарища:** принять PR #1 и продолжить пункты Б из отчёта.
**Заблокирован:** разработка не заблокирована; общая научная приёмка ещё не завершена.
**Следующий шаг:** А — адаптер архивов; Б — экспорт и достаточность.


## 2026-09-18 22:38 UTC — Codex — A2, содержательный разбор уведомлений

**Задача / версия:** A2, база cb0b222, ветка codex/a2-history-adapter. После проверки получен новый main 2d20793; совместная проверка с ним следует отдельной записью. Типы 2.1 согласованы ранее и не менялись.

**Сделано:** `vkd/sources/donki.py` читает оригинальные тела уведомлений, без подстановки поздних карточек. Все 247 сообщений классифицированы: 166 разобраны, 71 не относится к земной линии/каналу GOES, 9 обзоров, 1 конфликт публикации. Kp — фактические 3-часовые интервалы; SEP — пороговые события с отдельными каналами 10/100 МэВ, а не значения потока; CME — время прихода к Земле, отдельно диапазон неопределённости. Неизвестные конец/действие остаются None. `vkd.history.history_bundle()` автоматически заменяет старую DONKI-заглушку; `history_snapshot(Request)` дополнительно отдаёт typed NOAA, реестр, точные raw в base64, аудит, пропуски по каналам. Численный GOES не выдуман. CLI `python -m vkd.history` сохраняет снимок.

**Проверено:** на исходной объединённой базе — 101 passed, 670 subtests, 12,51 с; все три режима Streamlit без исключений. Восемь отсечек по 32 ч; проверены поздние версии, секундная публикация, будущие наблюдения, другой аппарат/энергетический канал, неизвестные концы, негированный приход CME, хеш каждого факта, порча и изоляция сессий. Полный пример 10.05 19Z: 73 численных значения, 24 сообщения-события, 26 исходных записей; `data/source_registry_2024/a2/`.

**Изменилось для товарища:** код использует только согласованные EnvironmentSample/EventInterval. В no-arg bundle — исходные уведомления вместо карточек; `raw['_history']` содержит coverage_map/source_versions/аудит. Нельзя объявлять полный каталог наблюдений по отсутствию сообщений. Порог SEP >100 МэВ не становится уровнем S по >10 МэВ; структурированные факты — `source_versions[source_id][rid].content_audit.facts`, без разбора русского note. Автоматическое и ручное сообщение могут относиться к одному событию: это выпуски, а не два физических воздействия. Предоставленный Request-адаптер NOAA можно использовать отдельно; подключённый Б noaa_forecast переписывать ради него не требуется.

**Нужно от товарища:** сохранять аудит/покрытие DONKI при экспорте; убрать искусственный конец события +24 ч и константную полноту каталога. По Р-О1: согласна объявленная реконструкция OEM отдельной линией при недоказанной доступности; она не становится строгим орбитальным прогнозом и не участвует как доступная тогда орбита в доказательстве заблаговременности. По R9: прогноз Kp≥7 допустим как явно подписанная политика прототипа при пересечении исходных 3-часовых ячеек с окном; не эксплуатационный запрет и не наблюдение. Неполный охват этого прогноза остаётся явным. Типы 2.1 — согласовано.

**Заблокирован:** нет для A4. Остаются исторические численные GOES, сезонная нормировка A5 и общая достаточность у Б; тело уведомления не доказывает непрерывный мониторинг или неизменность всех версий с 2024 года.

**Следующий шаг:** проверить A2 вместе с 2d20793, затем A4 — живые TLE/GOES/Kp с проверкой данных и метаданными кеша.


## 2026-09-18 22:43 UTC — Codex — A2 после интеграции

**Задача / версия:** A2, коммит ef196da, merge main 2d20793 → 721f86b, ветка codex/a2-history-adapter.
**Сделано:** сохранены орбитальный мост, NOAA и интегрирование метеороидов Б; A2 автоматически подключён вместо stub_history. Конфликт только в журнале, все записи сохранены.
**Проверено:** общий pytest: 110 passed, 670 subtests, 1 failed (49,23 с). Падение tests/test_replay.py::test_history_forecast_is_deterministic_with_pinned_tle: исторический run делает живые запросы, изменяемые сетевые/кеш-статусы попадают в снимок. Все три режима Streamlit с закреплёнными входами — без исключений.
**Изменилось для товарища:** это реальное ограничение воспроизводимости, не ошибка физических результатов A2. Собственные A2-тесты прошли; общий набор полностью зелёным не объявляется.
**Нужно от товарища:** в истории не получать неиспользуемые текущие GOES/Kp/TLE; отделить текущую диагностику транспорта от воспроизводимого исторического снимка. Сохранить замечания A2 по DONKI из предыдущей записи.
**Заблокирован:** нет для A4; сеть CelesTrak ответила 403 с указанием временной блокировки частых запросов. Обхода блокировки не делаем: нужен кеш и ограничение частоты не чаще двух часов.
**Следующий шаг:** A4 — пригодность текущих данных, проверяемый кеш и восстановление после отказа.


## 2026-09-18 23:08 UTC — Codex — A2/A4, передача реализации

**Задача / версия:** A2 v2 + A4 v1, ветка `codex/a4-live-sources`; ядро A4 c3c8569, merge актуального main 73a2ec8 → 8ad1606, дальнейшие правки и отчёт в следующем коммите. Все текущие правки коллеги сохранены. Типы 2.1 — согласовано.

**Сделано:** приложение автоматически использует `vkd.history` и `vkd.sources`. A4: GOES ≥10 МэВ, завершённый 3-часовой GFZ Kp, проверенный ISS TLE; отдельно готов `noaa_latest` с 27 исходными прогнозными ячейками. Тайм-ауты/ограниченные повторы, обработка 403/429/Retry-After, давность измерения/эпохи отдельно от получения, отключение на вызов, атомарный кеш и межпроцессная блокировка Mac/Linux/Windows. SHA проверяется при чтении; публикация наблюдений не выдумывается из mtime. Настройки источников из TOML Б учитываются и фиксируются в receipt. CelesTrak не опрашивается чаще двух часов, non-200 не повторяется внутри вызова. A2: `CME_ARRIVAL` согласован с потребителем; диапазон Kp из исходного абзаца о Земле сохранён в facts и note, без поздних ENLIL-карточек.

**Проверено:** `python tests/sources/run_pinned_suite.py` — 158 passed + 670 subtests, Python 3.14.3/macOS; это полный набор с закреплёнными входами приложения, внутри тестов источников проверяются сетевые ответы/отказы через управляемый transport. Три режима Streamlit AppTest без исключений. Реальные GOES/Kp/NOAA получены; CelesTrak вернул 403, работает проверенный TLE-резерв с исходным fetched и ограничением возраста. Реальный Windows/облако не проверены; Windows-lock проверен mock. JSON-проверки и инструкция — docs/integration/A2_A4_HANDOFF_2026-09-19.md, data/live_sources/verification.json. Обычный незакреплённый прогон после 73a2ec8: 153 passed, 670 subtests, 1 failed из-за живой диагностики в историческом снимке; закреплённый прогон не выдаётся за исправление этой ошибки.

**Изменилось для товарища:** брать итоговую ветку A4 — в ней A2 v2 и оставшиеся A3-исправления PR #1; промежуточная ветка A2 только v1. `tle_latest` возвращает ДВА значения `(text, Fetch)`, GOES/Kp — три. A4 raw теперь самодостаточный `{metadata, encoding, content_base64, selected}`; metadata направлять в source_versions[source_id][raw_record_id]. Общий manifest.json/iss.tle не перезаписываю: сохраняется staging Б, исходные A4 bytes/receipt передаются отдельно. `noaa_latest` ещё требует вызова в live-пути Б. Три сравнения с его прежним stub: 03.05 и 20.05 — без лишних условий; 10.05 12Z — оба окна требуют проверки по опубликованным CME-сообщениям. Перечни источников различаются, старые показатели Т5 не переносим на новый адаптер.

**Нужно от товарища:** (1) отделить историю от текущих сетевых вызовов/статусов; (2) сохранить `_history` audit/coverage и A4 source_versions в экспорте; (3) для GST читать `facts.kp`/EnvironmentSample: regex только «Kp до» не читает точное «Kp=», а 100 МэВ SEP не смешивать с ≥10 МэВ; (4) убрать из исходных фактов искусственные +24 ч/полное покрытие DONKI либо явно выделить отдельную политику; (5) Т5 запускать через A2, сейчас experiment импортирует stub. Подробные места и проверки — в HANDOFF.

R10: внешний прогноз — да, но modelCompletionTime не доказанная публикация версии enlilList. A2 берёт диапазон из датированного уведомления; его верхняя граница явно не kp_90. kp_90 — сценарий clock angle 90°, не автоматически «типичный»/медианный прогноз (NASA CCMC, слайд 13: https://ccmc.gsfc.nasa.gov/publicData/SWxWorkshops/2014/Mays_Demo_ExtraSlides_201409.pdf). Подбор по контролю превращает его в настройочную выборку; нужна независимая проверка. R11: эксцентричный диполь пока только объявленное приближение; Б должен исправить геодезическая широта → ECEF (сейчас сферическая формула) и согласовать подписи cutoff_GV, оставшегося от центрального диполя при замене L/B/B0. Ненулевой флюенс не заменяет эталонную валидацию. Р-О1/R9 — согласованы в ранее записанном ограниченном смысле.

**Заблокирован:** развитие А не заблокировано. Полная приёмка A4 требует Windows Б и облака. Численный GOES-2024 и сезонная нормировка A5 ещё не закрыты. Достаточность, экспорт, временное действие условий и Т5 — действия Б; main и его файлы вручную не изменял.

**Следующий шаг:** А — численный архив GOES мая–июня 2024 с проверкой доступности/покрытия, затем сезонная нормировка A5. Б — интеграция переданного SHA и перечисленные пункты стыка.


## 2026-09-18 23:40 UTC — Codex — численный GOES и сезонная спецификация

**Задача / версия:** A1 GOES + A2 `history-a2-v3` + A4 `live-a4-v2` + A5 `ecss-streams-audit-v1`; ветка `codex/a1-goes-history`, база merge `c832254` (main `a7f8a62` + предыдущая передача A4 `369bc03`), передаваемые правки — следующий коммит. Типы 2.1 неизменны.

**Сделано:** сохранены 65 точных суточных CSV NASA iSWA primary GOES за 29.04–02.07 и JSON схемы, 66 записей отдельного A1-реестра: SHA-256, URL, версия, время получения, поддержка измерений, идентификаторы протонного спутника. P10 ≥10 МэВ, pfu; 18 400 пригодных значений, 12 плохих строк исключены, пробелы не заполнены. В мае–июне временной охват 98,3436%, крупнейший разрыв 24.06 21:25–25.06 12:00 UTC. A2 получает ряд в history_review; history_forecast исключает весь этот архив, поскольку нет публикации/исторической доступности версии. A4 поддерживает три состояния источников и список TLE URL, оба JSON-формата, фактический URL и отдельный SHA извлечённого TLE, общий CelesTrak gate без обхода через другой URL. A5: новая сезонная спецификация с единицами/геометрией/профилями/скоростью/тенью/годовым вычитанием и инженерной v0 для немедленной реализации Б; исходный каталог не исправлялся молча. Полная физическая валидация не заявлена.

**Проверено:** `python tests/sources/run_pinned_suite.py` — 187 passed, 685 subtests (macOS, Python 3.14.3; закреплённый транспорт приложения). `python -m pytest tests/sources/test_goes_archive.py tests/history/test_bundle.py -q` — 22 passed, 123 subtests: реальные численные точки, пропуски, raw/схема/хеши, типы времени, полуоткрытые интервалы, три даты, 32 ч + 3 ч задержки, исключение будущей информации. Streamlit AppTest 3 режима × on/cache/off — без исключений; замечание bool-приведения ниже остаётся. `seasonal_controls.py --check` — 49 рядов, 3 интеграла проекции, 3 скоростных случая PASS; `seasonal_solar_controls.py --ephemeris /path/de421.bsp` — 6 дат DE421/USNO PASS, max 0,322 arcmin. В `data/goes_2024/validation/` дополнительная проверка 6 меток/значений P500 против оригинального NOAA GOES18 NetCDF; P500 не выдан за P10. Подробности и команды — `docs/integration/A1_A5_HANDOFF_2026-09-19.md`, `data/goes_2024/coverage.json`.

**Изменилось для товарища:** интегрировать эту ветку целиком — в ней предыдущие A2/A4/A3 и актуальный main a7f8a62. Для численного исторического GOES нужен `history_snapshot(request)` или `history_bundle(request)` с реальным Request, включая задержку; no-arg остаётся DONKI-каталогом. GOES `quality=unknown`, `published_utc=None`; valid_from/to — 5-мин усреднение. Источник `nasa_iswa_goes_primary_p5m`, схемы — отдельный source_id. Весь реестр, raw и покрытие уже возвращаются; per-window фильтрация/охват нужны в оценщике Б. Контекст 48 ч не должен становиться максимумом будущего окна. Для резервного TLE `Fetch.raw` может быть JSON, а payload — извлечённый TLE, хеши различаются законно.

**Нужно от товарища:** (1) в app/compute.py передать Request в историю, подключить GOES ряд и его интервальное покрытие вместо строки «архива нет»; не запрашивать текущую сеть для истории и сохранить `_history`/source_versions/raw в Manifest/выгрузке; (2) в app/main.py:122 убрать bool(disabled[...]) — оно превращает off в cache. A4 off уже не читает кеш и выдаёт пустые raw/metadata; поздняя маскировка отдельных полей Fetch не равно исключению источника; (3) сезонную инженерную v0 реализовать по §0 `METEOROIDS_SEASONAL_SPEC.md`, с PARTIAL/engineering_approximation и раздельными фоном/скорректированным фоном/потоками, не двойной суммой. Прежние замечания к +24 ч событий, поздним ENLIL-карточкам, Т5 и координатам ОСТ остаются в предыдущей передаче. После интеграции обновить PROGRESS.md фактическим принятым SHA и перепроверить экспорт/повтор.

**Заблокирован:** для продолжения А — нет. Строгая историческая доступность численного GOES не доказана, archive review это не исправляет. Повышение полноты A5 блокируют семантика нормировки табличного k, эпоха радиантов и неоднозначные двойные профили; они явно отделены от проверенной математики и разрешённой инженерной реализации. Для полного направленного расчёта Б нужны инерциальные положения/скорости A3; это следующий стык А. Windows и облако ещё требует проверить Б. Shared зависимости не менялись; h5py установлен только в локальном окружении А для дополнительной проверки NetCDF, runtime его не требует.

**Следующий шаг:** А — выдать из A3 инерциальные r/v с системой координат и происхождением для сезонной линии; Б — подключить GOES/A4 и реализовать инженерный сезонный оценщик по переданному стыку, затем совместная проверка на одном SHA.


## 2026-09-18 23:53 UTC — Codex — проверка после восстановления сети

**Задача / версия:** та же передача A1/A2/A4/A5, ветка `codex/a1-goes-history`; актуальный main 6224156 объединён в 6bdf007 без конфликтов, рабочий блок — следующий коммит.
**Сделано:** сохранены новая точка входа Streamlit Cloud и README Б; исходные GOES ответы защищены от преобразования переводов строк локальным `.gitattributes`. Команда получения новых версий также создаёт это правило. Устранены старые утверждения об отсутствии GOES в README A2.
**Проверено:** полный закреплённый набор на объединённой базе — 187 passed, 685 subtests. Выгрузка staged-архива с core.autocrlf=true/false: все 66 записей прошли проверку SHA, 18 400 значений прочитаны, дополнительные доказательства сохранили хеши. Полный Windows-запуск этим не заменяется. `git diff --cached --check` чистый.
**Изменилось для товарища:** итоговая передача уже включает main 6224156; инструкция `docs/integration/A1_A5_HANDOFF_2026-09-19.md`. Сырые байты/хеши одинаковы при обеих настройках Git.
**Нужно от товарища:** действия по подключению ряда GOES, исходным состояниям on/cache/off и сезонному оценщику из предыдущей записи; main интегрирует Б.
**Заблокирован:** нет; ограничения строгой исторической публикации и методической полноты A5 сохраняются.
**Следующий шаг:** инерциальные r/v A3 для направленной сезонной модели и проверка стыка с оценщиком Б.


## 2026-09-19 00:27 UTC — Codex — A3: r/v для сезонной модели

**Задача / версия:** A3 → A5/B2, алгоритм orbit-a3-v2, расширение provenance orbit-inertial-states-1; ветка codex/a3-inertial-states поверх adb99cf (включает предыдущую передачу GOES/A4/A5), main 6224156. Общие типы 2.1 не менялись; передаваемый SHA — следующий коммит.
**Сделано:** флаг `include_inertial_states=True` у `trajectory_with_provenance` возвращает инерциальные позиции/скорости из того же решения и на тех же временах, что TrajectoryPoint. EME2000/J2000, центр Земли, км/км/с; исходный файл и сегмент на каждой строке, хеши источников и блока, версии библиотек и fingerprint таблиц времени. OEM — исходные r/v и производная Эрмита; SGP4 — явное преобразование TEME через GCRS. Второй пропагатор или оценщик не создавался. Усилены непрерывность покрытия OEM между выходными точками (крупный шаг не скрывает пробел) и соответствие реально прочитанных TLE-байтов их хешу перед расчётом.
**Проверено:** полный закреплённый набор `python tests/sources/run_pinned_suite.py` — 194 passed, 691 subtests. Новые проверки: шесть преобразований по независимой ERFA, исходные OEM-узлы и аналитическая производная, три даты по 32 ч, совпадение геодезических точек и r/v, точные концы, сериализация/хеш/replay/изоляция, отказ строгой истории, скрытые пропуски OEM и изменённый receipt. Фактическое максимальное расхождение матрицы — 0,000191 м и 0,000214 мм/с при одинаковых UT1/SGP4; это не абсолютная точность орбиты. Пример JSON прошёл проверку хеша и совмещения времён. pyerfa установлен только локально для генерации эталона; общие зависимости не менялись.
**Изменилось для товарища:** в существующем build_orbit добавить include_inertial_states=True во все вызовы A3, брать `provenance['inertial_states']`. Новых классов/tuple-ABI нет; без флага прежние точки сохранены. Старый meta.frame — входная система, новый states.frame — выходная EME2000. Точный стык и правила — docs/integration/A3_STATES_HANDOFF.md; небольшой пример — data/orbit/inertial_states_example.json; численный отчёт — data/orbit/inertial_states_verification.json.
**Нужно от товарища:** подключить r/v к инженерному сезонному оценщику из спецификации A5, радианты привести к тем же осям/геоцентрическому соглашению. Не дифференцировать lat/lon, не вычитать вращение Земли повторно. Сохранить весь states-блок в снимке/выгрузке (нынешний provenance_summary его не переносит). Для задержки работ сдвигать начало всей орбиты, сохраняя до 32 ч горизонта и прежнюю отсечку. Приёмка: одинаковые времена points/states, совпадающие source hashes, повтор N по сохранённым данным и отсутствие повышения PARTIAL из-за самого наличия r/v.
**Заблокирован:** r/v готовы; реализация сезонного оценщика — область Б. Неизвестная публикация OEM по-прежнему запрещает строгую орбиту; реконструкция обозначается отдельно. Неопределённости нормировки k/радиантов/профилей A5 не закрываются орбитальным модулем. Скорости SGP4 используют принятую Skyfield мгновенную матрицу без малого frame-rate члена; ограничения явно сохранены.
**Следующий шаг:** Б подключает переданные состояния и оценивает сезонную линию; А проверяет тот же пример и чувствительность к шагу на объединённой версии, затем общая проверка критериев Т2–Т5/Т8.


## 2026-09-19 00:47 UTC — Codex — A2 v4, сверка с интеграцией Б

**Задача / версия:** A1/A2, history-a2-v4, типы 2.2 — согласовано. Ветка codex/a2-integration-hardening; origin/main d674dba объединён с предыдущей передачей А 66a2470; передаваемый SHA — следующий коммит.
**Сделано:** восстановлен окончательный GFZ Kp в штатном A2, который иначе терялся при замене заглушки Б. 488 трёхчасовых ячеек мая–июня, неизменённые байты среза исходника коллеги, реестр/хеш/качество, проверка формата, пропусков и покрытия; время наблюдения — конец ячейки. GFZ только для разбора, без доказанной публикации исключается из строгого режима; датированные наблюдения DONKI сохранены. Актуализированы README A1/A2 и версия схемы адаптера.
**Проверено:** python -m pytest tests/sources tests/history tests/orbit -q — 148 passed, 691 subtests. 24 ячейки GFZ совпали с отдельно полученным официальным JSON (проверка форматов одного поставщика). Общий закреплённый набор: 283 passed, 7 failed, 691 subtests; зелёная общая приёмка НЕ заявлена. Два сквозных исторических запуска с ZIP прошли. Предложенный импорт Fetch в отдельном процессе устранил два падения replay; файлы Б не менялись. Отчёт: docs/integration/A2_SYNC_CHECK_2026-09-19.json.
**Изменилось для товарища:** no-arg history_bundle теперь содержит GFZ и DONKI, raw ID GFZ обозначает версионированный байтовый выпуск, не синтетическую запись на каждый интервал. Для численного GOES всё ещё нужен Request. Полная сверка, семь падений с причинами/правками и ответы R9–R12/Р-О1/Р-О2 — docs/integration/A2_SYNC_2026-09-19.md. Типы 2.2 согласованы; наполнение Manifest ещё требует исправления у Б.
**Нужно от товарища:** приоритетно импортировать Fetch из vkd.sources в app/compute.py; перевести интеграционные тесты со stub-инъекций/старых ID на фактический A2; передать Request для GOES и сохранить source_versions из истории в настоящий Manifest вместо панели статусов. Затем подключить r/v и задержку орбиты по A3_STATES_HANDOFF.md, пересобрать примеры, проверить общий SHA и обновить PROGRESS. Интегрировать эту накопительную ветку один раз, предыдущие коммиты не переносить повторно.
**Заблокирован:** общая приёмка Т4/Т8 — правки потребителя Б; А не редактирует чужие пакеты. R10: завершение ENLIL+submission+60 мин не доказывает доступность редакции. R11: сферическая формула по геодезической широте всё ещё требует WGS84 ECEF; R12 принято как объявленный метод/ограничение, не закрытие отсутствующей функции. Сезонный MMOD остаётся неполным.
**Следующий шаг:** Б исправляет потребитель и интегрирует; А на том же SHA повторяет проверку покрытия, отсечки и ZIP, затем чувствительность орбиты к шагу.


## 2026-09-19 01:27 UTC — Codex

**Задача / версия:** интеграция A2/A3/A4 → B1–B6, база b283f6a, ветка codex/integration-closeout, локальный diff; алгоритм 0.6.0, типы без изменений.
**Сделано:** закрыт потребитель Request, численный GOES в разборе, задержка орбиты и r/v, настоящий реестр и байты ZIP, offline replay A4; WGS84 ECEF, отказ вместо clamp B/B0 и блокировка рекомендации при неполном обязательном покрытии. Детали — docs/integration/INTEGRATION_CLOSEOUT_2026-09-19.md.
**Проверено:** python tests/sources/run_pinned_suite.py: 296 тестов + 691 подпроверка прошли, macOS Python 3.14.3; tests/test_closeout.py: 6/6; AppTest: три состояния live, два исторических режима × два уровня и переключение со сценарием, без ошибок расчёта.
**Изменилось для товарища:** по явному поручению пользователя внесены необходимые изменения в app, assess, windows, explain, integration и replay; CONTRACT и общие типы не тронуты. Рекомендация теперь insufficient при неполном обязательном охвате: средний Grün не закрывает сезонный вклад; B/B0<1 не даёт модельного потока; частичное покрытие не FULL. Старые тесты, ожидавшие обратное, исправлены с регрессиями.
**Нужно от товарища:** интегрировать последнюю накопительную ветку один раз, не повторять PR #3–5; выполнить тесты/replay на Windows, подтвердить общий SHA, затем сезонная модель и публичный деплой.
**Заблокирован:** интеграционных блокеров А нет; полная рекомендация ограничена реальным покрытием моделей и источников, а не заглушкой орбиты.
**Следующий шаг:** пересобрать 9 примеров на закоммиченном коде 0.6.0, подтвердить replay и передать SHA ветки.


## 2026-09-19 01:29 UTC — Codex

**Задача / версия:** завершение интеграции и replay; codex/integration-closeout, код f4c85e9, алгоритм 0.6.0; передача примеров отдельным коммитом.
**Сделано:** пересобраны все 9 JSON/ZIP и INDEX; добавлен статус непроверенной на Windows ветки в начало PROGRESS, прежнее состояние main оставлено явно историческим.
**Проверено:** обычный python -m pytest -q — 296 passed, 691 subtests passed, 26.87 с; после пересборки tests/test_exp_examples.py — 24 passed; scripts/replay_all.py — все 9 ZIP код 0. Последний fetch: origin/main d674dba, новых изменений Б нет.
**Изменилось для товарища:** примеры с настоящими A2/A3/A4 и полными доказательствами; средний Grün и пробелы радиационной модели дают insufficient, сравнение и предупреждения сохранены. Старые эксперименты Т5 не выдавать за валидацию нового вердикта.
**Нужно от товарища:** принять единую накопительную ветку и повторить pytest/replay на Windows, зафиксировать общий SHA. Детали docs/integration/INTEGRATION_CLOSEOUT_2026-09-19.md.
**Заблокирован:** нет для передачи; полное физическое покрытие остаётся отдельной задачей.
**Следующий шаг:** приёмка Б и сезонная модель; А — независимые численные проверки орбиты/поля и шага.


## 2026-09-19 02:23 UTC — Codex

**Задача / версия:** A3 численная проверка + A4 текущий NOAA + стык с новым main 635b284; ветка codex/a3-validation-sync поверх 7a1d0e4, алгоритм 0.6.1.
**Сделано:** прочитан файл HANDOFF из Downloads и актуальная передача Б; слиты его M/U-исправления с нашей интеграцией. Текущий NOAA теперь включён в конвейер, UI и ZIP/replay с исходными 3-часовыми/суточными ячейками и отдельным off/cache. Условия читают facts.kp/kp_range_max; карточки ссылаются на raw_record_id, не event_id. Создан модуль диагностики dt/покрытия и воспроизводимое исследование шага 5–600 с на пяти случаях.
**Проверено:** python tests/sources/run_pinned_suite.py — 356 passed, 691 subtests passed, 65.35 с, включая AppTest Б; целевые тесты NOAA проверяют сырые байты, оценку, исключение и offline replay. Исследование шага выполнено; финальные JSON/ZIP будут пересобраны на коммите кода.
**Изменилось для товарища:** fetched поддерживает необязательный четвёртый кортеж NOAA; три прежних входа работают без скрытой сети. Типы/CONTRACT не изменены. Детали — docs/integration/HANDOFF_REVIEW_A3_2026-09-19.md. M11: неполный DONKI уменьшает охват событий, но не удаляет реальные GOES/NOAA на соседние даты. Сценарии проверяются в разборе, строгий режим их отвергает.
**Нужно от товарища:** интегрировать только последнюю накопительную ветку, повторить проверки на Windows/площадке и Т5 на A2. NOAA в текущем режиме уже подключён, повторно не реализовывать.
**Заблокирован:** честный McIlwain/сезонная нормировка не закрыты: нельзя объявлять igfr_trace/ok или FULL по целевым 80% покрытия. Timestamp-proof GOES не доказывает историческую публикацию — архив только для разбора.
**Следующий шаг:** регенерация 9 примеров и отчёта шага на общем закоммиченном коде, проверка replay, push. Затем исправление дискретизации флюенса с согласованным dt: даже минута дала до ≈20% отклонения известного интеграла от 5-секундной сетки; плотные точки нельзя подавать в минутный оценщик без изменения dt.


## 2026-09-19 02:31 UTC — Codex

**Задача / версия:** A3/A4, итог передачи codex/a3-validation-sync; код 64a0a5f, main Б до 8b2d063 включён; алгоритм 0.6.1, типы/CONTRACT неизменны. Артефакты — следующий коммит.
**Сделано:** синхронизированы новый код и предзащитные документы Б. Подключён текущий NOAA с реальными сырыми данными и offline replay; сохранены публикация в карточках, структурированные факты событий и независимое покрытие источников. Пересозданы 9 примеров и исследование орбитальной сетки; уточнены чек-лист и очередь Б без повторной реализации его задач.
**Проверено:** python tests/sources/run_pinned_suite.py — 357 passed, 691 subtests passed, 60,73 с; scripts/replay_all.py — 9/9, код 0; tests/test_exp_examples.py — 24 passed. Исследование пяти случаев 5/10/30/60/300/600 с пересоздано на 64a0a5f. Изменение известной части флюенса минутной сетки до 19,87% относительно 5 с, это не оценка полного флюенса и не физическая валидация.
**Изменилось для товарища:** читать docs/integration/HANDOFF_REVIEW_A3_2026-09-19.md и docs/methods/ORBIT_GRID_VALIDATION.md. Эта ветка накопительная и включает прежнюю передачу PR #6; интегрировать одну последнюю ветку. Запрос на завершение интеграции от пользователя разрешает необходимые правки общих модулей; новые типы и CONTRACT не вводились.
**Нужно от товарища:** принять общий SHA, повторить закреплённые тесты и 9 replay на Windows/площадке; пересчитать Т5 на A2, продолжить сезонную линию по готовой спецификации/r-v. Для 0.6.1 ожидать insufficient при неполной обязательной физике; не подгонять вердикт под старые картинки.
**Заблокирован:** нет для передачи. Полная физическая валидность McIlwain и нормировки сезонного каталога не доказана, численный GOES не имеет доказанной исторической публикации; эти ограничения остаются явными.
**Следующий шаг:** A3/B2 — интегрирование по фактическому dt с контролем численной сходимости/покрытия; нынешний оценщик предполагает минуту, простая подмена шага недопустима.


## 2026-09-19 07:42 UTC — Codex

**Задача / версия:** A3/B2, интегрирование по времени; codex/fluence-time-integration поверх f64bdcb, включён main Б a428dd8. Алгоритм 0.7.0; SHA кода — следующий коммит.
**Сделано:** vkd/orbit/integration.py подключён к assess_window: трапеции по фактическим dt, отсечение границ окна без экстраполяции, покрытие в секундах, линейные пересечения порогов B/обрезания. Диагностика соседних сеток на одинаковом известном участке — в снимке и ZIP. Сохранены новые сетевые пределы/экран/выгрузка Б и наше честное неполное покрытие. Стык и детали в docs/integration/TIME_INTEGRATION_2026-09-19.md.
**Проверено:** аналитический блок и регрессии стыка — 33 passed. Предварительный полный прогон: 594 passed, 691 subtests; один сбой изоляции тестового кеша Streamlit выявлен и исправлен, полный повтор запущен. Пять случаев × семь орбитальных сеток рассчитаны: для 60 с максимум отличия времени в аномалии от 5 с около 0,157 мин; известный флюенс всё ещё чувствителен до 19,87%, физическую точность это не доказывает.
**Изменилось для товарища:** assess_window принимает необязательный numerical_report; типы не расширялись. Временной множитель 60 снят, оба конца интервала обязательны. Производственный шаг остаётся 60 с; уменьшить его теперь можно без умножения экспозиции. Отчёт numerical_integration.json содержит покрытие и численную чувствительность. CONTRACT взят как есть из main Б, самостоятельно не редактировался. Исправлены стыки NOAA off/охват/дубликаты, SEP 100 МэВ, неполная граница CME Kp, подписи частичных вкладов.
**Нужно от товарища:** принимать последнюю накопительную ветку после финальной записи о тестах/9 примерах; повторить приёмку Windows/облака. Старые PR повторно не переносить. Сезонная линия и Т5 на A2 остаются у Б; математические тесты выбора используют явно синтетический полный охват, реальные неполные окна не становятся preferred.
**Заблокирован:** нет для завершения блока; физическая валидация и сезонная нормировка не объявлены закрытыми.
**Следующий шаг:** пересоздать примеры и исследование на коммите кода, проверить 9 offline replay, передать SHA и финальный результат тестов. Затем контролируемое сгущение сетки.


## 2026-09-19 07:50 UTC — Codex

**Задача / версия:** A3/B2, финальная передача интегрирования 0.7.0; codex/fluence-time-integration, код 6672e63414c89d68f5da994835584bf18f4ea1f2, включён main Б a428dd8; текущий коммит добавляет только документацию и пересозданные доказательства.
**Сделано:** блок фактического dt завершён и соединён с актуальным кодом Б. Девять JSON/ZIP и исследование семи сеток на пяти случаях пересозданы на 6672e63; README и верхняя запись PROGRESS приведены к реальному покрытию/отказам и формуле интеграла. Все реальные примеры сохраняют insufficient при неполном обязательном охвате.
**Проверено:** macOS/Python 3.14.3: python tests/sources/run_pinned_suite.py — 596 passed, 691 subtests passed, 283,16 с, код 0; scripts/replay_all.py — 9/9; tests/test_exp_examples.py — 24 passed. У всех 9 экспортов диагностика согласована между снимком/манифестом/ZIP, известный интеграл совпадает с фактором. Тесты Windows/облака этой версии не выполнялись.
**Изменилось для товарища:** передача docs/integration/TIME_INTEGRATION_2026-09-19.md, алгоритм 0.7.0, схема 2.2 без расширения типов. assess_window сохраняет совместимость, numerical_report необязателен. Производственный шаг 60 с; множителя «каждая точка — минута» больше нет. Прежние утверждения о полном флюенсе и preferred в реальных неполных примерах не действуют.
**Нужно от товарища:** принять одну последнюю накопительную ветку вместо повторного переноса PR #7; повторить тесты/replay на Windows и площадке на общем SHA. Т5 и сезонная линия остаются у Б; уже имеющийся эксперимент нужно сверить с итоговой версией, а не считать отсутствующим.
**Заблокирован:** нет для передачи. До доказательства физической модели/сезонного вклада и полного охвата рекомендация честно ограничена; успешные тесты этого не исправляют.
**Следующий шаг:** A3 — контролируемое сгущение временной сетки с оценкой сходимости на одинаковом известном участке и бюджетом вычислений. У минутной сетки известный интеграл всё ещё отличается от 5 с до 19,87%; это не граница физической ошибки.


## 2026-09-19 12:30 UTC — Codex

**Задача / версия:** A5/B2, сезонная модель по прямому поручению владельца; ветка `codex/a5-seasonal-model`, реализация `6a14d78`, соединение с `origin/main` `f34d743`; алгоритм 0.10.0, публичные типы от круга 11 без наших правок.
**Сделано:** 49 направленных потоков C-2, дата/радианты/скорость МКС/тень Земли, единая пластина и масса, годовое вычитание без двойного учёта. Сезонность включена в ручные окна и автоматический перебор, карточки, отчёт, реестр, ZIP и replay. Гипотезы нормировки/эпохи не скрыты; при изменении сравнения нет однозначного победителя. PARTIAL сохранён. В общий канал R14 и физику обрезания круга 11 правок не вносил.
**Проверено:** 94 целевых теста сезонной физики/стыка/перебора/сравнения, 30,28 с, macOS Python 3.14. Полная регрессия объединённой версии запускается следом; это не заявление о завершённой приёмке. Предыдущий полный прогон до круга 11 выявил четыре устаревшие/неверные экранные подписи; исправлены, проверяются повторно.
**Изменилось для товарища:** по поручению владельца затронуты B2, compare, scan и app. Типы и CONTRACT.md не правились; merger сохраняет новые поля коллеги. `assess_window` получил необязательный `mmod_seasonal`, `recommend` — `mmod_sensitivity`, `scan_windows` — `meteor_model`. `S.meteoroids` содержит ручные окна и начала перебора. Новое ограничение применимости: нужны r/v EME2000 с шагом ≤60 с, высота 200–2000 км. Формула Солнца USNO сверена на шести DE421-контролях. Полный список: docs/methods/METEOROIDS_SEASONAL_SPEC.md §9.
**Нужно от товарища:** интегрировать эту ветку целиком после итоговой записи проверок; сохранить сезонные изменения app/ui.py и app/main.py при соединении с wf11/screen. После изменения расчётных файлов пересобрать примеры, прежние 0.9.0 — другая версия. Не объявлять абсолютную валидацию C-2 завершённой.
**Заблокирован:** нет для реализации; подтверждение нормировки/эпохи каталога и абсолютного потока остаётся методическим ограничением.
**Следующий шаг:** полная регрессия на соединении, новые примеры и повтор, передача SHA.


## 2026-09-19 12:48 UTC — Codex

**Задача / версия:** приёмка A5/B2 сезонности, `codex/a5-seasonal-model`; общий main принят до `f34d743`, соединение `9cb34bc`, расчётный код после правок подписей `28d89d6`, алгоритм 0.10.0.
**Сделано:** закончено соединение с автоматическим перебором круга 11: один подготовленный горизонт, одни значения в кандидате и карточке, совместное сравнение космопогоды и сезонных метеороидов. При смене порядка от гипотез — компромисс, не победитель. Экран/ZIP показывают компоненты; `meteoroids.json` хранит все начала поиска. Обновлены README и методика. Каталог JSON зафиксирован LF в .gitattributes для одинакового SHA на Windows/macOS.
**Проверено:** полный прогон объединённой версии: 825 passed, 691 subtests passed, 4 ошибки оформления и 4 пропуска Node. Все четыре ошибки исправлены (код сетевого состояния request_incomplete на поверхности и длина блока сезонного сравнения); повтор затронутых проверок экрана: 13 passed. Все 56 проверок глобуса с bundled Node прошли, включая четыре ранее пропущенные. Финальные 32 теста сезонной физики/сквозного стыка/отказов/экспорта прошли; тест перевода незавершённого запроса проверяет фактический report_md. Итого незакрытых падений или пропущенных проверок из этого набора нет; это совокупность полного прогона и адресных повторов, не заявление об одном новом полном прогоне. 11/11 ZIP воспроизведены; после последней правки подписей примеры ещё раз пересобираются и повторяются перед push. На 1091 сохранённом окне максимум изменения итогового N при 10→5 с — 0,016505%; неприменимых гипотез нет. Это численная сходимость, не измеренная физическая ошибка. Браузерная проверка текущего режима: компоненты N, пять главных потоков и оговорки показаны на localhost:8503, ошибок страницы нет.
**Изменилось для товарища:** интегрировать всю ветку, не только vkd/assess/seasonal.py. Новая сезонность есть и в vkd/windows/scan.py: значимое различие метеороидов больше нельзя игнорировать при равнозначной космопогоде. При слиянии с wf11/screen сохранить сезонный блок во вкладке «Окна и факторы», новые компоненты факторов и честную методику. Старое «потоки даты не рассчитаны» и «метеороиды по построению не выбирают окно» для 0.10.0 неверно. Зависимости и CONTRACT.md не менялись. Внешняя отправка в main не выполнялась.
**Нужно от товарища:** общий merge и проверка на Windows; после новых расчётных правок снова пересобрать примеры. Не переключать PARTIAL на FULL: /4 доказано геометрически, но семантика k и эпоха C-2 остаются гипотезами. Нет наблюдательной калибровки потока на МКС, прогноза годовых всплесков, техногенного мусора или модели пробоя.
**Заблокирован:** нет для передачи работающей инженерной версии. Абсолютная физическая валидация — отдельный незакрытый пункт, а не ошибка вычислений.
**Следующий шаг:** итоговая пересборка/replay примеров и push этой ветки; затем интегратор соединяет её с финальным экраном.


## 2026-09-19 12:50 UTC — Codex

**Задача / версия:** финальная передача A5/B2, ветка `codex/a5-seasonal-model`, код примеров `91b1637`, алгоритм 0.10.0.
**Сделано:** 11 JSON и 11 ZIP пересобраны после всех расчётных и текстовых правок; сохранены исходные записи и моменты прежних текущих расчётов, без подмены новыми наблюдениями. `examples/INDEX.md` это явно объявляет.
**Проверено:** финальный `python scripts/replay_all.py` — 11/11, каждый код 0; проверены версия 0.10.0, SHA кода 91b1637 и наличие meteoroids.json во всех ZIP. Итоговые проверки — в предыдущей записи, все найденные падения закрыты адресными повторами.
**Изменилось для товарища:** передавать код и примеры вместе; исходная рабочая копия владельца и её незакоммиченный journal/friend.md не затронуты. Новая локальная сборка — http://localhost:8503/.
**Нужно от товарища:** соединить эту ветку с общим main и wf11/screen, проверить Windows, не потерять участие сезонной модели в переборе.
**Заблокирован:** нет; физические ограничения остаются в методике и на экране.
**Следующий шаг:** интеграция Б и повтор общей приёмки после слияния; main самостоятельно не менялся.
