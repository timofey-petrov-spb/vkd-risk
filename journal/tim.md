# Журнал: Тимофей + Claude Code

Пишет только Claude. Читает Codex. **Дописывать только в конец.**

---

## 2026-09-17 22:00 — Claude

**Сделано:** создан репозиторий, договор в `CONTRACT.md`, инструкции для обоих
ассистентов. Из ОСТ 134-1044-2007 извлечены 12 дозовых таблиц в `data/ost1044/`,
по 9 толщин на 16–18 высот, проверены сверкой с текстом стандарта. Собрана
нормативная база из 11 документов в `docs/`.

**Изменилось для товарища:** появился `CONTRACT.md` — читать целиком до начала
работы. Там заморожены единицы, семь зон с числами, разделение областей,
структура стыка и восемь правил кода. Таблицы ОСТ готовы, оцифровывать их
не надо.

**Нужно от товарища:** оцифровать ГОСТ Р 25645.165-2001 (вспышки, скан,
таблицы флюенса и пикового потока при вероятностях 0,9…0,010) и
ГОСТ Р 25645.167-2005 (плотность потока техногенного вещества) в CSV,
положить в `data/`. Формат: первая строка — заголовки с единицами в имени.

**Заблокирован:** нет.

---

## 2026-09-17 22:30 — Claude

**Сделано:** добавлен `docs/ZAMYSEL.md` — тезис проекта, находки с числами,
четыре нормативных режима, состав визуальной части и правила оформления,
принятые поправки, две обязательные проверки, структура защиты, порядок работ
и что резать при нехватке времени. В `CLAUDE.md` и `AGENTS.md` добавлены
списки того, что прочитать в первую сессию.

**Изменилось для товарища:** появился `docs/ZAMYSEL.md`, читать обязательно
вместе с `CONTRACT.md`. Там зафиксированы три поправки, принятые по разбору
Codex: ориентация скафандра обязательна для удара; вероятность разгерметизации
не заявляем; космопогоду переносим только через геомагнитное обрезание,
приложение Ж ОСТ 1044. Отклонено: ORDEM и MEM — берём ГОСТ Р 25645.167-2005
и ECSS раздел 10. Дополнительное направление выбрано одно — чувствительность
к неизвестным параметрам.

**Нужно от товарища:** без изменений — оцифровка ГОСТ Р 25645.165-2001
и ГОСТ Р 25645.167-2005 в CSV. Плюс подтвердить, что `scripts/build_check.bat`
проходит на его машине в обеих конфигурациях.

**Заблокирован:** нет.

---

## 2026-09-17 23:00 — Claude

**Сделано:** скачаны и описаны шесть баз космопогоды в `data/spaceweather/`:
Kp, ap, Ap, число пятен и F10.7 одним файлом с 1932 по 16.09.2026 (GFZ),
TLE станции (CelesTrak), коэффициенты IGRF-14, протонный поток GOES за трое
суток, прогноз F10.7 и Ap на 27 суток, месячные числа пятен (SILSO).
Форматы разобраны в `data/spaceweather/README.md`.
Добавлены `docs/VOZDEYSTVIE_NA_CHELOVEKA.md` (w_R, w_T, Q(L), пределы,
что каждое воздействие поражает) и `docs/SKAFANDR_RAZMERY.md` (параметризация,
пропорции, зазоры, границы зон, точки дозы).

**Изменилось для товарища:** базы космопогоды качать не надо, всё лежит
локально с описанием форматов. ВАЖНО: в расчёт добавляется w_R = 5 для
протонов и 1 для электронов — протонный вклад в зивертах впятеро больше,
чем в греях, доля протонов растёт с 8 % до 31 %. Разобрана орбита станции
из TLE: наклонение 51,6307°, высота 419,2 км, период 93,0 мин. Высота между
столбцами 400 и 600 км таблиц ОСТ — интерполируем; наклонение 51,6° есть
в К.3.5 напрямую.

**Нужно от товарища:** без изменений — сборка на его машине, оцифровка
ГОСТ Р 25645.165-2001 в `data/gost165/` и ГОСТ Р 25645.167-2005
в `data/gost167/`. Подробности в `PLAN_17_09.md`, раздел «Товарищу».

**Заблокирован:** нет. Единственная незакрытая позиция — пределы для кожи
и хрусталика из ООКОКП-2004. Решение принято: не найдём за час — показываем
дозу числом без вердикта.

---

## 2026-09-17 23:20 — Claude

**Сделано:** переименованы 12 файлов таблиц ОСТ с кириллической «К» на
латинскую «K». Обновлены `index.json` и `scripts/ost_parse.py`.
В `CONTRACT.md` добавлено правило 0: имена файлов и путей только латиницей.

**Изменилось для товарища:** **если ты уже ссылался на файлы `К_*.csv` —
исправь на `K_*.csv`, латинская буква.** Причина: кириллица в пути ломает
`std::ifstream` на MSVC при другой кодовой странице и проявляется как
«файл не найден» только на чужой машине. Содержимое файлов остаётся UTF-8,
там кириллица допустима.

**Нужно от товарища:** без изменений, см. `PLAN_17_09.md`.

**Заблокирован:** нет.

---

## 2026-09-18 00:10 — Claude

**Сделано:** организаторы выдали репозиторий на GitVerse. Настроена схема
с двумя удалёнными адресами: `origin` — GitHub, рабочий; `gitverse` —
`git@gitverse.ru:hackrus.experts/kosmo-piter_matmehovtsy_111.git`, для сдачи.
Сгенерирован SSH-ключ, ключи хоста gitverse.ru записаны в known_hosts.
Схема описана в `README.md`.

**Изменилось для товарища:** **финальное решение обязано лежать на GitVerse
к дедлайну.** Работаем по-прежнему на GitHub, но регулярно дублируем:
`git push gitverse main`. Тебе тоже надо указать свой username на GitVerse
в личном кабинете хакатона, иначе доступ к командному репозиторию не выдадут.
Доступы выдают модераторы вручную, в субботу волнами каждые 2 часа
с 10:00 до 20:00. Сделай это сегодня.

**Нужно от товарища:** привязать username на GitVerse в кабинете хакатона.
Остальное без изменений, см. `PLAN_17_09.md`.

**Заблокирован:** нет. Доступ к GitVerse ждём от модераторов.

---

## 2026-09-18 01:30 — Claude

**Сделано:** разобран `docs/REPOSITORY_REVIEW.md`. Приняты и исправлены четыре
настоящие ошибки, закрыт главный блокер и найдено решение методологической
дыры.

1. **Исходники добавлены в git**, `docs/istochniki/`, 31 МБ: ГОСТ Р 25645.165,
   167, 166, ГОСТ 25645.202, ОСТ 1044 (DOCX и текст), ECSS-10-04C и 10-12C,
   NASA ISS EVA Suits, ERI-000501, тезисы xEMU. Блокер оцифровки снят.
2. **Арифметика длины плеча исправлена**: 0,186 × 176 = 32,7 см, а не 26,4.
   Ошибка была моя, коэффициент верен, пример был неверен.
3. **Переход Гр(Si) → ткань отделён от w_R.** Это три перехода, а не два:
   материал, вид излучения, чувствительность органа. Коэффициент по материалу
   пока НЕ определён, до этого показываем Гр(Si) с подписью.
4. **Оговорка ICRP-60 против ICRP-103.** В 103-й редакции w_R протонов снижен
   с 5 до 2; наша оценка протонного вклада может быть завышена в 2,5 раза.
   Берём 60-ю, потому что на неё ссылается ECSS, но называем редакцию явно.
5. **Шлем — одна зона в двух состояниях**, не две. Площадь учитывать один раз.
6. CMake поднят до 3.25 в трёх местах: формат пресетов v6 требует 3.25+.
7. `scripts/ost_parse.py` переведён на пути от корня репозитория, вход теперь
   в git — скрипт стал воспроизводимым.

**ГЛАВНОЕ — закрыта дыра со средой во времени.** Замечание было верное:
усреднённые за 10 лет таблицы приложения К момент на орбите не задают.
Решение нашлось в самом ОСТ: **приложение А** даёт потоки как функцию
координат Мак-Илвейна `(L, B/B₀)` и энергии — это AE-8 и AP-8 в
дифференциальной форме. Извлечено в `data/ost1044_belts/`, 4 таблицы,
1395 строк. Метод: форма во времени из приложения А, уровень из приложения К,
и **совпадение долговременного среднего — третий обязательный тест**.

**Изменилось для товарища:** появились `data/ost1044_belts/` с моделью потоков
и `data/ost1044_belts/README.md` с методом. Твой контур теперь выдаёт не только
`в_аномалии_bool`, а **`L` и `B/B₀`** — по ним берётся поток. Это уточнение
стыка, его надо внести в `CONTRACT.md` раздел 4; правит человек, жду решения
Тимофея. Исходники всех ГОСТов лежат в `docs/istochniki/` под латинскими
именами, оцифровку можно начинать. У тебя есть Tesseract с русским — ГОСТ 165
скан, он пригодится.

**Нужно от товарища:** оцифровка ГОСТ 165 и 167 (блокер снят). Плюс подтвердить,
согласен ли со стыком через `L` и `B/B₀` вместо булева признака аномалии.

**Заблокирован:** нет.

---

## 2026-09-18 19:00 — Claude

**Сделано:** получены официальные документы кейса, лежат в `docs/case/`:
постановка (7 с.) и критерии (3 с.), извлечённый текст рядом. Полный разбор
в `docs/case/ANALIZ_KEYSA.md`. **Этот разбор отменяет всё, что ему
противоречит в остальных документах.**

**Изменилось для товарища — КРИТИЧНО, читать первым:**

1. **Настольное приложение недопустимо.** Постановка: «Допускается веб-сервис,
   Telegram-бот или бот с мини-приложением». Сдача — «развёрнутое решение
   со ссылкой». **C++, Qt, DLL, установщик отменяются.** Форма: веб на Python,
   развёрнуто по ссылке. Твоя macOS-среда теперь основная, Windows не нужна.
2. **Данные должны получаться автоматически.** Снимки в `data/spaceweather/`
   — только знание источников и кеш. Механизм получения надо писать.
3. **Исторический режим обязателен: любая дата 01.05–30.06.2024**, с прогнозом
   из прошлого по данным, опубликованным к моменту. 10–11 мая 2024 — буря
   Гэннон, G5. Это выраженное событие для Т5, организаторы дали его сами.
4. **Фокус кейса — среда на траектории, не доза человека.** Скафандр уходит
   в дополнительную функцию О7. С критического пути снят.
5. Требуется прогноз на 6 часов хотя бы по одному механизму; сравнение
   с простым подходом; отключение источника для проверки; выгрузка JSON+отчёт.

Что остаётся в силе: два механизма (радиация + MMOD), стык через L и B/B₀,
приложение А ОСТ как ядро, реестр источников, принцип «чего не заявляем»,
три теста, разделение областей. Твой контур — орбита, SOCRATES, потоки —
становится ещё важнее: Т1 (7) + Т2 (6) + половина Т3 (10) — это твоя область.

**Нужно от товарища:** прочитать `docs/case/ANALIZ_KEYSA.md` целиком до любой
работы. Оценить со своей стороны, что из постановки я мог прочитать неверно.
Оцифровка ГОСТ 167 остаётся нужной для механизма 2; ГОСТ 165 отходит
на второй план — для прогнозного режима важнее живой GOES и архив NOAA.

**Заблокирован:** решение по стеку и по разделу 5 договора (плагинный ABI
на C++) — за Тимофеем. До решения код не пишу.

---

## 2026-09-18 21:00 — Claude

**Сделано:** Тимофей принял веб-версию. Проект перестроен под постановку.
`CONTRACT.md` версии 2: что выдаёт сервис (три вещи вместе), два механизма,
величины по окну, лексикографическое правило рекомендации, разделение по
пакетам Python, стык через `vkd/types.py`, обязательные тесты Т3/Т4/Т6.
`PLAN_RAZRABOTKI.md`: два дня по критериям, что резать первым.
`vkd/types.py` написан и заморожен. Каркас пакетов создан, `pyproject.toml`.
C++, CMake, .bat — удалены из git. Архивы DONKI за май–июнь 2024 в
`data/archive_2024/`: GST, SEP, FLR, CME, IPS, RBE — у каждой записи
`submissionTime` и `versionId`. Буря Гэннон подтверждена: 2024-05-10T15:00Z,
Kp 9,0.

**Изменилось для товарища:** всё. Читать `CONTRACT.md` v2 целиком.
Твои пакеты: `vkd/sources/`, `vkd/orbit/`, `vkd/conjunctions/`,
`vkd/history/`. Ты выдаёшь `TrajectoryPoint`, `TrajectoryMeta`,
`EnvironmentSample`, `Conjunction` из `vkd/types.py` — типы заморожены.
Кроме `in_saa` теперь обязательны `L`, `B_over_B0`, `cutoff_GV`.
У `EnvironmentSample` три времени раздельно: `t_utc`, `published_utc`,
`valid_from/to_utc` — так требует постановка. `published_utc = None`
означает «непригоден для строгого replay», не «сейчас».

**Нужно от товарища:** день 1 — пункты 2, 3, 8 из `PLAN_RAZRABOTKI.md`:
живое получение с кешем и отключением источника, траектория с L/B₀/обрезанием,
исторический разбор по DONKI. Проверить доступ к NOAA NCEI и Space-Track
с твоей сети. Поставить `streamlit skyfield ppigrf`.

**Заблокирован:** нет.

---

## 2026-09-18 22:30 — Claude

**Сделано:** `experiments/demo_pipeline.py` — сквозная цепочка на реальных
данных: TLE → SGP4 по минутам → |B| по IGRF (ppigrf) → дипольная L и B/B₀
→ аномалия → поток протонов по прил. А ОСТ → окна 6 ч с шагом 30 мин
→ правило из договора → рекомендация. Работает. За 24 ч: 212 мин в аномалии,
12 пролётов. Разброс между окнами: от 3 до 102 минут в аномалии, интеграл
потока отличается в 90 раз. GOES 0,207 pfu, ниже S1.

**Изменилось для товарища:** это ОБРАЗЕЦ, что должен выдавать твой `vkd/orbit/`,
и что я из этого беру. В демо три упрощения, которые ты заменяешь:
(1) L — дипольное приближение, нужна трассировка силовой линии или
IRBEM-подобный расчёт; (2) обрезание не считается — нужен `cutoff_GV`;
(3) порог аномалии 24000 нТл — инженерный, вынести в настройку. Шаг 1 мин
достаточен: станция проходит 460 км/мин, аномалия тысячи км, точность
TLE ~1 км не оправдывает секунды. `wgs84.geographic_position_of` из skyfield
даёт lat/lon/alt массивами; `ppigrf.igrf` требует naive datetime.

**Нужно от товарища:** без изменений — пункты 2, 3, 8 плана. Демо можно
запускать как эталон: результат твоего `TrajectoryPoint` по тем же TLE и
времени должен давать те же lat/lon/alt и |B| с точностью до округления.

**Заблокирован:** нет. Механизм 2 (SOCRATES + статистика MMOD) в демо не
подключён, поэтому по п.1 правила полная рекомендация пока невозможна —
это честное состояние, не ошибка.

---

## 2026-09-18 23:30 — Claude

**Сделано:** запущена многоагентная подготовка мастер-плана проекта: девять
направлений исследования (космопогода, MMOD, прочие факторы и условия работ,
орбита и магнитные координаты, исторический режим, критерии и эксперименты,
нормативы, интерфейс и развёртывание, архитектура) с фактической проверкой
нагруженных утверждений тремя скептиками каждое; три независимых проекта
решения с судейством по 15 критериям; затем 14 разделов мастер-плана с критикой
и правкой. Результат ляжет в `docs/master/` — от постановки нашего решения
до детального плана работ с владельцами и критериями приёмки.

**Изменилось для товарища:** пока ничего в коде. Когда появится `docs/master/`,
там будет раздел `14_VOPROSY_DLYA_SVERKI.md` — развилки, по которым ты можешь
видеть иначе, с нашей позицией. Это и есть место для сверки твоего видения
с нашим. До появления мастер-плана не тратить время на собственный обзор
литературы и источников — он делается сейчас с проверкой доступа.

**Нужно от товарища:** продолжать пункты 2, 3, 8 плана (получение данных,
траектория, исторический разбор по DONKI). Они не изменятся.

**Заблокирован:** нет.

---

## 2026-09-19 00:30 — Claude

**Сделано:** оркестрация мастер-плана остановлена по лимиту сессии после
первой фазы. Спасено и залито: `docs/master/research/` — шесть направлений
из девяти (mmod, otherfactors, criteria, norms, ux, engineering), каждое
как MD и JSON, с реестром источников, проверенных фактически, пробелами
и рекомендациями; 12 голосов скептиков по 4 утверждениям приложены.
Бонус: `data/ost1044_cutoff/` — таблица Ж.1 ОСТ (жёсткость обрезания, 450 км).
`docs/master/README.md` — состояние и порядок чтения.

**Изменилось для товарища:** появились готовые к использованию исследования
по твоим областям: `research/mmod.md` (SOCRATES: формат 11 полей, порог 5 км,
горизонт 7 суток, прогоны 3 раза в сутки; снимки Wayback за май–июнь 2024;
28 снимков официальных эфемерид МКС NASA/JSC — сильный актив для Т2/Т4),
`research/engineering.md` (слой источников, кеш, отключение, тесты, выгрузка).
Направления «космическая погода», «орбита» и «история» не завершились —
по ним действуют CONTRACT.md и README данных. Таблица Ж.1 — вход для
`cutoff_GV`.

**Нужно от товарища:** прочитать `research/mmod.md` и `research/engineering.md`
до начала работы над `vkd/sources/` и `vkd/conjunctions/`. Записать в свой
журнал, где твоё видение расходится с этими исследованиями.

**Заблокирован:** нет. Продолжение оркестрации — при следующем окне лимита,
из кеша.

---

## 2026-09-19 02:00 — Claude

**Сделано:** разбор Codex по 4c32d82 принят почти полностью — это лучший
разбор за проект. Главное подтверждено по файлу: запись GST 2024-05-10 имеет
submissionTime 18:44, а внутри allKpIndex — наблюдения до 12 мая 06:00;
отсечка по времени подачи пропускала будущее. Внесено:

- **CONTRACT.md v3** — четыре решения, которые Codex просил зафиксировать:
  (1) строгий режим строится на датированных выпусках — DONKI sentNotifications
  по messageIssueTime, NGDC 3day_forecast + daypre для разрыва 15.05–16.06
  (Mid_k/High_k не подставлять вместо Kp), GOES NCEI по observedTime, NASA OEM
  через интерполяцию с раздельными created/available/fetched; карточки DONKI
  и окончательный Kp — только для проверки после факта; фильтрация по каждому
  наблюдению, не по родительской записи; (2) SOCRATES — отдельная
  необязательная линия, обязательный охват = космопогода + одна статистическая
  модель MMOD с согласованными единицами (пластина по ECSS); заключение всегда
  относится к объявленному охвату; (3) величины по окну переопределены:
  интегрирование по энергии трапециями с явным хвостом, «нет модели» вместо
  нуля вне сетки, доступность частиц канала через обрезание с явным
  предположением о спектре, давность GOES для будущих участков; (4) правило
  рекомендации переписано: охват → условия проверки → сравнение по каждому
  механизму → сведение с компромиссом без победителя → допуск из
  чувствительности.
- **vkd/types.py v2** — по таблице Codex: B_nT, метод и статус магнитного
  расчёта, OEM/frame/coverage/available в метаданных, channel_id/quality/
  fetched/raw_record_id в выборке, EventInterval с неизвестными границами,
  Presence и Coverage раздельно, Request и Manifest, MechanismAssessment
  с needs_check, Recommendation с per_mechanism_comparison и tolerance_basis.
  **Не заморожен** — жду «согласовано».
- **PLAN_RAZRABOTKI.md** — моя ошибка исправлена: резать сложность, не
  обязательные функции; история проверяется утром первого дня на трёх датах.
- README, CLAUDE.md, AGENTS.md очищены от C++.
- demo_pipeline.py помечен «не эталон» со списком найденных ошибок.

**Не принято, с обоснованием:** ничего по существу. Одно уточнение: фраза
про прогноз SEP заменена на «не разрабатываем собственную модель за время
хакатона», как предложено.

**Изменилось для товарища:** CONTRACT v3 и types v2 — читать целиком.
Твой контур получает OEM-путь в `vkd/orbit/` и обязанность вести
created/available/fetched раздельно. Строгий режим — на sentNotifications
DONKI и NGDC-выпусках, по твоему списку.

**Нужно от товарища:** (а) записать «согласовано» по types v2 или список
правок; (б) подтвердить или поправить карту покрытия NGDC (3day_forecast
1–14.05 и с 16.06, daypre для разрыва) и OEM (26 файлов, апрель–июнь);
(в) выбрать одну модель MMOD с обоснованием применимости к 51,6° и 2024 году —
ГОСТ 167 или ECSS/Grün — и записать единицы.

**Заблокирован:** заморозка типов — до твоего ответа.

---

## 2026-09-19 04:00 — Claude

**Сделано:** ветка `codex/plan-coordination` слита в `main` быстрой перемоткой —
редакция плана, `docs/PLAN_REVIEW.md`, `docs/TEAM_SYNC.md`, `docs/PROGRESS.md`
приняты. После созвона с трекером добавлен `docs/KRITERII_PLAN.md`: каждый
из 15 критериев → что проверяют → что делаем → чем доказываем → кто → когда →
как видно на защите. Там же решения: Streamlit Community Cloud бесплатно,
домен не покупаем, видео-демо как страховка, одна модель метеороидов по ECSS,
SOCRATES отдельно, три явных режима, семь слайдов с метками критериев.

**Изменилось для товарища:** правки к типам принимаются — `EventInterval.kind`,
`valid_from_utc`, `valid_to_utc`, реестр `source_id → raw_record_id → metadata`
в `Manifest`; внесу и после этого жду «согласовано». Вывод трекера: решает
подача, код смотрят вторым. Поэтому у каждой задачи в `KRITERII_PLAN.md`
есть строка «на защите» — делаем так, чтобы это было что показать.

**Нужно от товарища:** (1) `requirements.txt` и `runtime.txt` для Streamlit
Cloud из твоего lock — ты владелец lock по TEAM_SYNC; (2) история на трёх
датах утром первого дня: 3 мая, 1 июня, 25 июня — это первое, что покажем
жюри по Т4; (3) по R3–R7 из твоего обзора — согласен, внесу в договор v3.1
точечно, если ты дашь формулировки одной строкой каждую.

**Заблокирован:** нет.

---

## 2026-09-19 05:30 — Claude

**Сделано:** по заметкам с сессии экспертов добавлен раздел 5
в `docs/KRITERII_PLAN.md`: 14 пунктов экспертов → что делаем → уровень
(ядро / усиление / фича) → критерий.

**Изменилось для товарища — три вещи в ядре:**
1. **Два класса условий вместо одного** «требует проверки»: *критическое*
   (окно отменяется, ВКД прерывается: S2+ по наблюдению GOES) и
   *ограничивающее* (окно не планируется: S1 или прогноз S1 в окне, Kp ≥ 7,
   сближение с TCA в окне). Пороги обосновываются шкалами NOAA и ГОСТ.
   Это правка раздела 4 договора — внесу как v3.1 после твоего слова.
2. **SOCRATES фильтруется по 25544 в обоих столбцах, TCA в окне, промах**;
   список с промахом, скоростью и возрастом элементов. Вероятность попадания
   не выводим.
3. **Горизонт 24 ч** явно; траектория 32 ч; внешние прогнозы обрезаются
   до 24 ч с пометкой.
Усиление после ядра: техногенный мусор ориентировочно по ГОСТ 167 с оговоркой
о 55° и 2025; режим «Что если» с подставным `EventInterval`; два уровня
интерфейса. Фичи в конце: устойчивость, «Что если», оперативный интерфейс,
мусор, настольная сборка PyInstaller того же приложения — эксперты сказали,
что установщик зачтётся, но только после полного соответствия.

**Нужно от товарища:** подтвердить пороги критического и ограничивающего
классов или предложить свои с источником; `EventInterval` должен уметь нести
`kind_of_event = "simulated"` для режима «Что если».

**Заблокирован:** нет.

---

## 2026-09-19 06:30 — Claude

**Задача / версия:** матрица ролей; база e4950ff, ветка main.

**Сделано:** в `docs/KRITERII_PLAN.md` раздел 6 — согласованная матрица
ролей: кто есть кто (А — данные и время, Б — оценка и подача), граница
по типам стыка, каждый из 15 критериев → владелец → задачи A1–A5/B1–B6
из твоего `TEAM_SYNC.md` → что предъявляем → рубеж I0–I4; пункты экспертов
→ владелец. Твои идентификаторы и рубежи не менялись — только привязаны
к критериям.

**Проверено:** матрица сверена с `docs/TEAM_SYNC.md` разделами 1 и 4.

**Изменилось для товарища:** это единственный источник по ролям. Если что-то
в нём расходится с твоим пониманием — правим здесь, а не в двух местах.

**Нужно от товарища:** одна запись в твоём журнале: «роли согласованы»
либо список правок построчно. До этой записи и моей ответной матрица
не принята. Плюс подтверждение порогов двух классов условий из раздела 5.

**Заблокирован:** нет.

**Следующий шаг:** после «согласовано» — договор v3.1 (пороги, два класса,
правки типов R1/R2) и заморозка `vkd/types.py`.

---

## 2026-09-19 08:00 — Claude

**Задача / версия:** B1 + начало B2/B3; база 10972c1, ветка main.

**Сделано:** `requirements.txt`, `runtime.txt` (python-3.12 для Streamlit Cloud;
локально 3.14 работает). `vkd/assess/trapped.py` — интеграл потока захваченных
протонов по прил. А ОСТ: трапеции по энергии, хвост выше последнего узла
отброшен и объявлен, вне сетки L — «нет модели», выше зеркальной точки —
физический ноль со статусом, B/B0 < 1 не клампится, а помечается.
`vkd/windows/compare.py` — величины по окну и правило v3: охват → условия
(критическое S2+, ограничивающее S1/Kp7/сближение) → сравнение → сведение →
допуск (пока инженерный, помечен). `app/main.py` — каркас Streamlit, один
экран: запрос, траектория с эпохой и давностью, картина по времени с пролётами
аномалии и окнами, сравнение, вердикт с правилом, карточки доказательств,
данные с отключением GOES, выгрузка JSON. `experiments/stub_orbit.py` —
ВРЕМЕННАЯ орбита (дипольная L, помечена) до твоего A3.

**Проверено:** сквозной прогон, t0 = 2026-09-19 12:00Z явно; 1800 точек;
окна 53/30/7 мин в аномалии; вердикт «недостаточно оснований» с честным
списком: линия метеороидов не подключена, космопогода — частичное покрытие
(точки вне сетки L). 93 точки с B/B0 < 1 помечены статусом.

**Изменилось для товарища:** `app/main.py` импортирует `vkd.orbit.trajectory`,
если модуль есть, иначе заглушку. Сигнатура, которую жду от A3:
`trajectory(start_utc, minutes, saa_B_threshold_nT) -> (TrajectoryMeta, list[TrajectoryPoint])`.
Для A4 жду `EnvironmentSample` с `raw_record_id` и `published_utc`.

**Нужно от товарища:** «роли согласованы» и пороги; спецификация A5 (Grün):
формула, параметры, единицы — без неё линия метеороидов остаётся «не подключена»
и вердикт честно «недостаточно».

**Заблокирован:** нет.

**Следующий шаг:** карточка предупреждения по семи пунктам и ExportManifest;
затем деплой на Streamlit Cloud с аккаунта Тимофея.

---

## 2026-09-19 10:30 — Claude

**Задача / версия:** B2 (частично), B3, B4; база 652768d, ветка main.

**Сделано:** `vkd/explain/cards.py` — карточка предупреждения по семи пунктам
постановки, включая карточки условий с классом (критическое/ограничивающее)
и источником порога. `app/export.py` — выгрузка единым снимком: ZIP с
report.md, request/factors/recommendation/cards/sources/manifest.json и
raw/<record>.json. `app/norms.py` — панель норм как контекст (ГОСТ 215-85,
ООКОКП-2004, шкалы NOAA). `app/main.py` v0.2: два уровня интерфейса,
горизонт 24 ч на ленте, метка GOES, экран и выгрузка из одного снимка.
`vkd/assess/trapped.py` переписан: интерполяция по L между оболочками
(нет «дыр» между табличными L), интегрирование по энергии по степенному
закону между узлами — точное для a·E^b; трапеции на сетке ОСТ завышали
∝E⁻² на ~30 %, это задокументировано тестом. `vkd/windows/compare.py`:
хук `mmod_hits` для линии метеороидов (B2 по твоей спецификации A5).
Тесты: `tests/test_trapped.py`, `tests/test_compare.py` — 19 зелёных:
аналитический эталон интегрирования, границы модели, правило по ветвям,
отключённый источник → «недостаточно», устаревший GOES → частичное покрытие.

**Проверено:** `python -m pytest -q` — 19 passed. Приложение поднято и
открыто в браузере: рендер без ошибок, все блоки на месте.

**Изменилось для товарища:** линия метеороидов подключается одним аргументом
`mmod_hits` в `assess_window` — жду от A5 формулу и параметры, считаю в B2.
`EnvironmentSample.raw_record_id` попадает в архив как raw/<id>.json —
клади в него ключ сырой записи из своего кеша.

**Нужно от товарища:** без изменений: «роли согласованы», пороги, A5.

**Заблокирован:** нет.

**Следующий шаг:** деплой на Streamlit Cloud (нужен вход Тимофея); затем
режим «Что если» и чувствительность (О7).

---

## 2026-09-19 12:30 — Claude

**Задача / версия:** интеграция разбора Codex 20:35 UTC; B1/B4; база b64e7c4 → main.

**Сделано:** ветка `codex/agree-roles` слита. По разбору принято всё по существу:
- `vkd/types.py` **v2.1**: `EventInterval` с `kind`, `valid_from/to_utc`,
  `is_simulated` при физическом `kind_of_event`; `Manifest` с реестром по
  выпускам `source_versions[source_id][raw_record_id]`, `schema_version`,
  `effective_config`; `RecordMeta`; `Request.scenario_id`, `work_delay_min`;
  `FactorValue.horizon_utc`; `MechanismAssessment.priority`. Не заморожены —
  жду «согласовано».
- `vkd/windows/compare.py`: условия переименованы по NOAA — S ≥ 3 приоритетное
  (срочная проверка специалистом), S1–S2 предупреждение, Kp ≥ 7 триггер
  проверки; **никаких «прервать/продолжать»**; исключение из автовыбора
  подписано как политика прототипа. Тест на отсутствие таких команд.
- `CONTRACT.md` **v3.1**: раздел 4 п. 2 переписан; раздел 12 с R3–R7,
  горизонтом 32 ч, кешем, нормами как контекстом, типами 2.1. Внесено под
  поручением Тимофея на автономную работу; это ровно запрошенные тобой
  минимальные изменения.
- `docs/KRITERII_PLAN.md` раздел 7: поправки с приоритетом над текстом выше
  (команды отменены, lock у Б, сценарий у Б, горизонт, охват, история, пять
  исходов, недоказанное убрано, нормы вторичные).
- `requirements.txt` — точные версии; `runtime.txt` удалён (Python в Advanced
  settings). `.streamlit/config.toml` — три цвета, минимальная панель.
- `experiments/stub_sources.py` — ВРЕМЕННЫЙ слой источников до A4: живой
  GOES/Kp/TLE, кеш на диске с временем получения, при отказе — кеш или снимок
  с давностью и статусом, при отсутствии — «данных нет». Кеш Streamlit 5 мин
  плюс кнопка «Обновить данные сейчас» (явный путь обновления, не TTL).
- `app/main.py`: живые источники со статусами (ok / из кеша / отказ), метка
  Kp на ленте, `policy_note` в снимке.

**Проверено:** `pytest` 20 passed; приложение открыто в браузере с живыми
данными: GOES 0,22 pfu на 21:00Z получен живьём, TLE при таймауте CelesTrak
взят из снимка с давностью — статус виден; рендер без ошибок.

**Изменилось для товарища:** типы 2.1 по твоему списку — проверь и напиши
«согласовано» или остаток. `vkd.sources` при появлении вытеснит заглушку
автоматически: приложение ждёт `goes_latest(disabled)`, `kp_latest(disabled)`,
`tle_latest(disabled)` → `(sample, raw_dict, Fetch)`; сигнатура `Fetch` —
в `experiments/stub_sources.py`. Если удобнее другая — скажи, подстроюсь.

**Нужно от товарища:** «согласовано» по типам 2.1; A5 (Grün) — формула,
параметры, единицы; A2 — хотя бы отбор датированных уведомлений DONKI
за 10–11 мая для строгого режима. Без A5 вердикт остаётся «недостаточно».

**Заблокирован:** нет.

**Следующий шаг:** режим «Что если» и устойчивость вердикта; потребитель
истории с отсечкой по `published_utc` и тест вложенного будущего.

---

## 2026-09-19 14:30 — Claude

**Задача / версия:** B3/B4 усиление: история, «Что если», устойчивость; база fafc2fa → main.

**Сделано:**
- `vkd/assess/cutoff.py` — отсечка по публикации для каждого наблюдения
  (не по родительской записи): без времени публикации — «непригодно для
  строгого воспроизведения», наблюдение позже отсечки внутри старой записи —
  исключается, своевременный прогноз будущего — разрешён (R4, R6).
  Тесты: вложенное будущее не просачивается; добавление данных после
  отсечки не меняет отобранный набор; синтетика без публикации отсекается.
- `experiments/stub_history.py` — ВРЕМЕННЫЙ поставщик из архива DONKI до A2:
  `sentNotifications` → события с `published_utc = messageIssueTime`
  (датированные выпуски — основа строгого режима); `allKpIndex` → образцы
  БЕЗ публикации (в строгом режиме исключаются автоматически). На отсечке
  10.05 19:00Z из 283 событий остаются 77: FLR 14:15, SEP 15:03, GST 18:44
  видны, уведомление 19:19 отсечено.
- `vkd/windows/scenario.py` — «Что если»: задержка работ как свойство запроса
  (сдвиг окон, «изменение плана»), моделируемое протонное событие как
  `EventInterval(is_simulated=True, published_utc=None)`, скачок Kp как
  образец с `quality='model'`; метка на весь результат и выгрузку.
- `vkd/windows/sensitivity.py` — устойчивость: пересчёт на сетке порог
  аномалии × канал; допуск равнозначности = разброс минут в аномалии
  у лучшего окна (заменил инженерные 5 мин); «выбор устойчив / меняется».
- `compare.py`: события с интервалами дают условия; синтетика помечена
  «МОДЕЛИРУЕМОЕ».
- `app/main.py`: три режима с отсечкой, панель сценария, баннеры режима
  и сценария, таблица устойчивости, список исключённых отсечкой, события
  с публикацией; поставщик истории подменяется `vkd.history` автоматически.

**Проверено:** pytest 26 passed; в браузере — режим «Прогноз из прошлого»
на 10.05.2024 12:00Z: исключено 242 записи с причинами, события учтены,
реконструкция орбиты помечена, ошибок нет.

**Изменилось для товарища:** приложение ждёт от A2 `history_bundle() ->
(samples, events, raw)` с `published_utc` у каждой записи; от A3 —
`trajectory(start_utc, minutes, saa_B_threshold_nT, tle_path=None)`.
Заглушки в `experiments/` — образец сигнатур, не спецификация.

**Нужно от товарища:** без изменений — «согласовано» по типам 2.1, A5, A2.

**Заблокирован:** нет.

---

## 2026-09-19 18:00 — Claude

**Задача / версия:** B2 метеороиды, B4 визуализация, B5 эксперименты, B6 примеры и README; база f45e091 → main.

**Сделано:**
- **`vkd/assess/meteoroids.py` — вторая обязательная линия подключена.** ECSS-E-ST-10-04C
  Rev.1: Grün (10-1) × Table J-6 (G=2,00, s_f=0,63, K=1,09 на 400 км) × A × T,
  односторонняя случайно кувыркающаяся пластина 1 м². **Твоё контрольное число
  5,609728448e-7 воспроизведено до 1e-6** (год 365,25 сут, как в константе
  формулы). Table J-5 стандарта воспроизводится с точностью 0,6 % на 9 массах.
  Метеорные потоки даты не включены и это объявляется (10.2.2.2c); техногенный
  мусор не включён; вне Table J-5/J-6 — отказ, не кламп. Спецификация со
  страницами — от агента, разобравшего PDF стандарта.
- `app/compute.py` — конвейер без интерфейса; экран, выгрузка и примеры строятся
  одним `run()`. `scripts/make_examples.py` → `examples/` — 9 сохранённых
  расчётов (JSON + ZIP) на демо-даты: буря Гэннон при отсечках 12:00 и 19:00,
  3 мая, 20 мая (разрыв архива), 25 июня, разбор, два сценария «Что если».
- Вердикты различаются по ситуациям: сегодня — preferred; 10.05 12:00Z —
  preferred (к полудню ничего не опубликовано); 10.05 19:00Z — all_need_check;
  20.05 — equivalent; 25.06 — preferred.
- `vkd/windows/compare.py`: покрытие по долям (≥95 % полное, ≥50 % частичное),
  частичное объявляется и не блокирует (твой п. 14); пустой каталог DONKI за
  покрытый период — «событий не объявлено», а не «нет данных»; исход
  **trade_off** реализован (механизмы указывают на разные окна); условиями
  становятся только SEP и GST — **эксперимент на контрольной неделе показал,
  что «любое событие → условие» давало 113 ложных пометок**, исправлено.
- `experiments/stub_orbit.py`: эксцентричный диполь (смещение 605 км по
  квадрупольным членам IGRF); доля точек ниже сетки L=1,14 упала с 21 % до
  17,5 % — приближение всё ещё уходит ниже в ядре аномалии, доля объявляется
  в карточке; окончательно лечит только трассировка силовой линии (A3).
- `app/viz.py`: карта трассы с областью аномалии по IGRF и окнами; график
  сравнения окон (минуты в аномалии, флюенс, N метеороидов). `app/main.py`
  переведён на `compute.run`.
- `experiments/gannon.py` + `docs/EKSPERIMENTY.md` (агент): заблаговременность
  уведомлений DONKI относительно физического начала, таблица «отсечка → известно
  / случилось потом», контрольная неделя 15–22 июня, базовый метод «последнее
  наблюдение» в строгом режиме слеп (24/24 Kp без публикации), устойчивость
  набора условий при сдвиге отсечки ±3 ч — все изменения объяснены именованными
  записями. README.md перезаписан агентом как документ сдачи; будет освежён в конце.
- Карточки условий: слова «прервать/продолжать» удалены; класс «приоритетное»
  распознаётся; Kp DONKI: observedTime — конец 3-часового интервала.

**Проверено:** pytest 39 passed; примеры сгенерированы; приложение в браузере:
карта, график, вердикт «предпочтительно», N = 5,6e-7 в подписи, ошибок нет.

**Изменилось для товарища:** A5 закрыт с нашей стороны по твоей спецификации
и ECSS; если у тебя иные множители (например, G по одной скорости 20 км/с
даёт 1,29 вместо 2,00) — скажи, переключу. Каталог DONKI считается покрывающим
1 мая — 30 июня; если A2 даст иное покрытие — поправлю.

**Нужно от товарища:** A2 (архив GOES 2024 и датированные выпуски NOAA),
A3 (трассировка L — снимет 17,5 % точек вне сетки), «согласовано» по типам 2.1.

**Заблокирован:** нет.

---

## 2026-09-19 22:00 — Claude

**Задача / версия:** Т1 автообновление, Т8 воспроизводимость; база cd83f1a → main.

**Сделано:**
- Автообновление в текущем режиме: фрагмент Streamlit по таймеру (5/10/15 мин,
  0 — выключено) очищает кеш и перезапускает расчёт; кнопка — принудительное
  обновление. Частоты публикации подписаны (GOES 5 мин, Kp 3 ч).
- `scripts/replay_example.py`: повтор сохранённого расчёта из ZIP или JSON
  с проверкой совпадения вердикта. **Найден и устранён дефект воспроизводимости:**
  (1) исторический режим до OEM зависит от текущего TLE — теперь снимок хранит
  текст TLE, повтор строит орбиту по нему; (2) разброс устойчивости зависел от
  сохранённого допуска, потому что внутри сетки «равнозначны» обнулял
  предпочтение — сетка теперь считается с нулевым допуском. Все 7 исторических
  примеров воспроизводятся; тесты `tests/test_replay.py` закрепляют оба случая.
- Таймаут живых источников 8 с; при отказе — кеш или снимок со статусом.

**Проверено:** pytest 41 passed; повтор 7/7; приложение в браузере с
автообновлением, ошибок нет. GitHub и GitVerse синхронны.

**Изменилось для товарища:** `run(..., tle_override_path=)` — при подключении OEM
(A3) для истории используй тот же механизм: снимок должен хранить
идентификатор и хеш файла эфемерид, чтобы повтор был строгим.

**Нужно от товарища:** без изменений: A2, A3, «согласовано» по типам 2.1.

**Заблокирован:** нет.

---

## 2026-09-19 23:30 — Claude — ИТОГ ВЕЧЕРА, для сверки

**Задача / версия:** main на этом коммите; всё ниже проверено тестами и в браузере.

**Что работает целиком:** запрос → живые источники с кешем и статусами →
траектория с полем → оценка по двум обязательным линиям (космопогода на
траектории, метеороиды ECSS) → условия только по SEP/GST/сближениям →
пять исходов с видимым правилом → карточки по семи пунктам → нормы как
контекст → выгрузка единым снимком → повтор из снимка. Три режима, два уровня
интерфейса, «Что если», устойчивость, автообновление. Визуализация: наблюдения
за трое суток, лента, карта с аномалией, сравнение окон.

**Точки стыка, где ты меня заменяешь (заглушки в `experiments/` — образец
сигнатур, приложение подхватит твои модули автоматически):**
- `vkd.orbit.trajectory(start_utc, minutes, saa_B_threshold_nT, tle_path=None)
  -> (TrajectoryMeta, list[TrajectoryPoint])`; в истории — OEM, `method='oem_interp'`,
  `is_reconstruction` по доказанной доступности; поле `field_model` —
  «IGRF-13» для строгой линии 2024 (R5).
- `vkd.sources.goes_latest(disabled)`, `kp_latest(disabled)`, `tle_latest(disabled)`
  -> `(EnvironmentSample|None, raw_dict, Fetch)`; `Fetch` — см. `experiments/stub_sources.py`.
- `vkd.history.history_bundle() -> (samples, events, raw)`; у всего, что идёт
  в строгий режим, обязателен `published_utc`; наблюдения без публикации
  отсекаются `apply_cutoff` автоматически.
- `vkd.conjunctions` — по желанию; `assess_window(..., conj=[...])` уже принимает
  `Conjunction` с `table_truncated`.

**Чек-лист сверки для тебя (ответь построчно «ок» или правку):**
1. Метеороиды: множители Table J-6 (G 2,00, s_f 0,63, K 1,09), год 365,25 сут,
   пластина 1 м², m ≥ 1e-3 г — твоя спецификация? Число совпало до 1e-6.
2. Условия: только SEP и GST из событий; CME/FLR — информация. Kp ≥ 7 триггер.
   S ≥ 3 приоритетное. «Прервать/продолжать» удалены везде.
3. Покрытие: ≥95 % полное, ≥50 % частичное (объявляется, не блокирует),
   пустой каталог DONKI за 01.05–30.06 = «событий не объявлено».
4. Kp из карточек DONKI: `observedTime` — конец 3-часового интервала;
   без публикации → только разбор после факта.
5. Типы 2.1 — «согласовано»?
6. Каталог покрытия и отсечки: примеры 03.05, 10.05 (12Z и 19Z), 20.05, 25.06
   лежат в `examples/`, воспроизводятся `scripts/replay_example.py`.

**Конфликтов нет:** твои файлы (`docs/TEAM_SYNC.md`, `docs/PLAN_REVIEW.md`,
`docs/PROGRESS.md` — дописал раздел состояния, твой текст не тронут) и
пакеты `vkd/sources|orbit|history|conjunctions` не менялись. Все мои правки —
в `app/`, `vkd/assess`, `vkd/windows`, `vkd/explain`, `experiments/`, `tests/`,
`scripts/`, `examples/`, `CONTRACT.md` v3.1 (по твоему п. 16), `docs/KRITERII_PLAN.md`.

**Нужно от товарища:** A2, A3, A4 в любом порядке; чек-лист выше.

**Заблокирован:** нет.

## 2026-09-19 03:45 UTC — Claude (Б)

**Задача / версия:** слияние `codex/a1-sources` (d095c7a) в main — коммит aee0b42;
интеграция I1 — следующий коммит. Твои файлы не менялись; конфликт был только
в `journal/friend.md` (обе дописки сохранены по времени).

**Сделано:**
- Твои тесты на Windows / Python 3.14 проходят вместе с моими: 88 тестов, 559 подтестов.
- **Орбита A3 подключена** через `vkd/integration/orbit_bridge.py`. Заглушка
  `experiments/stub_orbit.py` удалена, отката к ней нет: ошибка орбиты → «орбита
  недоступна», покрытие NONE, вердикт insufficient (тест `test_stale_tle_gives_no_orbit…`).
  - текущий режим: TLE от слоя источников кладётся в `data/cache/orbit_root/<sha12>/data/orbit/`
    с manifest.json (sha256, bytes, fetched/available) и копиями IGRF — твой `_checked_model`
    проверяет байты снимка как свои. Предел возраста — настройка `Thresholds.tle_max_age_days = 3`.
  - история: `history_forecast` → строгий вызов → `OrbitDataError` (доступность OEM не доказана)
    → `history_review` с `cutoff_utc = t0` (создание и S3-модификация не позднее отсечки) →
    `strictness = 'declared_reconstruction'`; ограничение дописано в provenance, показано
    в интерфейсе и выгрузке. **Решение Р-О1 — моё**, не молчаливая подмена. Если ты против,
    напиши — переключу строгий режим на отказ без орбиты.
  - `provenance` → снимок (`trajectory_meta.provenance`) и `raw/orbit_provenance.json` в ZIP.
- **Прогнозы NOAA A2 подключены** через `vkd/integration/noaa_forecast.py`: каналы
  `kp_forecast` (3 ч), `s1_prob_daily`, `proton_prob_daily` как `EnvironmentSample(kind=EXTERNAL_FORECAST)`
  с исходным разрешением. В окне — факторы «прогноз Kp NOAA, максимум в окне» и две суточные
  вероятности (в окно не пересчитываются, подпись об этом). Условие проверки: прогноз Kp ≥ 7
  в окне, помечено словом «прогноз», карточка `external_forecast`. **Предложение R9** в CONTRACT §12.
  - Gannon, отсечка 10.05 12:00: выпуск 202405100030 даёт Kp 7,0 / 8,33 на 11.05 03–09 UTC,
    S1+ 50 %, daypre 20240509 — протон 50 %. Окно 11.05 03:00 помечено по прогнозу; окно
    10.05 12:00 без условий → preferred. Отсечка 20.05: `kp_forecast` missing — объявлено, не блокирует.
- **Метеороиды B2 по A5**: `meteoroid_hits_track` — F_LEO по фактической высоте трассы, J-6
  линейно между узлами, трапеции по фактическим dt, концы окна включены; неполная трасса →
  PARTIAL с долей. Тесты против `data/meteoroids/grun_reference.json`: 30 пар J-5 в 0,6 %,
  контроль 6/8 ч с rtol 1e-10, линейность по площади, аддитивность при дроблении, убывание
  по массе, независимость от числа точек, отказ ниже 100 км.
- DONKI: время публикации = max(messageIssueTime, `published_utc` из твоего реестра) —
  закрывает 20240516-7D-001 (13 ч 56 мин).
- Примеры `examples/` (9) перегенерированы; исторические воспроизводятся без TLE.

**Изменилось для товарища:** main = твоя ветка + мост Б. `Thresholds` получил
`tle_max_age_days`; `assess_window` — `forecasts`, `mmod_cov_fraction`. Версия алгоритма 0.4.0-i1.

**Нужно от товарища (по приоритету):**
1. **A4** — живые источники с записью манифеста (TLE, GOES ≥10 МэВ, Kp): сигнатуры —
   `experiments/stub_sources.py` (`goes_latest/kp_latest/tle_latest -> (sample, raw, Fetch)`).
   Вход орбиты принимает любой корень с `manifest.json`: если A4 будет писать
   `data/orbit/manifest.json` + `iss.tle` сам, я уберу staging.
2. Ответ по **Р-О1** (OEM до отсечки как объявленная реконструкция в строгом режиме).
3. Ответ по **R9** (условие по прогнозу Kp).
4. **A2**: содержательный разбор DONKI (target_scope, интервалы действия) и архив наблюдений
   GOES 2024 — тогда канал GOES в истории станет наблюдением, а не «частично по уведомлениям».
5. Типы 2.1 — «согласовано»?

**Проверено:** `python -m pytest -q` — 88 passed, 559 subtests; AppTest трёх режимов без
исключений; `scripts/replay_example.py` — 7/7 совпадений.
**Заблокирован:** нет.

## 2026-09-19 04:40 UTC — Claude (Б), дополнение по моделям

**Найдено по сохранённым примерам после стыковки, исправлено, покрыто тестами (92 passed):**
1. **Флюенс захваченных протонов был тождественно нуль** с координатами A3: центральный
   наклонённый диполь в ядре аномалии даёт L ≈ 1,07 (< 1,14 — вне сетки ОСТ), вне ядра
   B/B0 уходит за точку отражения; на 6 ч трассы 10.05.2024 — ни одной точки с моделью.
   Для входа в таблицы теперь эксцентричный диполь (Б, `vkd/assess/magcoords.py`,
   Fraser-Smith 1987 по квадрупольным членам того же IGRF-13/14): смещение ≈0,08 R_E,
   L и B0 от смещённого центра, |B| — твой полный IGRF. B/B0 < 1 помечается (inconsistent_BB0),
   не обрезается. Флюенс стал 0,6–4,6·10⁶ част./см² за 6 ч при 76–86 % точек с моделью.
   **Предложение R11 в CONTRACT §12.** Твои L/B_over_B0 в точках не используются для
   таблиц — только |B| и признак аномалии. Правильная замена — трассировка силовых линий
   (McIlwain L) в A3; если сделаешь, переключусь на неё.
2. **Одно протонное событие давало три условия** (уведомление + карточки по двум
   приборам). Связанные записи сводятся в одно условие с перечнем записей (Т3).
3. **Прогнозы прихода выбросов WSA-ENLIL** из карточек CME (`enlilList`: estimatedShockArrivalTime,
   estimatedDuration, kp_18/90/135/180, modelCompletionTime как публикация) подключены как
   датированный внешний прогноз — условие при ожидаемом Kp ≥ 7 (**R10**). На отсечке
   10.05 12:00 прогоны 9.05 19:00–20:46 давали приход 12:12–13:03 с Kp до 9 — оба окна
   помечены; факт: SSC ≈17:05, Kp 9. Раньше система на этой отсечке рекомендовала окно 16:00.
4. **Уведомления о буре — условие только при Kp ≥ 7** в теле сообщения (читаю из твоего
   `donki/raw/messages/*.json`): 3 мая буря G2 (6,67) больше не помечает окна; 20 мая приход
   выброса с Kp до 3 — тоже. Уровень не назван → условие с пометкой (консервативно).

**Нужно от товарища:** ответ по R10/R11; в A2 при разборе DONKI — уровень Kp из тела
уведомления и `enlilList` как отдельный датированный источник (я читаю их из
`data/archive_2024/donki_cme_*.json`, твой реестр сообщений — для времени публикации и тела).

## 2026-09-19 05:30 UTC — Claude (Б), настройки вне кода и выбор оценки Kp для ENLIL

- **Т7**: появился `config/settings.toml` (пороги, тайм-аут и адреса источников, поля Kp ENLIL,
  умолчания интерфейса) и `vkd/config.py` (`VKD_SETTINGS` для подмены пути). `Thresholds.from_settings()`
  — неизвестный ключ в файле даёт ошибку, а не молчаливое умолчание. Профессиональный уровень
  показывает и правит пороги поверх файла; всё применённое — в снимке.
- **Т5, ENLIL**: `experiments/forecast_lines.py` → `docs/EKSPERIMENTY_PROGNOZ.md`. 240 отсечек ×
  4 окна = 964. Буря Гэннон: система 28 попаданий / 0 пропусков, база «последнее наблюдение» 19 / 9.
  Контроль: с верхней оценкой kp_180 линия ENLIL давала 54 ложные тревоги, с типичной kp_90 — 3
  при той же полноте на буре. Принял `[history].enlil_kp_fields = ["kp_90"]`; верхняя оценка
  остаётся в тексте условия («Kp до 8; верхняя при южном поле 9»).
- Тесты: 94 passed, 559 subtests; AppTest трёх режимов и пути «GOES отключён» без исключений.

**Нужно от товарища:** ничего нового; ответы по R9–R11 и Р-О1, A4/A2 по-прежнему.

## 2026-09-19 06:00 UTC — Claude (Б), Т6 и О4

- **Т6**: переключатель источника стал трёхпозиционным: «включён» / «отказ: только кеш»
  (покрытие частичное, объявлено — по твоему п. 14) / «исключён: нет данных» (линия без
  покрытия → «оснований недостаточно»). Проверено AppTest: оба состояния без исключений,
  вердикты соответствуют. Значение попадает в `request.disabled` снимка.
- **О4/Т1**: карточка условия теперь несёт идентификаторы записей, давших условие, и ссылки
  на первоисточник (URL выпуска NOAA, messageURL DONKI, ссылка прогона ENLIL, адрес GOES/TLE);
  дубли одного сообщения DONKI по нескольким карточкам убраны (341 событие, 341 уникальный id).

## 2026-09-19 06:30 UTC — Claude (Б), резервные адреса TLE (Т6/Т1)

- CelesTrak (gp.php и GROUP=stations) 19.09 давал тайм-аут по 12 с; снимок TLE в репозитории
  к защите станет старше допустимых 3 сут → «текущая обстановка» осталась бы без орбиты.
  Введена резервная цепочка `[sources.urls].tle` (CelesTrak → api.wheretheiss.at →
  tle.ivanstanojevic.me): первый разобранный ответ идёт в расчёт и кеш, адрес — в `Fetch.url`
  и в манифест орбиты (`celestrak_gp:25544:<sha12>.url`). Статус источника перечисляет,
  кто отказал. Разбор JSON/текста — `tle_from_text`, тест без сети `tests/test_sources_tle.py`.
- Для A4: у NASA есть текущий OEM (`nasa-public-data.s3.amazonaws.com/iss-coords/current/ISS_OEM/
  ISS.OEM_J2K_EPH.txt`, 19.09 — 792 КБ, CREATION_DATE 2026-09-18T15:36) — это тот же формат,
  что твой `parse_oem`; как живой источник орбиты он лучше TLE (учитывает планируемые манёвры).
  Если A4 добавит его как `mode='live_oem'` с записью манифеста — подключу вместо SGP4 с пометкой.

## 2026-09-19 02:50 MSK (18.09 23:50 UTC) — Claude (Б), сверка документов с кодом

**Задача / версия:** область «docs» общего захода по находкам аудита; база `main` `6224156`,
ветка `wf/docs` (worktree), передаваемый SHA — коммит этой ветки. Код не менялся.

**Сделано (только документы):**
- `docs/PROGRESS.md` переписан: один актуальный раздел «Состояние к защите 20.09.2026»
  (SHA, 97 проверок и 559 подпроверок за ≈10 с, AppTest 3 режима × 2 уровня без исключений,
  повтор примеров 6/6 исторических, три режима, два уровня, орбита A3, временные слои
  источников и DONKI, известные ограничения), «что осталось и кто следующий»; старые снимки
  свёрнуты в «История редакций» с датами и пометками «устарело» там, где они противоречат
  коду («заглушка орбиты», «41 тест», «интерфейс требует переделки», «TLE в истории»).
- `docs/KRITERII_PLAN.md`: Т3 — степенной закон между узлами, хвост выше 300 МэВ отброшен и
  объявлен, линия «минут доступности частиц» не реализована; Т7 — `config/settings.toml`,
  только `VKD_SETTINGS`, `.env` не читается, проверки запроса сверх ползунков нет (открытый
  пункт T7-1); О2, О3 (пять исходов), Т4 (OEM как объявленная реконструкция), Т6 (три
  состояния источника) и строка «интерфейс» в разделе 0 приведены к коду; добавлен раздел 9
  «Состояние по критериям к защите» — сделано / нет или иначе / чем показываем, по всем О1–Т8.
- `docs/case/ANALIZ_KEYSA.md`: в шапке — что C++/Qt/DLL и скафандр ниже суть история;
  добавлен раздел 13 «Что изменилось при реализации» (обрезание не используется, ECSS вместо
  ГОСТ 167, OEM вместо TLE, SOCRATES не подключён, эксперименты Т5 по прогнозам NOAA/ENLIL).
- `docs/ZAMYSEL.md`: статусный блок сверху — история замысла до разворота, таблица «что дожило
  до сервиса»; тело не редактировалось.
- `CONTRACT.md` §12: предложение **R12** — интегрирование степенным законом и хвост выше
  300 МэВ, единица флюенса част./см², линия доступности не реализована. Строки раздела 3
  («трапеции», «част./(см²·ср)») не тронуты — правит только человек.

**Проверено:** `python -m pytest -q` в worktree — 97 passed, 559 subtests, 9,5 с;
`streamlit.testing.v1.AppTest` — 3 режима × 2 уровня, 0 исключений;
`scripts/replay_example.py` на всех 9 `examples/*.json` — 6/6 исторических «ВОСПРОИЗВЕДЕНО»,
3 текущего режима совпали (не гарантируется). Утверждения раздела 1 PROGRESS сверены по коду:
`app/compute.py` (`SRC_LAYER`, `HIST_SRC`, `ALGO_VERSION`), `vkd/assess/trapped.py`,
`vkd/windows/compare.py` (`cutoff` не встречается), `vkd/config.py`, `app/main.py` (ползунки,
`st.spinner`, вкладки), манифесты `examples/*.zip` (`git_commit` 9b3cc87).

**Не сделано, вне области (для интегратора):** README — «около 40 с» → «≈10 с», «баннер» →
«плашка в полосе состояния» (O5-21, T8-4; README правит отдельный шаг); `data/spaceweather/README.md`
разделы 2 и 4 описывают старый замысел «живые данные только показываются» и обновление TLE
без манифеста (T1-14) — файл А; перегенерация `examples/` на итоговом коммите; `python-dotenv`
в `requirements.txt`/`pyproject.toml` не используется — убрать или оставить, но не заявлять.

**Изменилось для товарища:** ничего в коде и в твоих файлах. В `CONTRACT.md` §12 добавлено
предложение R12 (ждёт твоего «ок» вместе с R9–R11, Р-О1, Р-О2); `docs/PROGRESS.md` теперь
читается по разделу 1, твои старые строки сохранены в «Истории редакций».

**Нужно от товарища:** ответ по R12 (и по R9–R11, Р-О1, Р-О2); `data/spaceweather/README.md`
разделы 2 и 4 привести к текущей схеме (GOES/Kp — входы условий с порогами из `config`,
снимок TLE — `data/orbit/iss.tle` с манифестом, дата выгрузки архива DONKI).

**Заблокирован:** нет.

**Следующий шаг:** после закрытия T7-1 (проверка запроса в `run()`) обновить строку Т7 в
`docs/KRITERII_PLAN.md` и пункт ограничений в `docs/PROGRESS.md`; после перегенерации примеров —
SHA в разделе 1 PROGRESS.

## 2026-09-19 04:10 MSK (01:10 UTC) — Claude (Б), интеграция четырёх ветвей захода 19.09

**Задача / версия:** интегратор захода по находкам аудита; база `main` `6224156`; слиты
`wf/models` (df48c23), `wf/data` (707dee9), `wf/ui` (1a1ae0a), `wf/docs` (94787bd) в порядке
models → data → ui → docs, все `--no-ff`; затем интеграционные коммиты `2f8178b`, `350840a`,
примеры пересобраны на `350840a`. Push не делался.

**Сделано:**
- Единственный стык, который потребовал правки: модели ждали Kp разбора из карточек GST DONKI,
  слой данных подал окончательный ряд GFZ (488 интервалов). В `app/compute.py` тексты источника
  Kp и `origin` теперь зависят от `source_id` записи (GFZ / GST), тест давности от t0
  переписан: внутри архива интервал моложе 180 мин — покрытие полное; за концом архива
  (01.07.2024 12:00) — «устарело» по давности от t0, а не от 2026 года.
- Экран (`app/main.py`, `app/ui.py`): карточки объяснений для любого окна (переключатель),
  «Проверка после отсечки» (в расчёт не входит) — строкой под карточками и подробно во вкладке
  наблюдений; живые источники запрашиваются только в текущем режиме; границы постановки
  проверяются `validate_request` до любого запроса и печатаются по-русски; исключённые отсечкой
  записи — итог по источникам и причинам, интервалы Kp свёрнуты в одну строку; вкладка
  устойчивости показывает два исхода на ячейку (лучшее без допуска / предпочтительное с итоговым
  допуском), разброс разности минут по порогу и отношения флюенсов по каналу; полоса состояния
  в разборе показывает Kp архива с давностью; источник `donki_archive` назван по-русски.
- `config/settings.toml` `[history] enlil_publication_lag_min = 60` (читался кодом слоя данных
  с умолчанием — теперь виден в настройках). Версия алгоритма `0.5.0`.
- Т8: JSON-снимок примеров и выгрузки экрана несёт `git_commit`; `replay_example.py` читает
  его из JSON, поэтому исторические примеры по JSON дают код 0, а не 3.
- Документы: T7-1 закрыт (`KRITERII_PLAN` Т7, `PROGRESS` ограничения), число тестов, правило
  рекомендации и примеры в `PROGRESS` разделе 1.

**Проверено:** `python -m pytest -q` после каждого слияния и в конце — 179 passed, 559 subtests
(≈15 с); `AppTest` — три режима × два уровня, переключатель окна, Kp исключён из архива,
GOES «исключён» и «отказ: только кеш», сценарий (протонное событие + задержка 90 мин + скачок
Kp), три окна, одинаковые сдвиги (ожидаемый `st.error` без исключения), разбор 30.06 20:00
за концом архива — 0 исключений; `scripts/make_examples.py` (сеть, live-примеры);
`scripts/replay_all.py` — 9/9 ВОСПРОИЗВЕДЕНО (код 0); `replay_example.py` по каждому JSON —
6 исторических код 0, 3 текущего режима код 2 (JSON без сырых записей, по замыслу).

**Изменилось для товарища:** твои каталоги (`vkd/sources`, `vkd/orbit`, `vkd/history`,
`vkd/conjunctions`, `data/*`, `tests/orbit|history|sources`, `docs/methods`, `docs/TEAM_SYNC.md`,
`docs/PLAN_REVIEW.md`, `docs/REPOSITORY_REVIEW.md`, `journal/friend.md`) не тронуты. Стык:
`vkd/types.py` 2.2 — `Condition`, поля `MechanismAssessment.conditions/coverage_notes` с
умолчаниями; `Thresholds` — `kp_max_age_min`, `fluence_equiv_ratio`, `sep_valid_hours`;
`FluxResult.value_per_cm2_s_sr` → `value_per_cm2_s`; снимок S — ключи `effective_config`,
`verification`, `windows[].conditions`, `history.catalog_coverage`, `robustness.*`; `Fetch` —
поля `raw_response_path`, `raw_response_sha256`, `provider_time`, `fetched_basis`.
`CONTRACT.md` §12: R12 (предложение, ждёт твоего «ок»).

**Нужно от товарища:** ответы по R9–R12, Р-О1, Р-О2; для A2/A4 — где взять датированную
публикацию Kp (GFZ nowcast с временем выпуска), иначе в строгом режиме Kp остаётся без
наблюдения.

**Заблокирован:** нет.

**Следующий шаг:** README отдельным шагом (число тестов 179, версия алгоритма 0.5.0, время
тестов ≈15 с, «баннер» → «плашка в полосе состояния», коммит примеров); затем деплой
`streamlit_app.py` на share.streamlit.io владельцем и семь слайдов к защите 20.09.

## 2026-09-19 — Claude, второй круг: документы (D1–D4)

**Задача / версия:** D1–D4 области «docs», базовый SHA `d674dba`, ветка `wf2/docs`,
локальный коммит в этой ветке (в `main` не сливал, не пушил). Версия алгоритма `0.5.0`,
схема снимка `2.2`.

**Сделано:**
- `README.md` сверен с кодом целиком, а не только по числам. Числа: 97 → **179 проверок**
  (559 подпроверок), «около 40 с» → **около 15 с**, `0.4.1-i1` → **`0.5.0`**, схема `2.1` →
  `2.2`, коммит описания `df7305c` → `d674dba`, R9–R11 → R9–R12. Слово «баннер» убрано
  везде: на экране их нет, есть **плашки в полосе состояния** («Архив: весь, без отсечки»,
  «После отсечки не использовано: N записей», «Сценарий: моделируемые значения»).
- Раздел «Что делает сервис» переписан под текущий `app/main.py`: объяснения доступны для
  **любого** окна через переключатель (было «окна 1»); в строгом режиме под карточками —
  строка «Проверка после отсечки» (в расчёт не входит); живые запросы делает **только**
  текущий режим (было «в любом режиме»); границы запроса проверяются до расчёта (период
  поиска 60–1440 мин, а не 0–1440; сдвиг окна ограничен периодом, окна нумеруются по
  времени, одинаковые начала предупреждаются); устойчивость даёт **два исхода на ячейку**
  сетки (лучшее без допуска / предпочтительное с итоговым допуском), допуск — разброс
  разности минут по порогу и отношения флюенсов по каналу.
- Найдено сверкой и исправлено сверх задания (README говорил обратное тому, что делает код):
  1) **«минут доступности частиц канала» теперь выдаётся** — две величины по вертикальной
  жёсткости обрезания A3 (≥10 и ≥100 МэВ, `compare.py`); README и раздел 13 утверждали, что
  жёсткость в оценке не используется. `CONTRACT.md` §12 R12 п. 2 («линия не реализована»)
  тоже отстал от кода — правка договора за человеком, я его не трогал;
  2) **метеорные потоки даты** присутствуют отдельной величиной-признаком по календарю IMO
  (вклад в число попаданий не рассчитан) — было «не включены»; поправлены разделы 4, 12, 13
  и строка недостающего охвата;
  3) покрытие флюенса в девяти примерах **полное** (`n_outside_model` = 0) — было «76–87 %
  точек, покрытие частичное»; частичным остаётся покрытие механизма из-за GOES;
  4) наблюдения Kp в истории берутся из **окончательного ряда GFZ** (карточки DONKI —
  резерв), 488 интервалов исключаются строгой отсечкой — было «Kp из карточек DONKI»;
  5) публикация прогона WSA-ENLIL = завершение + `enlil_publication_lag_min` (60 мин), но не
  раньше подачи анализа — было «время завершения прогона»;
  6) `[history].sep_valid_hours` **читается** кодом (`Thresholds.from_settings()`) — README
  трижды утверждал обратное; не читается как настройка только `[sources].cache_ttl_s`
  (`ttl=300` зашит в `app/main.py`) — это осталось в ограничениях;
  7) `policy_note` в разделе 12 приведён к тексту `POLICY_NOTE` из `app/compute.py`;
  8) таблица примеров и таблица тестов пересобраны по факту (в том числе
  `end_2024-06-25_12Z` — `preferred`, а не `equivalent`; допуски 31/32/21/20 мин).
- `data/spaceweather/README.md`, разделы 2–4 приведены к текущей схеме: цепочка **трёх**
  адресов TLE из `config/settings.toml` → кеш → снимок как **последний резерв**, причём
  снимок TLE берётся из `data/orbit/iss.tle` с записью манифеста A3 (файлы Codex), а
  справочный `data/spaceweather/iss.tle` в цепочке не участвует; IGRF сервис читает из
  `data/orbit/IGRF13.shc` и `IGRF14.shc` (файлы Codex, хеши в манифесте), а
  `igrf14coeffs.txt` читает только `experiments/demo_pipeline.py`; добавлен живой **Kp через
  JSON API GFZ** (`kp.gfz.de/app/json/`, двое суток, `datetime` — начало интервала, `def` →
  окончательное, снимка в репозитории нет → без сети и кеша «данных нет»). Убрана устаревшая
  фраза «живая космопогода — отдельный от расчёта информационный блок».
- `docs/EKSPERIMENTY.md` и `docs/EKSPERIMENTY_PROGNOZ.md` **не менял**: сверка показала, что
  оба файла уже соответствуют коду 0.5.0 и `examples/INDEX.md`. Все 14 строк таблиц
  `EKSPERIMENTY_PROGNOZ.md` совпали с `examples/experiments/forecast_lines.json` построчно;
  у `EKSPERIMENTY.md` сошлись числа исключённых отсечкой записей с примерами
  (488 + 27 + 218 = 733 на 12:00Z и 488 + 27 + 208 = 723 на 19:00Z — ровно как в `INDEX.md`).
  Скрипты не перезапускал. Устаревшим оказался пересказ этих отчётов в README разделе 10 —
  его и поправил: система на буре 27/0/18 против 18/9/10 у базовой линии (было 28/0/20 и
  19/9/17), на контроле 0/5/16 по буре и 35/29/3 по протонному событию, чувствительность
  ENLIL на контроле 69/37/6 ложных тревог для kp_180/kp_135/kp_90 (было 54/34/3), первая
  отсечка с условием по буре — ENLIL 10.05 00:00Z, NOAA 10.05 06:00Z, уведомления
  11.05 00:00Z (было «уведомления 09.05 18:00Z»).

**Проверено:** `python -m pytest -q` в копии — **179 passed, 559 subtests** (16,9 с), до и
после правок; код не трогал, только документы. `scripts/replay_example.py` по всем девяти
примерам, ZIP и JSON: шесть исторических дают **код 0** и по ZIP, и по JSON (в README это
теперь записано явно), три примера текущего режима — код 0 по ZIP и код 2 по JSON (нет сырых
записей, так задумано). Сверка `EKSPERIMENTY_PROGNOZ.md` с сохранённым JSON — 14/14 строк.
AppTest не запускал: в области «docs» нет изменений экрана.

**Изменилось для товарища:** ничего. Каталоги `vkd/sources`, `vkd/orbit`, `vkd/history`,
`vkd/conjunctions`, `data/source_registry_2024`, `data/orbit`, `data/meteoroids`,
`tests/orbit|history|sources`, `docs/methods`, `docs/TEAM_SYNC.md`, `journal/friend.md` не
тронуты; `CONTRACT.md` не тронут. Правил только `README.md`,
`data/spaceweather/README.md` и этот журнал.

**Нужно от товарища:** прежние ответы по R9–R12, Р-О1, Р-О2. Дополнительно: в `CONTRACT.md`
§12 R12 п. 2 написано, что линия «минут доступности частиц канала» не реализована, — она
реализована; текст договора правит человек, нужно твоё «ок» на новую формулировку.

**Заблокирован:** нет.

**Второй круг, 19.09 — что где:**
- **D1–D4 (документы)** — сделано, ветка `wf2/docs`, этот коммит.
- **M1–M12 (модели)** и **U1–U5 (экран)** — **в работе**: ветки `wf2/models` и `wf2/ui`
  созданы (`git branch --list wf2/*`), на момент записи своих коммитов поверх `d674dba` в них
  ещё нет, содержание списков в репозитории не опубликовано. Слияние и общая приёмка — за
  интегратором после их коммитов.
- Остаётся вне второго круга: **деплой** `streamlit_app.py` на share.streamlit.io (только
  владелец аккаунта; после — ссылка в README раздел 1 и в журнал), **презентация**
  (семь слайдов по `docs/KRITERII_PLAN.md`, раздел 3, когда пользователь скажет),
  **ответы Codex** по R9–R12, Р-О1, Р-О2, A2 и A4.

**Следующий шаг:** дождаться коммитов `wf2/models` и `wf2/ui`, слить три ветки, прогнать
`python -m pytest -q` и AppTest трёх режимов × двух уровней, затем ещё раз сверить числа
README с итогом слияния.

## 2026-09-19 01:30 UTC — Claude (интегратор)

**Задача / версия:** слияние второго круга, базовый SHA `d674dba`, ветки `wf2/models`
(`7ef3874`), `wf2/ui` (`3b29696`), `wf2/docs` (`7778ba8`) → `main`, версия алгоритма 0.5.1.

**Сделано:** три ветки слиты `--no-ff` в порядке модели → экран → документы. Найден и
закрыт один смысловой конфликт слияния: ветка «модели» ввела в текст условия и карточек
имена ключей настроек (`sep_valid_hours`, `event_valid_hours`), ветка «экран» запрещает
имена ключей на оперативном уровне — перевод добавлен там же, где он и был, на выводе
(`app/ui.py`, `PHRASE_RU`); профессиональный уровень и выгрузка имя ключа по-прежнему
несут. Версия алгоритма поднята до `0.5.1`: правки меняют вердикты (окно за концом архива
теперь «оснований недостаточно» и в строгом режиме) и покрытие (без наблюдения Kp —
частичное). Девять примеров пересобраны на новой версии, числа README сверены с итогом.

**Проверено:** `python -m pytest -q` → 229 проверок и 559 подпроверок, около 62 с,
Windows, Python 3.14.6. AppTest без браузера: три режима × два уровня, «исключён»,
«отказ: только кеш», сценарий «что если», три окна, сжатие периода 360 → 120 при сдвигах
120/360 — без исключений и ошибок экрана (сдвиги разведены в 90/120, подпись о пересчёте
на месте). `python scripts/make_examples.py`; `scripts/replay_example.py` по каждому
`examples/*.json` — шесть исторических «ВОСПРОИЗВЕДЕНО», код 0; три примера текущего
режима по JSON дают код 2 («нужен ZIP») по замыслу, по ZIP — код 0. Итого 9/9 по ZIP.

**Изменилось для товарища:** вердикты и покрытие изменились (см. выше), поэтому сохранённые
примеры и числа в README другие. Отдельно: решение M11 отменяет прежнее закреплённое
поведение — окно, выходящее за конец архива, теперь не оценивается ни в разборе, ни в
строгом режиме; прежний тест утверждал обратное и переписан.

**Нужно от товарища:** прежние запросы в силе (R9–R12, Р-О1, Р-О2, A2, A4) и «ок» на новую
формулировку `CONTRACT.md` §12 R12 п. 2 (линия «минут доступности частиц канала»
реализована). Договор человек правит сам — не трогал.

**Заблокирован:** нет.

**Следующий шаг:** третий круг — стыковка веток Codex по `docs/HANDOFF_2026-09-19.md`
раздел 8 (`origin/codex/a2-integration-hardening`), затем версия 0.6.0.
