# Предзащитный чек-лист

Выполнять за 2–3 часа до защиты, по порядку. Каждый пункт — одна проверка с явным признаком «годно».
После пункта 9 код замораживается: дальше только прогоны и слова.

> Уточнение Codex для ветки 0.6.1: проверки выполнять на общем принятом SHA.
> До интеграции этой ветки облако может показывать другую версию.

## Код и сборка

1. `python -m pytest -q` в корне — зелёный, красных нет. Число проверок совпадает с README
   (на `b920bed`, слияние восьмого круга, — **663 проверки и 691 подпроверка за 372,09 с, код возврата 0**,
   Windows 11 / Python 3.14.6, прогон 19.09; на `79b2557` — 597 за 325,21 с; на `517cf28` — 573
   за 323,35 с; на `9e147bb` собиралось 564;
   на `56f30a7`, слияние пятого круга, было 502 проверки за 234,81 с; на `925f923` —
   450 за 211,18 с; на `9614128` — 408
   проверок). Если число разошлось — исправлять README и `docs/PROGRESS.md`, а не пересказывать
   по памяти.
2. `python scripts/replay_all.py` — все сохранённые расчёты воспроизведены, код возврата 0
   (на `b920bed`, коммите примеров восьмого круга: **10 из 10 «ВОСПРОИЗВЕДЕНО»**, и каждый архив
   отдельно через `scripts/replay_example.py` — тоже код 0; ожидаемая строка итога — «итого:
   воспроизведено — 10»). Если вместо «ВОСПРОИЗВЕДЕНО» стоит «ВОСПРОИЗВЕДЕНО ДРУГОЙ ВЕРСИЕЙ»
   (код 3) — результат совпал, но код расчёта менялся после сборки примеров: пересобрать
   `python scripts/make_examples.py` и закоммитить.
3. `git status -sb` — чисто, без `ahead`. Если есть `ahead`, выполнить
   `git push origin main && git push gitverse main` и дождаться пересборки облака.

## Свежесть данных

4. Обновить снимок орбитальных элементов в репозитории: получить текущий набор через слой
   A4 и обновить резерв `data/orbit/iss.tle` **вместе с его записью манифеста**
   (точные байты, SHA-256, размер, реальное время получения); проверяет А.
   `data/spaceweather/iss.tle` — справочный файл Б, активная цепочка его не читает. Признак «годно»: возраст
   эпохи меньше предела `tle_max_age_days` из настроек с запасом на сутки вперёд.
   Зачем: если во время защиты сеть недоступна, приложение возьмёт снимок; просроченный снимок
   даёт «орбита недоступна» — честно, но на защите выглядит как отказ.
5. Проверить, что живые источники отвечают: открыть сервис, посмотреть полосу состояния —
   у орбиты должно стоять «живьём» с адресом, у наблюдений — давность в минутах.

## Развёрнутый сервис

6. Открыть https://vkd-risk.streamlit.app/ за 15 минут до защиты, чтобы приложение проснулось
   (после простоя первый вход занимает до минуты).
7. Пройти три сценария и убедиться, что экран не пуст и вердикт есть:
   - текущая обстановка;
   - буря 10.05.2024, час 12 — проверяется наличие датированного предупреждения о буре/приходе CME;
     итог в 0.8.0 «все окна требуют проверки» (условие по опубликованному прогнозу прихода
     выброса с Kp до 8), предупреждение не исчезает. Число исключённых записей берётся
     из текущей выгрузки;
   - тихая дата 25.06.2024, час 12 — отсутствие условий бури различимо, итог в 0.8.0
     «предпочтительно окно 2». Рядом с вердиктом обязана стоять объявленная область вывода
     («по учтённым факторам при покрытии модели … % окна … это сравнение рассчитанных факторов,
     а не заключение о полном риске ВКД»). Вердикта без области быть не должно: до восьмого
     круга частичное покрытие давало «оснований недостаточно», теперь оно даёт вывод
     с объявленной областью, а отказ остаётся там, где покрытия нет вовсе.
8. Скачать архив выгрузки и открыть в нём читаемый отчёт: проверить, что там есть запрос,
   величины, рекомендация и сырые записи.
9. Открыть сервис с телефона: проверить, что вкладки и карточки не ломаются.

## Демонстрация

10. Держать открытыми две вкладки браузера: сервис и запись демонстрации. Если сервис не
    отвечает, переключиться на запись без пауз и комментария об ошибке.
11. Проверить, что на экране для показа нет посторонних окон, а масштаб браузера такой, что
    вердикт и обе карточки окон видны без прокрутки.
12. Иметь под рукой: досье защиты (`docs/design/DOSSIER_DEFENCE.md`) с числами и ответами,
    руководство (`docs/RUKOVODSTVO.md`), адрес репозитория.

## Сверка документов с экраном (добавлено четвёртым кругом, 19.09; уточнено пятым)

13. **Шаг жюри «дойти до первоисточника» — НА ЭКРАНЕ.** На пресете «Буря Гэннон» раскрыть вкладку
    «Объяснения» → карточку условия окна 2 и убедиться, что в строке «Первоисточник» стоит
    рабочая ссылка на `kauai.ccmc.gsfc.nasa.gov` (сообщение DONKI), а не фраза «записей без
    ссылки». То же в режиме «Сейчас»: хотя бы одна строка выпуска NOAA ведёт на
    `services.swpc.noaa.gov`. Признак «годно»: ссылка открывается в новой вкладке.
    Зачем: это прямой пункт критерия О4, и на `9614128` адрес части записей лежал
    в `metadata` и на экран не попадал. На экране исправлено четвёртым кругом и перепроверено
    интегратором прогоном `AppTest` после слияния пятого: на «Буре Гэннон» — 13 адресов,
    среди них `kauai.ccmc.gsfc.nasa.gov`; в «Историческом разборе» — 32 на оперативном уровне;
    в режиме «Сейчас» — 1 адрес на оперативном уровне (выпуск NOAA SWPC) и 4 на профессиональном
    (добавляются CelesTrak и GFZ). Число адресов в режиме «Сейчас» зависит от того, какие
    источники ответили живьём, а какие пришли из кеша, — на защите смотреть не на число,
    а на то, что хотя бы один адрес есть и открывается.
    Здесь остаётся ручная проверка того, что ссылка открывается в браузере.
    **В выгрузке закрыто пятым кругом — см. пункт 16.**
14. **Руководство рядом с экраном.** Открыть `docs/RUKOVODSTVO.md` и пройти по нему разделы 1 и 2:
    имена трёх кнопок пресетов («Сейчас», «Буря Гэннон», «Тихая дата»), порядок блоков
    (полоса состояния → вердикт → карточки окон → свёртка «Картина по времени», на оперативном
    уровне закрытая), список и порядок шести вкладок. Признак «годно»: каждое названное место
    есть на экране, и на экране нет ничего, чего нет в руководстве. Порядок правки —
    `docs/HANDOFF_2026-09-19.md`, раздел 10.3. Сверка после слияния четвёртого круга
    выполнена интегратором: имена кнопок и вкладок, порядок блоков и заголовки вердиктов
    совпали, три места руководства исправлены (строка о сверке, подпись «Первоисточник»
    и строка таблицы величин про выпуск прогноза и непокрытое наблюдение). Здесь остаётся
    прочитать руководство рядом с живым экраном ещё раз, глазами.
    **Пятый круг, 19.09 04:45 UTC:** на момент записи области «docs» у ветки `wf5/screen` не было
    ни одного коммита поверх `925f923`, поэтому руководство было сверено с `main`.
    **Сверка повторена интегратором после слияния `wf5/screen`** по дампам `AppTest` (три режима ×
    два уровня и три пресета): исправлены три места — подпись «Первоисточник» во вкладке
    «Объяснения» (две раздельные причины вместо общей фразы), состав блока под вердиктом
    (строка о происхождении допуска; охват переехал во вкладку «Окна и факторы») и пример
    «Гэннон» в разделе 5 (обе записи названы раздельно). Имена вкладок, пресетов, заголовки
    вердиктов и порядок блоков совпали и правки не потребовали. Здесь остаётся прочитать
    руководство рядом с живым экраном глазами.
15. **Ни одного «безопасно».** `grep -rin "безопасн" app/ vkd/` — на `9614128` единственное
    совпадение это комментарий в коде (`app/ui.py`, про смысл цвета), на экран он не печатается.
    Признак «годно»: новых совпадений в строках, которые выводятся зрителю, не появилось.

## Согласие выгрузки и экрана (добавлено пятым кругом, 19.09)

16. **Отчёт `report.md` из ZIP — то, что жюри уносит с собой.** На `925f923` он расходился
    с экраном в трёх местах; все три закрыты слиянием пятого круга (`app/export.py`, область
    «модели»). Перепроверено интегратором на коммите слияния прогоном `report_md(S, raw_records)`
    в трёх режимах (окна 360 мин, сдвиги 0/240 и 0/480; числа — из прогона, не из памяти):
    - первоисточники в отчёте есть. Колонки «первоисточник» в таблице больше нет — под таблицей
      «Источники и публикация» стоит перечень «Первоисточники записей» по источникам:
      «Гэннон» — 4 строки и 5 разных адресов во всём отчёте, «Тихая дата» — 3 строки и 2 адреса,
      «Исторический разбор» — 6 строк и 14 адресов, «Сейчас» — 5 адресов;
    - раздел «Проверка после отсечки» на «Тихой дате» начинается словами «условий проверки
      на отсечку не ставилось» — тем же, чем экран;
    - идентификаторов кода (`nasa_jsc_oem`, `nasa_donki_notification:…`, `celestrak_gp`,
      `noaa_swpc_3day_forecast:<хеш>`) в отчёте не осталось ни одного ни в одном из четырёх
      прогонов, ни в таблицах, ни вне их.

    Признак «годно» на месте: открыть ZIP любого примера из `examples/`, найти под таблицей
    источников строку «Первоисточники записей» и открыть один адрес в браузере. Правда про
    архив, если спросят: ссылки на первоисточники есть и на экране, и в отчёте, а сами сырые
    записи лежат в папке `raw/` архива.

## Что говорить, если что-то пошло не так

- Сервис не отвечает: «покажу запись, сервис развёрнут на бесплатной площадке и засыпает без
  посещений» — и продолжать по записи.
- Источник недоступен в момент показа: это сильная сторона, показать честный статус и отказ от
  рекомендации вместо благоприятного вывода.
- Вопрос, на который нет ответа: «не проверяли, поэтому не заявляем» — и назвать, что для этого
  нужно. Это лучше догадки: весь продукт построен на различении проверенного и непроверенного.
