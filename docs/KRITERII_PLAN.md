# План по критериям: критерий → что делаем → чем доказываем → кто → когда

Рабочий документ после разговора с трекером 19.09. Его вывод: решает подача,
код смотрят, но впечатляет презентация. Поэтому каждый критерий здесь
заканчивается строкой «как это видно на защите». В сторону не отклоняемся.

Владельцы: **А** — товарищ с Codex (данные, орбита, история),
**Б** — Тимофей с Claude (оценка, окна, объяснения, интерфейс, эксперименты, презентация).

---

> **Техническая передача А, 19.09, ветка 0.8.0 (до приёмки Б):** фактическое состояние
> описано в [передаче интегратора](integration/GRID_REFINEMENT_2026-09-19.md).
> Т3: флюенс по времени — трапеции с фактическими dt и известными концами; покрытие по секундам.
> Минуты доступности по обрезанию уже реализованы, но перенос потока GOES на станцию не выполняется.
> Т4: численный архив GOES существует для разбора; его публикация к исторической отсечке не доказана.
> Т5: добавлена включаемая проверка сходимости на одинаковом известном участке, сетки 60/30/15/5 с.
> Строгие допуски на пяти случаях не подтверждены; физическая валидация и актуализация baseline ещё не закрыты.
> Прежние формулировки ниже — план/история, не доказательство готовности новой сборки.

## 0. Решения, принятые сегодня

| развилка | решение | почему |
|---|---|---|
| площадка | **Streamlit Community Cloud**, бесплатно, ссылка сегодня | ноль администрирования, деплой из GitHub, публичная ссылка для жюри |
| домен | **не покупаем** | адрес баллов не приносит; О5 оценивает путь пользователя, а не URL. Если к вечеру 20-го всё готово — VPS и домен за час, не раньше |
| запасной показ | **видео демонстрации** 90 секунд, записано заранее | онлайн-защита, связь и сон сервиса не должны решать исход |
| сближения SOCRATES | отдельная необязательная линия | архива за 2024 нет; заключение относится к объявленному охвату |
| статистика MMOD | **одна модель: метеороиды по ECSS-E-ST-10-04C, Grün**, пластина 1 м² | согласованные единицы, применимость к 51,6° без экстраполяции; техногенные частицы не моделируются и это сказано |
| типы стыка | правки Codex (`EventInterval.kind`, `valid_from/to`, реестр в `Manifest`) принимаются, затем **заморозка** | параллельная работа без заморозки невозможна |
| окна | два-три заданных вручную + сдвиг и длительность; автоматический перебор — если останется время | постановка требует сравнения не менее двух, а не поиска |
| интерфейс | Streamlit, одна страница с шестью блоками: запрос, траектория, картина, сравнение, доказательства, данные и выгрузка. Реализовано 19.09 (0a112ea) как «ответ первым»: полоса состояния, панель вердикта, карточки окон, одна лента, вкладки «Объяснения», «Окна и факторы», «Методика», «Карта», «Наблюдения и прогнозы», «Данные» и на профессиональном уровне «Устойчивость и нормы»; запрос — в боковой панели | один путь пользователя, как оценивает О5 |
| режимы | «текущая обстановка», «исторический разбор», «прогноз из прошлого» — три явных переключателя | Т4 требует различимости |

---

## 1. Отраслевые эксперты, 50 баллов

### О1. Выбор факторов и пользовательская задача — 8

**Что проверяют:** разные механизмы, связь с ВКД, пригодность данных, помощь по конкретному окну, ограничения охвата.

**Что делаем:** две обязательные линии — космопогода на траектории и метеороиды по ECSS; сближения отдельно. Для каждой в интерфейсе карточка: наблюдаемые величины, источники, связь с ВКД, границы применимости — четыре пункта постановки дословно. Явное заявление об охвате в каждом результате: «учтено», «не учтено и почему».

**Доказательство:** блок «Охват» на экране результата; раздел в README.

**Кто, когда:** Б, день 1, после стыка типов.

**На защите:** слайд 3 «Два механизма и границы»: одна таблица, четыре столбца.

### О2. Интерпретация обстановки — 8

**Что проверяют:** физический смысл, различие измерений, условий на траектории и последствий, последовательные уровни угрозы, отсутствие необоснованных заявлений о безопасности.

**Что делаем:** каждая величина помечена одним из трёх цветов: наблюдение, внешний прогноз, наш расчёт. Поток GOES показывается как наблюдение на геостационаре и отдельно от нашего расчёта по траектории (минуты в аномалии, флюенс по прил. А); перенос потока GOES на станцию через обрезание **не выполняется** и это сказано (сверено с кодом 19.09, Т3). Ни одного слова «безопасно»: только «окно А предпочтительнее Б по механизму X на столько-то», «требует проверки», «оснований недостаточно». Список «чего не заявляем» из договора вынесен в интерфейс и в README.

**Доказательство:** легенда трёх происхождений на каждом экране; блок «Границы оценки».

**Кто, когда:** Б, день 1.

**На защите:** слайд 3, нижняя треть: «что мы не заявляем и почему». Это отличает от команд, которые пишут «безопасно».

### О3. Сравнение и рекомендация по окну — 8

**Что проверяют:** сравнение окон одинаковой длительности, пересчёт при изменении плана, видимость факторов и интервалов, компромиссы, равнозначность, отказ от рекомендации.

**Что делаем:** таблица сравнения: по строкам механизмы, по столбцам окна, в ячейках величина и минуты пересечения. Под таблицей — правило из договора, раздел 4, пунктами, и какой пункт сработал. Сдвиг начала и длительность — два отдельных элемента управления, пересчёт по кнопке. Сокращение длительности подписано «изменение плана». Вердикты — пять (v3.1, раздел 7 п. 7): предпочтительно, компромисс, равнозначны, недостаточно оснований, все окна требуют проверки.

**Доказательство:** живой пересчёт в демо; сохранённый расчёт с вердиктом «компромисс» и с вердиктом «недостаточно».

**Кто, когда:** Б, день 1, ядро.

**На защите:** демо-видео: сдвинули начало на два часа — вердикт изменился, видно почему.

### О4. Объяснения и доказательства — 8

**Что проверяют:** переход от предупреждения к значениям, времени и первоисточнику; что наблюдалось, что прогнозировалось, что рассчитано; помогает проверить, а не повторяет уровень угрозы.

**Что делаем:** карточка предупреждения по семи пунктам постановки: воздействие и значение для ВКД, период, данные и единицы, источник и время публикации, правило или модель, ограничения и уверенность. Ссылка на сырую запись в кеше и на URL источника. Три времени раздельно: событие, действие, публикация.

**Доказательство:** клик по любому предупреждению открывает карточку с ссылкой на первоисточник.

**Кто, когда:** Б, день 1 вечер.

**На защите:** слайд 6: одна карточка крупно, стрелка на сырую запись DONKI с `messageIssueTime`.

### О5. Удобство работы — 8

**Что проверяют:** самостоятельно задать окно, переключить режим, смотреть картину и сравнение, открыть источник, сохранить; статусы загрузки, ошибок и неполноты понятны.

**Что делаем:** один экран сверху вниз: запрос → траектория с эпохой и давностью → лента времени с пролётами аномалии, событиями и окнами → сравнение → предупреждения → данные и выгрузка. Индикатор состояния каждого источника: получено когда, давность, отключить, заморозить. Прогресс длительного расчёта. Три цвета, один шрифт.

**Доказательство:** жюри проходит путь само по ссылке за три минуты без подсказок.

**Кто, когда:** Б, день 1 каркас, день 2 утро — отделка.

**На защите:** демо-видео проходит весь путь без монтажа.

### О6. Презентация — 5

**Что проверяют:** логичный, понятный, интересный рассказ.

**Что делаем:** семь слайдов, у каждого в углу метка критериев, которые он доказывает, ненавязчиво. Видео 90 секунд. Три прогона вслух. Раздел 3 ниже.

**Кто, когда:** Б готовит, выступающий репетирует, день 2 после полудня.

### О7. Дополнительные функции — 5

**Что проверяют:** польза сверх обязательного, связь с основным сценарием, подтверждение примером.

**Что делаем:** одна функция, доведённая до конца: **чувствительность** — вердикт пересчитывается при вариации порога аномалии, канала GOES и параметров модели; на экране «устойчивость рекомендации». Скафандр и ML — только если день 2 свободен, и тогда как вторая функция с оговорками.

**Доказательство:** сохранённый расчёт, где вердикт «равнозначны» получен из разброса, а не из числа 5 минут.

**Кто, когда:** Б, день 2, после обязательного.

**На защите:** слайд 7, один график устойчивости.

---

## 2. Технические эксперты, 50 баллов

### Т1. Получение и подготовка данных — 7

**Что проверяют:** автоматическое получение текущих и архивных, происхождение, единицы, времена, качество, ограничения, частоты, пропуски, дубли, прослеживаемость до записи.

**Что делаем:** слой источников с единым интерфейсом: живой запрос, кеш с временем получения, архив, признаки успешности и покрытия, дедупликация по идентификатору сообщения. Реестр источников в интерфейсе и в `data/spaceweather/README.md`: величина, единицы, частота, ограничения, время публикации.

**Доказательство:** экран «Данные»: каждый источник с последним успешным получением и давностью; выгрузка содержит сырые записи.

**Кто, когда:** А, день 1 утро.

**На защите:** слайд 4, верхняя половина: реестр источников одной таблицей.

### Т2. Орбита и согласованность — 6

**Что проверяют:** положение по подходящим данным, эпоха, системы координат, историческая и текущая орбиты не смешаны, привязка фактора к траектории содержательна.

**Что делаем:** текущий режим — CelesTrak GP и SGP4; исторический — NASA OEM через интерполяцию, с раздельными временами создания, доступности и получения; пометка «реконструкция», если доступность к отсечке не доказана. На экране: источник, эпоха, давность, метод. Траектория используется в расчёте минут в аномалии и доступности частиц — не для анимации.

**Доказательство:** тест SGP4 по опубликованным тест-кейсам; экран показывает метод и эпоху.

**Кто, когда:** А, день 1.

**На защите:** слайд 5, одна фраза: «глобус только показывает уже посчитанное; минуты считаются по точкам трассы, а не по картинке». (С седьмого круга глобус на вкладке «Карта» есть, поэтому прежняя фраза «траектория считает минуты, а не рисует глобус» противоречила бы экрану.)

### Т3. Методы оценки и прогноза — 10

**Что проверяют:** воспроизводимая логика, обоснованные правила и объединение, собственный расчёт влияния на окно, связанные сигналы не множат риск, внешний прогноз отделён от нашего вклада.

**Что делаем** (формулировка сверена с кодом 19.09, см. предложение R12 в CONTRACT §12): величины по договору, раздел 3: флюенс захваченных протонов по прил. А ОСТ — интегрирование по энергии **степенным законом между узлами** (log-log; трапеции на сетке ОСТ завышали бы интеграл E⁻² более чем на 20 %, `vkd/assess/trapped.py`), **хвост выше 300 МэВ отброшен и объявлен** в правиле величины, единица част./см²; «нет модели» вне сетки L, не ноль; вход в таблицы — L и B/B_0 эксцентричного диполя (R11), объявленное приближение. **Линия «минут доступности частиц канала» через обрезание не реализована:** поток GOES показывается как наблюдение на геостационаре без переноса на станцию, жёсткость обрезания A3 остаётся в точке траектории и в оценке не участвует; это сказано в README (разделы «Методы», «Ограничения») и в CONTRACT §9. Метеороиды по ECSS/Grün с объявленной геометрией (пластина 1 м², случайная ориентация). Правило сведения без лексикографического победителя. Буря, протонное событие и радионарушение — не складываются; связанные записи одного события — одно условие. Шестичасовой собственный расчёт — пролёты по траектории; внешние прогнозы NOAA и ENLIL — отдельным слоем с горизонтом и происхождением `external_forecast`.

**Доказательство:** раздел «Методы» в README с формулами и пунктами стандартов; тесты интегрирования на аналитическом спектре (`tests/test_trapped.py`), разбора таблиц и контроля Grün (`tests/test_meteoroids*.py`); проверка согласованности прил. А и прил. К, названная проверкой, а не валидацией.

**Кто, когда:** Б, день 1 ядро, день 2 численные проверки.

**На защите:** слайд 5: схема цепочки в одну строку и три теста.

### Т4. Исторический режим и временная честность — 8

**Что проверяют:** произвольные даты периода, отсечка по публикации и версии, последующая информация только для проверки, неполный архив не маскируется.

**Что делаем:** строгий режим на датированных выпусках: уведомления DONKI по `messageIssueTime` (и `published_utc` реестра A1, если позже), NGDC `3day_forecast` и `daypre`, GOES — только доказанно доступная к отсечке версия (раздел 7 п. 6; архива наблюдений GOES за 2024 нет, канал объявляется частичным), орбита из OEM NASA/JSC — как объявленная реконструкция, пока доступность версии к отсечке не доказана (Р-О1). Фильтрация по каждому наблюдению. Карта покрытия с разрывом 15 мая — 16 июня показана честно. Три режима различимы в интерфейсе. Тест: вложенные будущие значения не меняют прогноз.

**Доказательство:** три сохранённых расчёта: 3 мая, 1 июня (внутри разрыва), 25 июня; тест отсечки в репозитории.

**Кто, когда:** А, день 1 утро — первым, до всего остального.

**На защите:** слайд 4, нижняя половина: буря Гэннон — что было известно в 19:00 10 мая и что случилось потом, две колонки.

### Т5. Эксперименты и проверка — 6

**Что проверяют:** сравнение с простым подходом на сопоставимых условиях, выраженное событие и контроль, показатели по прогнозируемой величине, ошибки и ложные тревоги, сохранённые расчёты.

**Что делаем:** два эксперимента раздельно. Первый: корректность применения внешнего прогноза NOAA к последующим наблюдениям на буре Гэннон и на тихой неделе июня — заблаговременность, пропуски, ложные тревоги, только по данным, доступным на момент. Второй: полезность нашего сравнения окон по модельной величине — насколько отличаются окна и устойчив ли выбор при сдвиге отсечки на сопоставимом наборе окон. Базовый метод «последнее наблюдение» получает только тогдашние данные.

**Доказательство:** `experiments/` с таблицей результатов и сохранёнными расчётами.

**Кто, когда:** Б, день 2 утро.

**На защите:** слайд 5, таблица из четырёх строк.

### Т6. Надёжность и повторный расчёт — 5

**Что проверяют:** отказ источника без падения и без ложного «благоприятно», кеш, ошибки, восстановление, пересчёт, статус длительной операции, запросы не смешиваются.

**Что делаем:** отключение источника переключателем; при отказе — кеш с давностью и статус «частичное покрытие»; ложного «благоприятно» нет по правилу 8. Прогресс расчёта. Состояние запроса в сессии пользователя, не глобально. Единый снимок для экрана и выгрузки.

**Доказательство:** в демо отключаем GOES — вердикт меняется на «недостаточно оснований», экран не падает. Реализовано (сверено 19.09): переключатель источника трёхпозиционный — «включён» / «отказ: только кеш» (покрытие частичное, объявлено) / «исключён: нет данных» (линия без покрытия → «оснований недостаточно»); TLE — резервная цепочка из трёх адресов; ошибка орбиты → «орбита недоступна», не заглушка (`tests/test_compare.py`, `tests/test_integration.py`).

**Кто, когда:** А — слой источников (A4, пока временный слой Б в `experiments/stub_sources.py`); Б — состояния источника, сессии и единый снимок (сделано).

**На защите:** демо-видео, последние 15 секунд: выключили источник — честный статус.

### Т7. Качество кода — 4

**Что проверяют:** структура, разделение получения, расчёта и интерфейса, настройки вне кода, проверки, обработка входа и ошибок, секреты защищены.

**Что делаем** (сверено с кодом 19.09): пакеты по договору — получение (`vkd/sources`, A4; `experiments/legacy/stub_sources.py` — запасной слой при `ImportError`), расчёт (`vkd/assess`, `vkd/windows`, `vkd/explain`), интерфейс (`app/`); настройки вне кода — `config/settings.toml` (`vkd/config.py`), переменная окружения **только `VKD_SETTINGS`** для подмены пути; **`.env` не читается**; зависимость `python-dotenv` снята из `requirements.txt` и `pyproject.toml` 19.09 — ни один модуль её не импортирует; секретов нет, источники открытые; `pytest` с обязательными тестами договора (раздел 8). Границы запроса проверяются в самом конвейере: `app.compute.validate_request` (режим, часовой пояс t0, длительность 60…480 мин, период поиска до 1440 мин, 2–3 различающихся сдвига внутри периода) вызывается из `run()` и экраном до любого запроса к источникам — вызов из скрипта с длительностью вне постановки отклоняется с русским сообщением (`ValueError`, `tests/test_models_findings.py`; T7-1 закрыт 19.09).

**Доказательство:** структура репозитория, зелёный `pytest` (450 проверок и 691 подпроверка на итоговом коммите `ae366f7` после слияния четвёртого круга, 195,78 с; на `9614128`, до слияния, было 408 проверок за 166,14 и 171,55 с), `tests/test_config.py` — неизвестный ключ настроек даёт ошибку, а не умолчание.

**Кто, когда:** оба, постоянно.

### Т8. Документация и воспроизводимость — 4

**Что проверяют:** полный код, однозначная инструкция запуска, для сохранённого запроса восстановимы данные, параметры и версия алгоритма.

**Что делаем:** README с адресом сдачи и запуском одной командой; версия Python и lock; выгрузка единым архивом: отчёт, запрос, факторы, рекомендация, источники с версиями, сырые записи, версия алгоритма; примеры сохранённых расчётов в `examples/`.

**Доказательство:** жюри восстанавливает расчёт из архива.

**Кто, когда:** Б, день 2.

---

## 3. Презентация: семь слайдов, у каждого метка критериев

| # | слайд | что на нём | критерии |
|---|---|---|---|
| 1 | Задача аналитика и ответ сервиса | один экран вердикта крупно, одна фраза о задаче | О1, О6 |
| 2 | Демонстрация | видео 90 секунд: запрос → картина → сдвиг → новый вердикт → карточка → выгрузка | О3, О5 |
| 3 | Два механизма и границы | таблица: величины, источники, связь с ВКД, применимость; внизу «чего не заявляем» | О1, О2 |
| 4 | Данные и честность времени | реестр источников; буря Гэннон в две колонки: известно к отсечке / случилось потом | Т1, Т4 |
| 5 | Методы и проверка | цепочка в одну строку; три теста; таблица сравнения с простым подходом | Т2, Т3, Т5 |
| 6 | От предупреждения к первоисточнику | одна карточка, стрелка на сырую запись | О4 |
| 7 | Границы и развитие | устойчивость вердикта; что вне охвата; репозиторий, выгрузка, запуск | О7, Т6, Т7, Т8 |

Метка критериев — мелким шрифтом в углу, без комментариев вслух. Жюри
считывает соответствие само.

**Видео:** один дубль без монтажа, курсор виден, без голоса — выступающий
говорит поверх. Записать утром 20-го, когда сервис стабилен.

**Вопросы жюри, готовые ответы:** почему нет сближений в обязательной части;
чем ваш шестичасовой расчёт является и чем не является; как вы гарантируете,
что прогноз из прошлого не видел будущего; почему одна модель метеороидов;
почему нет дозы человека; что будет при падении NOAA во время выхода.

---

## 4. Развёртывание: сделано 19.09

**Адрес сдачи: https://vkd-risk.streamlit.app/** — Streamlit Community Cloud,
репозиторий `timofey-petrov-spb/vkd-risk`, ветка `main`, точка входа
`streamlit_app.py` (она кладёт корень в `sys.path`, поэтому импорты `app.*` и
`vkd.*` работают из облака). Обновление автоматическое при пуше в `main`.

1. `requirements.txt` с точными версиями лежит в корне — Streamlit Cloud читает его.
2. Секретов нет и не планируется: все источники открытые, ключи API не нужны.
3. Ссылка стоит в `README.md` (раздел 1) и в `docs/RUKOVODSTVO.md`; в презентацию и в сдачу на GitVerse — за владельцем.
4. Бесплатный сервис засыпает без обращений и просыпается за полминуты. **Перед защитой открыть ссылку за пять минут.**
5. Запасной вариант на случай недоступности площадки: локальный запуск одной командой и показ видео.

Домен и VPS — только если 20-го после полудня всё остальное закрыто.
Ориентир, если дойдёт: российский VPS, nginx перед Streamlit, сертификат
Let's Encrypt, домен в зоне .ru; час-полтора.

---

## 5. Дополнения после сессии с экспертами 19.09

Источник: заметки с сессии. Каждый пункт эксперта → что меняется → уровень:
**ядро** (обязательно), **усиление** (после ядра), **фича** (О7, после всего).

| эксперт сказал | что делаем | уровень | критерий |
|---|---|---|---|
| риски ВКД: солнечная радиация, вспышки, мусор, сближения | наши линии покрывают все четыре: космопогода (радиация и вспышки), метеороиды статистически, SOCRATES для сближений. **Техногенный мусор статистически — добавить** как ориентировочную линию по ГОСТ Р 25645.167 с явной оговоркой: ближайшая табличная орбита 55° при 51,6°, срок модели до 2025; подпись «ориентировочно, вне области применимости» | усиление | О1, О2 |
| ориентироваться в нормах радиации | панель «Нормы» в профессиональном режиме: контрольная часовая доза по ГОСТ 25645.215-85 (85 мкЗв/ч при экспедиции 6 мес), 500 мЗв/год по ООКОКП-2004, шкала S NOAA с порогами 10/100/1000 pfu. Показываем как справочный контекст рядом с показателями среды, **не как вердикт по дозе**: дозу человека не считаем | ядро | О2, О4 |
| критические показатели требуют возвращения; предварительные не дают планировать окно | **два класса условий вместо одного «требует проверки»**: **критическое** — окно отменяется или ВКД прерывается; **ограничивающее** — окно не планируется, но идущая ВКД не прерывается. Пороги задаём сами и обосновываем: S2 и выше по наблюдению GOES — критическое; S1 или прогноз S1 внутри окна, Kp ≥ 7 по наблюдению — ограничивающее; сближение с TCA внутри окна — ограничивающее с ручной проверкой. Таблица порогов с обоснованием в интерфейсе и в README | ядро | О2, О3, Т3 |
| стресс-сценарий: что-то пошло не так, факторы изменились, сокращение или отмена | режим **«Что если»**: в текущее окно вводится событие (протонное событие в T+2 ч, скачок Kp, отказ источника, задержка работ на час) и сервис пересчитывает: сократить до момента, перенести, отменить. Помечается «моделируемый сценарий», не наблюдение. Реализуется тем же конвейером с подставленным `EventInterval` | усиление | О3, О7, Т6 |
| какие риски критические, какие нежелательные — обосновать самим | раздел «Обоснование порогов» в README с источником на каждый порог: шкалы NOAA, ГОСТ 25645.215-85, ECSS. Одна страница | ядро | О2, Т3 |
| прогноз на 24 часа | горизонт 24 ч показан явно на ленте времени; собственный расчёт покрывает 32 ч траектории; внешние прогнозы NOAA на 3 суток обрезаются до 24 ч с пометкой горизонта | ядро | Т3 |
| ГОГУ — главная оперативная группа управления, там аналитики | пользователь назван точно: аналитик ГОГУ. Слайд 1 и README: «для аналитика ГОГУ при подготовке ВКД» | ядро | О1, О6 |
| два интерфейса: профессиональный и упрощённый | переключатель «Профессиональный / Оперативный». Оперативный: вердикт, лента, три причины, кнопка «подробнее». Профессиональный: всё — три происхождения, сырые записи, нормы, покрытие, версии. Один путь пользователя, два уровня детализации | усиление | О5 |
| время отклика минимальное | траектория и поле считаются один раз на запрос и кешируются; сдвиг окна и длительность пересчитывают только оценку — цель до 2 с; прогресс показывается для расчётов дольше 3 с | ядро | О5, Т6 |
| выбрать ровно те объекты мусора, что на орбите МКС и опасны | SOCRATES фильтруется по станции (NORAD 25544 в любом из двух столбцов), по TCA внутри окна и по промаху; показываем список с промахом, скоростью и возрастом элементов. Вероятность попадания в космонавта не выводим — постановка это запрещает, и мы говорим почему | ядро | О2, Т1 |
| большие языковые модели не нужны | не используем. В README одна строка: «языковые модели в расчёте не применяются, данные не текстовые» | ядро | Т3 |
| локально или облако — обосновать; локальная работа даёт баллы за фичи | сервис работает и по ссылке, и локально одной командой без облака — это уже так. Обосновываем: локальный запуск для ГОГУ без внешней зависимости; облако для доступа жюри | усиление | О7, Т8 |
| экономика: дешёвое и эффективное лучше дорогого | слайд 7: стоимость эксплуатации — ноль, только открытые источники, без платных данных и без GPU. Одна строка | усиление | О7 |
| дополнительные фичи — выигрышный фактор | порядок фич после ядра: 1) устойчивость вердикта, 2) режим «Что если», 3) оперативный интерфейс, 4) техногенный мусор ориентировочно, 5) **настольная сборка с установщиком** через PyInstaller того же приложения — эксперты сказали, что это зачтётся, но только после полного соответствия; C++ и Qt не возвращаются, упаковывается тот же Python | фича | О7 |

**Не меняется:** охват обязательной части, правило рекомендации, строгий
исторический режим, площадка. Всё выше либо уточняет ядро, либо ложится
сверху после него. Порядок фич — строго как в последней строке.

## 6. Согласованная матрица ролей

Единственный источник по ролям. Идентификаторы задач A1–A5 и B1–B6 и рубежи
I0–I4 — из `docs/TEAM_SYNC.md`, здесь они привязаны к критериям и к пунктам
экспертов. Согласование: Codex пишет в `journal/friend.md` «роли согласованы»
или список правок; Claude — то же в `journal/tim.md`. До двух записей матрица
не считается принятой.

### Кто есть кто

| роль | человек | ассистент | одним словом |
|---|---|---|---|
| **А** | Александр | Codex | **данные и время**: получить, сохранить, доказать, что было известно когда |
| **Б** | Тимофей | Claude Code | **оценка и подача**: посчитать, сравнить, объяснить, показать, сдать |

Правило границы: А отвечает за всё до типов `TrajectoryPoint`,
`EnvironmentSample`, `EventInterval`, `Conjunction`; Б — за всё после них.
Типы пишет Б, согласует А, потом заморозка.

### Матрица: критерий → владелец → задачи → доказательство → рубеж

| критерий | баллы | владелец | задачи | что предъявляем | рубеж |
|---|---|---|---|---|---|
| О1 факторы | 8 | Б, спецификация линий от А | B2, A5 | блок «Охват» на экране, карточки четырёх пунктов по линии | I2 |
| О2 интерпретация | 8 | **Б** | B2, B3 | три происхождения цветом, два класса условий с порогами и источниками, «чего не заявляем» | I2 |
| О3 сравнение окон | 8 | **Б** | B3 | таблица окон, правило с пунктом, сдвиг и длительность раздельно, четыре вердикта | I2 |
| О4 объяснения | 8 | **Б**, записи от А | B4, A1 | карточка семи пунктов, ссылка на сырую запись и URL | I2 |
| О5 удобство | 8 | **Б** | B4, B6 | один путь сверху вниз, статусы источников, прогресс, два уровня интерфейса | I3 |
| О6 презентация | 5 | **Б готовит**, выступающий репетирует | B6 | семь слайдов с метками, видео 90 с, три прогона | I4 |
| О7 дополнительно | 5 | Б, «Что если» с типом от А | B5 | устойчивость вердикта; далее по порядку фич | после I4 |
| Т1 данные | 7 | **А** | A1, A4 | экран «Данные»: получение, давность, отключение; реестр с единицами и публикацией | I1, I3 |
| Т2 орбита | 6 | **А** | A3 | SGP4 и OEM, эпоха и метод на экране, тест-кейсы SGP4, реконструкция помечена | I1 |
| Т3 методы | 10 | **Б**, спецификация ECSS от А | B2, B3, A5 | README «Методы» с формулами и пунктами; тесты интегрирования и разбора; горизонт 24 ч | I2, I4 |
| Т4 история | 8 | **А** | A2 | три отсечки: 3 мая, 1 июня, 25 июня; тест вложенного будущего; карта покрытия с разрывом | I1 |
| Т5 эксперименты | 6 | **Б**, данные отсечек от А | B5, A2 | два эксперимента, таблица метрик, сохранённые расчёты | I4 |
| Т6 надёжность | 5 | **А** источники, **Б** сессии и снимок | A4, B4 | отключили источник — честный статус; две независимые сессии; единый снимок | I3 |
| Т7 код | 4 | оба, интегрирует Б | B6 | пакеты по договору, конфиг вне кода, `pytest` зелёный | I4 |
| Т8 воспроизводимость | 4 | **Б**, lock от А | B1, B6 | README с адресом сдачи и одной командой запуска, точные версии зависимостей, выгрузка единым архивом, примеры | I4 |

Сумма по владению: А — 26 баллов напрямую (Т1, Т2, Т4, половина Т6),
Б — 61 напрямую, остальное общее. Это не про важность, а про то, кто
отвечает за доказательство на рубеже.

### Пункты экспертов → владелец

| пункт | владелец | куда |
|---|---|---|
| два класса условий и пороги | **Б** формулирует, **А** подтверждает источники | B3, договор v3.1 |
| SOCRATES только по станции, TCA в окне, промах | **А** | A5 |
| горизонт 24 ч, траектория 32 ч | **А** траектория, **Б** лента | A3, B4 |
| панель норм как контекст | **Б** | B4 |
| режим «Что если» | **Б**, `EventInterval` с `simulated` от **А** | B3, A2 |
| два уровня интерфейса | **Б** | B4 |
| время отклика | **А** кеш поля, **Б** кеш оценки | A3, B4 |
| техногенный мусор ориентировочно | **А** спецификация, **Б** расчёт | A5, B2 |
| настольная сборка | **Б**, после всего | B6 |

### Ежедневный ритм

Каждые 60–90 минут — короткая ветка, второй проверяет, Б сливает в `main`.
На рубежах I0–I4 оба проверяют один SHA. Журналы по шаблону из инструкций
ассистентам. Договор правят только люди.

## 7. Поправки по разбору Codex от 19.09 — имеют приоритет над текстом выше

Разбор в `journal/friend.md`, запись 2026-09-18 20:35 UTC. Принято:

1. **Условия проверки — не команды.** Формулировки «ВКД прерывается» и
   «идущая ВКД не прерывается» из разделов 5 и 6 отменены. S ≥ 3 —
   приоритетное предупреждение, срочная проверка специалистом; S1–S2 и Kp ≥ 7 —
   предупреждение и триггер проверки; исключение из автовыбора — политика
   прототипа с проверкой чувствительности. Сближение с TCA в окне — ручная
   оценка без порога промаха как вероятности.
2. **Зависимости и lock — Б**, А проверяет на macOS. Python на Streamlit
   Community Cloud выбирается в Advanced settings; `runtime.txt` убран;
   `requirements.txt` — точные версии.
3. **Сценарий «Что если»** — схема и реализация у Б (B1, B3, B4); А проверяет
   происхождение и контрольный пример. Тип события хранит физический
   `kind_of_event` и флаг `is_simulated`; задержка работ — свойство запроса;
   отказ источника — статус источника; сценарий помечает весь результат.
4. **Горизонт.** Период начала до 24 ч; траектория и пригодные прогнозы —
   до конца последнего окна, до 32 ч; горизонт каждого канала показан отдельно.
5. **Обязательный охват** — космопогода и природные метеороиды; сближения
   отдельно; техногенная статистика вне охвата до конца обязательного.
   Исследование ГОСТ 167 — после I4.
6. **Строгая история.** «GOES по времени наблюдения» заменено на «доказанно
   доступная к отсечке версия значения». Контрольные отсечки: 01.05 00:00Z,
   20.05 12:00Z, 30.06 12:00Z из TEAM_SYNC плюс демо-даты 3 мая, 1 июня,
   25 июня с отсечкой 12:00Z, длительностью 6 ч и поиском 24 ч.
7. **Пять исходов рекомендации** сохраняются; «все требуют проверки» отличается
   от «недостаточно данных». Отказ источника с пригодным кешем не обязан давать
   отказ от рекомендации.
8. **Не заявлять недоказанное.** Суммы баллов по людям из раздела 6 удалены как
   не имеющие официального основания. «Стоимость ноль» читать как «без
   обязательных платных API и GPU, бесплатный тариф с ограничениями».
   Локальный автономный повтор — по сохранённому набору; текущие данные
   требуют сети.
9. **Нормы** — справочный контекст; «500 мЗв/год» помечено как вторичный
   источник до проверки первичного текста.

## 8. Порядок дней — совпадает с планом Codex

**День 1.** Утро: стык и типы заморожены; история доказана на трёх датах (А); каркас экрана и правило (Б). День: живые источники, траектория, кеш, отключение (А); оценка, сравнение, карточка предупреждения (Б). Вечер: сквозной путь работает по ссылке.

**День 2.** Утро: строгая отсечка с тестом, надёжность (А); эксперименты, выгрузка, чувствительность (Б); запись видео. День: README, примеры, презентация, три прогона. После полудня код не трогать.

## 9. Состояние по критериям к защите — сверка плана с кодом 19.09

База `main` после слияния четвёртого круга, итоговый коммит `ae366f7`; тесты 450 проверок
и 691 подпроверка, код возврата 0 (195,78 с; на `9614128`, до слияния, было 408 проверок);
AppTest трёх режимов × двух уровней, трёх пресетов,
«исключён», «отказ», сценария, трёх окон и сжатия периода — без исключений;
повтор примеров 6/6 исторических по JSON и 9/9 по ZIP.
Подробно — `docs/PROGRESS.md`, раздел 1. Здесь только: что из обещанного выше
есть, чего нет, и что говорить на защите вместо обещания.

| критерий | сделано | нет или иначе, чем в плане | чем показываем |
|---|---|---|---|
| О1 факторы | две обязательные линии, охват «учтено / не учтено и почему» в панели вердикта и карточках | сближения SOCRATES не подключены — заявление об охвате это называет | панель вердикта, вкладка «Окна и факторы» |
| О2 интерпретация | три происхождения у каждой величины; слова «безопасно» нет; «чего не заявляем» в README | перенос GOES на станцию через обрезание не делается (сказано) | карточки величин, README раздел «Чего не заявляем» |
| О3 сравнение окон | два-три окна, сдвиг и длительность раздельно, пять исходов, правило с номером пункта, допуск из разброса устойчивости | автоматического перебора окон нет (и не обещался как обязательный) | примеры `gannon_*` (`all_need_check`), `end_2024-06-25_12Z` (`preferred`, при этом вердикт меняется на сетке порогов — так и написано на экране) |
| О4 объяснения | карточки по семи пунктам, три времени раздельно, ссылка на сырую запись и первоисточник (URL выпуска NOAA, `messageURL` DONKI) | — | вкладка «Объяснения», профессиональный уровень |
| О5 удобство | один путь: боковая панель запроса → полоса состояния → вердикт → карточки окон → лента → вкладки; статусы источников; два уровня | баннеров `st.info`/`st.warning` нет — состояние показано плашками в полосе состояния («Исключено отсечкой N записей», «Сценарий: моделируемые значения»); README приведён к этому словарю 19.09 | живой проход по ссылке или локально |
| О6 презентация | план слайдов — раздел 3 | слайды и видео ещё не сделаны | — |
| О7 дополнительно | устойчивость вердикта (сетка порогов, допуск из разброса); «Что если»; оперативный уровень | техногенный мусор, настольная сборка, скафандр — не делались | вкладка «Устойчивость и нормы», примеры `whatif_*` |
| Т1 данные | реестр 2024 (A1), GOES/Kp/TLE живые с кешем и тремя состояниями, дедупликация DONKI, сырые записи в выгрузке; слой живых источников — `vkd.sources` (A4), разбор истории — `vkd.history` (A2), архивы наблюдений GOES и GFZ за 2024 год поставлены и подключены (`data/goes_2024`, `data/gfz_2024`) | в «Прогнозе из прошлого» численного GOES нет: у записей архива нет доказанного времени публикации, и отсечка их отбрасывает — объявленное ограничение, а не пробел | вкладка «Данные» |
| Т2 орбита | SGP4 по TLE с цепочкой адресов, IGRF-14; история — OEM NASA/JSC, IGRF-13; тесты SGP4 по AIAA 2006-6753 (А); метод и эпоха на экране | в истории орбита — объявленная реконструкция; L/B_0 для таблиц — эксцентричный диполь, не трассировка | полоса состояния, `trajectory_meta.provenance` |
| Т3 методы | см. раздел Т3 выше: степенной закон по энергии, хвост объявлен, ECSS/Grün против Table J-5, одно условие на событие, внешний прогноз отделён | «минут доступности частиц канала» нет (R12) | README «Методы», `tests/test_trapped.py`, `tests/test_meteoroids*.py` |
| Т4 история | отсечка по публикации и по вложенным значениям; прогнозы NOAA из выпусков до отсечки; разрыв 15.05–16.06 объявлен; три режима различимы; в «Историческом разборе» канал GOES — численное наблюдение архива NASA iSWA | в «Прогнозе из прошлого» численного GOES нет (нет доказанного времени публикации); наблюдение Kp там приходит из тела опубликованного уведомления DONKI | примеры 03.05, 10.05 (12Z и 19Z), 20.05, 25.06; `tests/test_cutoff.py` |
| Т5 эксперименты | буря Гэннон и контроль, 964 окна, система против базы «последнее наблюдение» (27/0/18 против 18/9/10 на буре, 1/4/14 против 0/5/0 на контроле), чувствительность — к границе объявленного в уведомлении диапазона Kp (`max` против `min`) | — | `docs/EKSPERIMENTY_PROGNOZ.md`, `experiments/` |
| Т6 надёжность | три состояния источника, кеш с давностью, «недостаточно» вместо ложного «благоприятно», единый снимок, состояние в сессии | прогресс — `st.spinner` на трёх шагах (источники, траектория и оценка, область аномалии), отдельной шкалы прогресса нет | демо «GOES исключён» |
| Т7 код | три слоя, `config/settings.toml` (в снимке — `effective_config`), `VKD_SETTINGS`, проверка границ запроса в `run()`, зелёный `pytest`, ни одной неиспользуемой зависимости (`python-dotenv` снят 19.09) | `app/main.py` — длинный сценарий верхнего уровня; рефакторинг перед защитой не начинаем | структура репозитория |
| Т8 воспроизводимость | README с адресом сдачи и одной командой запуска, `requirements.txt` с точными версиями и без неиспользуемых зависимостей, ZIP-выгрузка с манифестом и `git_commit`, 9 примеров, `scripts/replay_example.py`, `scripts/replay_all.py`; числа тестов в README привязаны к коммиту, на котором измерены (итоговый `ae366f7`: 450 проверок и 691 подпроверка за 195,78 с; на `9614128` было 408) | примеры пересобраны на коммите слияния четвёртого круга `ac2a84e`, версия алгоритма `0.6.1` (см. `examples/INDEX.md`) | повтор примера при жюри |
