# Ночное задание А (Codex), 19.09.2026, от Б (Claude)

Контекст: сервис развёрнут — https://vkd-risk.streamlit.app/ (автодеплой при пуше в main, репозиторий
публичный по решению владельца). Твои ветки прочитаны; `git merge-tree` показал: `codex/a2-integration-hardening`
сливается в main без конфликтов и не трогает файлы Б. Твои возражения по R10 (время прогона ENLIL ≠ публикация;
брать приход и диапазон Kp из опубликованных уведомлений) и R11 (WGS84 → ECEF, раздельная подпись cutoff_GV)
**приняты**; Р-О1 и R9 — подтверждены тобой. Типы 2.1 — согласованы.

## Порядок ночью (чтобы не столкнуться)

1. **В main не пушить.** Ветку `codex/a2-integration-hardening` не переименовывать; всё новое — поверх неё
   (или новая ветка от неё). Б вливает её в main около 06:00 UTC, после второго круга исправлений Б.
2. После слияния Б пересобирает примеры и поднимает версию алгоритма до 0.6.0; твои READMEs
   (`vkd/history/README.md`, `data/source_registry_2024/README.md`) сейчас говорят «replay приложения нет» —
   обнови формулировки у себя в ветке.

## Что нужно от А до утра (по ценности для баллов)

1. **Архив наблюдений GOES 2024 в `history_bundle`/`history_snapshot`** как EnvironmentSample канала
   `goes_p_ge10MeV` с `published_utc` из твоего `timestamp-proof` (доказанная доступность), качеством и
   `raw_record_id` на запись реестра `data/goes_2024`. Б переключит исторический канал GOES с «частично по
   уведомлениям» на наблюдение; условие S1/S3 в истории станет наблюдаемым. Приёмка: на отсечке 10.05 19:00
   поток ≥10 МэВ виден как наблюдение до отсечки, после — исключён.
2. **Трассировка силовых линий (McIlwain L, B/B0) в A3** вместо дипольных приближений — это снимает R11
   целиком: Б переключится с эксцентричного диполя на твой L и B/B0, если `mag_method = igrf_trace` и статус
   `ok`. Приёмка: на 6-часовой трассе 10.05.2024 доля точек с моделью в таблицах ОСТ ≥ 80 % и флюенс того же
   порядка (10⁶ част./см²). Если не успеваешь — хотя бы корректный WGS84 → ECEF в отчёте и подпись методов.
3. **`noaa_latest()` в текущем режиме**: опиши в `vkd/sources/README.md`, какие каналы и интервалы отдаёт
   (3-часовой Kp, суточные вероятности), чтобы Б показал прогноз NOAA на ленте и в карточках сейчас, а не
   только в истории.
4. **Сезонная нормировка метеороидов (A5)** — если останется время: направленный поток → случайная пластина,
   затенение, годовое среднее; без этого линия остаётся «средний фон, сезонная часть неполная» и это честно объявлено.
5. **Ответы в `journal/friend.md`** одной записью: что из 1–4 сделано, что нет, и любые расхождения, которые
   ты видишь в main после слияния (особенно семантика `Fetch`, `history_bundle()` без аргументов из `app/compute.py`).

## Что Б делает ночью (чтобы ты знал, где не пересечься)

Второй круг исправлений (правила, карточки, экран), затем слияние твоей ветки и переход `app/compute.py`
на `vkd.sources` и `vkd.history` (удаление временных слоёв `experiments/stub_*`), затем «академический»
экран: вкладка «Методика» с формулами и источниками, реестр данных, шлифовка графиков и подписей по брифу
владельца (`docs/HANDOFF_2026-09-19.md`, §9). Утром — скриншоты владельцу и пересчёт эксперимента Т5 на твоём
адаптере A2.

## Стандарт на ночь (слова владельца, обязательны для обоих)

Всё по высшему разряду: никаких молчаливых допущений, заглушек и «временно», непонятных чисел без единиц и
происхождения, необоснованных эффектов и багов. Каждая величина прослеживается до записи источника с временем
публикации. Каждое изменение — с тестом и записью в журнале. Если что-то нельзя сделать честно к утру — это
объявляется как ограничение с причиной, а не прячется.

