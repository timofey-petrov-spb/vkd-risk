# Эксперимент Т5 по ВЕРДИКТУ: сквозной перебор отсечек, 01.05.2024 — 30.06.2024

Сгенерировано `experiments/verdicts.py` (2026-09-19 14:03Z), одна команда: `python experiments/verdicts.py`. Сохранённые расчёты по каждой отсечке — `examples/experiments/verdicts.json`; все числа ниже собраны из этого файла, а не набраны руками.

## Что именно меряется

Существующий эксперимент `docs/EKSPERIMENTY_PROGNOZ.md` считает срабатывания ОТДЕЛЬНЫХ ЛИНИЙ условий — промежуточную величину. Критерий Т5 требует показателей по ПРОГНОЗИРУЕМОЙ ВЕЛИЧИНЕ, а прогнозируемая величина сервиса — это ВЕРДИКТ и НАЗВАННОЕ ОКНО. Здесь на каждой отсечке запускается полный расчёт тем же кодом, что и приложение (`app.compute.run`, режим «прогноз из прошлого»), и считается то, что видит пользователь.

- период: 01.05.2024 00Z — 30.06.2024 23Z, шаг отсечки **6 ч** (обязательное «не реже раза в 12 ч» перекрыто вдвое), всего отсечек **244**;
- постановка запроса — умолчания приложения из `config/settings.toml` [ui]: длительность ВКД 360 мин, период поиска 720 мин, два окна-кандидата со сдвигами 120 и 360 мин. Под результат ничего не подбиралось;
- пороги — из настроек приложения: `kp_check = 7`, `goes_p10_warning_pfu = 10` (версия алгоритма `0.10.0`);
- прогон занял **7 мин 49 с** (1.92 с на отсечку в среднем), сеть не использовалась.

### Как вердикты сведены в три исхода

| вердикт сервиса | исход | смысл |
|---|---|---|
| `preferred` | назвал окно | одно окно предпочтительнее |
| `equivalent` | назвал окно | окна равнозначны, годится любое из названных |
| `all_need_check` | решение аналитика | все окна под условием, правило выбирать не вправе |
| `trade_off` | решение аналитика | механизмы указывают на разные окна |
| `insufficient` | отказался | нет покрытия обязательной линии |

### Определения показателей

- **ПРОПУСК** — сервис назвал окно, а по ОКОНЧАТЕЛЬНЫМ архивным данным в этом окне событие всё-таки было: буря Kp ≥ 7 в 3-часовом интервале ряда GFZ, пересекающем окно, или наблюдённый поток GOES ≥10 МэВ ≥ 10 pfu в пятиминутной ячейке, пересекающей окно;
- **ЛОЖНАЯ ТРЕВОГА** — условие поставлено хотя бы на одно окно отсечки, а по факту ни в одном окне этой отсечки не было ни бури, ни протонного события;
- факт берётся теми же функциями, что в `experiments/forecast_lines.py`, — у двух экспериментов Т5 определение факта буквально одно.

## Простой подход для сравнения

**«Последнее наблюдение»**: решение принимается по последнему завершённому наблюдению на отсечку — если последний окончательный 3-часовой Kp GFZ ≥ 7 или последний пятиминутный поток GOES ≥10 МэВ ≥ 10 pfu, подход объявляет условие на оба окна («решение аналитика»); если наблюдений до отсечки нет вовсе — отказывает; иначе называет ближайшее окно-кандидат. Ни прогноза, ни траектории, ни сравнения окон у него нет.

Почему это честный соперник, а не соломенное чучело:

1. он решает ТУ ЖЕ задачу на ТЕХ ЖЕ отсечках и тех же двух окнах-кандидатах, теми же порогами;
2. ему сознательно дано БОЛЬШЕ данных, чем сервису: окончательный ряд Kp GFZ и численный архив GOES, которые строгий режим использовать не вправе (историческая публикация именно этих версий не доказана, CONTRACT §10). Проверка публикации к нему не применяется;
3. это ровно тот порядок, которым обстановку оценивают «на глаз» по последнему индексу, — его и надо побеждать, чтобы сервис имел смысл;
4. он проигрывает (или выигрывает) не из-за нехватки данных, а только из-за отсутствия прогноза и траектории — то есть сравнение адресует именно то, что сервис добавляет.

## Сервис против простого подхода: весь период

| показатель по вердикту | сервис | простой подход «последнее наблюдение» |
|---|---:|---:|
| назвал окно | 222 | 218 |
| отказался | 1 | 0 |
| решение аналитика | 21 | 26 |
| расчёт не состоялся | 0 | 0 |
| **пропуски** (названо окно, а событие в нём было) | **15** | **6** |
| **ложные тревоги** (условие поставлено, события не было ни в одном окне) | **4** | **0** |
| отсечек, где условие вообще ставилось | 22 | 26 |
| всего отсечек | 244 | 244 |

Как читать: строки «назвал окно / отказался / решение аналитика» — это распределение исходов, а не оценка качества; качество — в двух выделенных строках.

### Из чего сложилось распределение вердиктов сервиса

| вердикт | отсечек | доля |
|---|---:|---:|
| `preferred` (предпочтительное окно) | 167 | 68,4 % |
| `equivalent` (окна равнозначны) | 55 | 22,5 % |
| `all_need_check` (все окна под условием) | 21 | 8,6 % |
| `insufficient` (оснований недостаточно) | 1 | 0,4 % |

### Ошибки сервиса по видам события (по окнам, весь период)

| вид события | попадания | пропуски | ложные тревоги | верные отказы | факт неизвестен |
|---|---:|---:|---:|---:|---:|
| буря | 14 | 3 | 19 | 451 | 1 |
| протонное событие | 24 | 36 | 0 | 425 | 3 |

### Ошибки простого подхода по видам события (по окнам, весь период)

| вид события | попадания | пропуски | ложные тревоги | верные отказы | факт неизвестен |
|---|---:|---:|---:|---:|---:|
| буря | 11 | 6 | 3 | 467 | 1 |
| протонное событие | 50 | 10 | 2 | 423 | 3 |

Бурю и протонное событие не складываем (договор): каждая строка считается против своей величины. Столбец «факт неизвестен» — окна, на которые соответствующего ряда наблюдений в архиве нет; записывать их в «ничего не было» нельзя.

## Чтение результата: где сервис выигрывает и где проигрывает

**По пропускам сервис проигрывает простому подходу: 15 против 6.** Это неудобный для нас результат, и он не смягчается.

| линия | пропуски сервиса | пропуски простого подхода | ложные тревоги сервиса | ложные тревоги простого подхода |
|---|---:|---:|---:|---:|
| буря | 3 | 6 | 19 | 3 |
| протонное событие | 36 | 10 | 0 | 2 |

Разложение по линиям показывает, откуда берётся разница:

1. **Протонное событие.** Простой подход видит ЧИСЛЕННЫЙ архив GOES и ловит событие в ту же минуту, когда поток перешёл порог. Строгий режим сервиса этот архив использовать НЕ ВПРАВЕ (историческая публикация именно этой версии не доказана, CONTRACT §10) и знает о протонном событии только из уведомлений DONKI, которые выходят позже. Счёт пропусков по окнам: 36 у сервиса против 10 у простого подхода — это ЦЕНА строгости, и её надо знать, а не прятать за средним числом.
2. **Буря.** Сервис опирается на опубликованный прогноз прихода выброса и прогноз NOAA, поэтому может поставить условие ЗАРАНЕЕ, а простой подход узнаёт о буре, только когда она уже в последнем наблюдении. Счёт по окнам: попаданий 14 у сервиса против 11, ложных тревог 19 против 3 — заблаговременность подтверждается и оплачивается ложными тревогами.
3. **Осторожность вердикта.** Решения аналитика сервис потребовал на отсечках числом 21, простой подход — 26. Каждое такое требование — это отказ назвать окно, и неназванное окно в счёт пропусков не попадает ни у кого: часть разницы по пропускам объясняется именно этим, а не точностью.

Вывод, который отсюда следует и который нельзя подменить: **по протонному событию сервис в строгом режиме уступает простому подходу, потому что у простого подхода есть численный ряд наблюдений, а у строгого режима его нет.** Закрыть этот разрыв можно доказанной исторической публикацией архива GOES, а не изменением правила; пока она не доказана, разрыв остаётся и объявляется.

## Промахи сервиса поимённо

Каждая строка — отсечка, на которой сервис НАЗВАЛ окно, а по окончательным архивным данным в этом окне событие было.

| отсечка (UTC) | названное окно | что было по факту | вердикт |
|---|---|---|---|
| 2024-05-14 12:00 | 2024-05-14 18:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-05-14 18:00 | 2024-05-14 20:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-05-15 00:00 | 2024-05-15 02:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-05-15 06:00 | 2024-05-15 12:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-05-15 12:00 | 2024-05-15 18:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-05-15 18:00 | 2024-05-16 00:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-05-16 00:00 | 2024-05-16 02:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-05-16 06:00 | 2024-05-16 12:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-06-07 18:00 | 2024-06-08 00:00 | поток GOES ≥ 10 pfu | `equivalent` |
| 2024-06-08 00:00 | 2024-06-08 02:00 | поток GOES ≥ 10 pfu | `equivalent` |
| 2024-06-08 00:00 | 2024-06-08 06:00 | поток GOES ≥ 10 pfu | `equivalent` |
| 2024-06-09 00:00 | 2024-06-09 06:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-06-09 06:00 | 2024-06-09 12:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-06-09 12:00 | 2024-06-09 14:00 | поток GOES ≥ 10 pfu | `preferred` |
| 2024-06-09 18:00 | 2024-06-09 20:00 | поток GOES ≥ 10 pfu | `equivalent` |
| 2024-06-28 06:00 | 2024-06-28 08:00 | буря Kp ≥ 7 | `preferred` |

Строк в таблице 16, а пропусков по вердиктам 15: счёт по вердиктам идёт по ОТСЕЧКАМ, и отсечка с двумя названными равнозначными окнами, оба из которых оказались неудачными, считается одним пропуском. Правило, которым окно было названо, по каждой строке лежит в `examples/experiments/verdicts.json` (ключ `rows[].service.rule`).

Для сравнения, промахи простого подхода: **6** (список — в `verdicts.json`, `rows[].baseline`).

## Ложные тревоги сервиса по дням

2024-05-12: 2, 2024-05-13: 1, 2024-06-28: 1

## Выраженное событие: буря Гэннон, отсечки 10.05–12.05 (всего 12)

| показатель по вердикту | сервис | простой подход «последнее наблюдение» |
|---|---:|---:|
| назвал окно | 1 | 5 |
| отказался | 0 | 0 |
| решение аналитика | 11 | 7 |
| расчёт не состоялся | 0 | 0 |
| **пропуски** (названо окно, а событие в нём было) | **0** | **2** |
| **ложные тревоги** (условие поставлено, события не было ни в одном окне) | **2** | **0** |
| отсечек, где условие вообще ставилось | 11 | 7 |
| всего отсечек | 12 | 12 |

### Ошибки сервиса на буре (по окнам)

| вид события | попадания | пропуски | ложные тревоги | верные отказы | факт неизвестен |
|---|---:|---:|---:|---:|---:|
| буря | 14 | 0 | 8 | 2 | 0 |
| протонное событие | 11 | 7 | 0 | 6 | 0 |

## Контрольный период: 2024-05-17 — 2024-06-07 (всего отсечек 88)

| показатель по вердикту | сервис | простой подход «последнее наблюдение» |
|---|---:|---:|
| назвал окно | 88 | 88 |
| отказался | 0 | 0 |
| решение аналитика | 0 | 0 |
| расчёт не состоялся | 0 | 0 |
| **пропуски** (названо окно, а событие в нём было) | **1** | **0** |
| **ложные тревоги** (условие поставлено, события не было ни в одном окне) | **0** | **0** |
| отсечек, где условие вообще ставилось | 0 | 0 |
| всего отсечек | 88 | 88 |

### Ошибки сервиса на контроле (по окнам)

| вид события | попадания | пропуски | ложные тревоги | верные отказы | факт неизвестен |
|---|---:|---:|---:|---:|---:|
| буря | 0 | 0 | 0 | 176 | 0 |
| протонное событие | 0 | 1 | 0 | 175 | 0 |

Контрольный отрезок выбран ПРАВИЛОМ ПО ДАННЫМ, а не под результат: самый длинный непрерывный отрезок целых суток периода, в котором по окончательным архивным данным нет ни бури Kp ≥ 7, ни протонного события ≥ 10 pfu. Границы пересчитываются при каждом прогоне.

На контроле у сервиса пропусков числом 1 — при том, что отрезок выбран как спокойный. Противоречия нет: отрезок спокоен ПО СУТКАМ, а горизонт поиска (1080 мин) выносит окна за его правую границу, где событие уже начиналось. Первое такое окно — 2024-06-08 00:00. Это граница самого способа резать период сутками, и она названа, а не спрятана.

## Границы применимости: где расчёт не состоялся и почему

Расчёт **состоялся на всех 244 отсечках**: ни одного исключения в конвейере. Это отдельный результат — строгий режим не падает ни в разрыве архива выпусков, ни на границах периода.

Отказ вердиктом («оснований недостаточно») — это НЕ несостоявшийся расчёт, а состоявшийся вывод: таких отсечек **1** из 244 (0,4 %).
- 1 раз: космопогода на траектории: DONKI: окно за границей архива уведомлений; полнота событий на непокрытом участке неизвестна

Прогнозная линия Kp NOAA на горизонт есть не всегда — это второй предел применимости, и он считан отдельно по причинам, а не одним словом «разрыв»:

| состояние линии прогноза NOAA | отсечек | доля |
|---|---:|---:|
| полное покрытие горизонта (`full`) | 118 | 48,4 % |
| частичное покрытие (`partial`) | 2 | 0,8 % |
| выпуска на горизонт нет (`missing`) | 124 | 50,8 % |

Из **124** отсечек без прогноза **123** приходятся на РАЗРЫВ АРХИВА ВЫПУСКОВ NOAA 15.05—16.06.2024 (последний допустимый выпуск до отсечки — от 14.05 12:30Z, его горизонт до запрошенного периода не достаёт), и **1** — на другие причины (начало периода: допустимого выпуска до отсечки ещё нет). Это «нет данных», а не «нет условия»: в этих отсечках сервис работает без внешнего прогноза, и его показатели там относятся к обстановке по уведомлениям DONKI и к траектории. Доля периода без внешнего прогноза — 50,8 %: это надо читать вместе с таблицами выше, а не после них.

Окон, на которые численных наблюдений GOES в архиве нет (факт по протонному событию неизвестен): **3** из 488 (0,6 %) — они исключены из счёта по протонному событию, а не записаны в «ничего не было».

## Что этот эксперимент НЕ показывает

- выраженное событие за период ОДНО (буря Гэннон): числа по нему — разбор случая, а не оценка вероятностей; доверительных интервалов здесь нет и быть не может;
- «пропуск» здесь означает, что в названном окне по архиву было событие, а не что ВКД в нём закончился бы происшествием: связь события с исходом работ сервис не заявляет (CONTRACT §9);
- простой подход получает окончательные данные, то есть сравнение поставлено НЕ в нашу пользу; обратное сравнение (оба на строгом режиме) дало бы сервису преимущество по построению и потому не считалось;
- отсечки внутри одного события не независимы: соседние отсечки видят одни и те же уведомления, поэтому числа по отсечкам — это доля времени, а не доля независимых случаев.
